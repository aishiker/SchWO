# Phase 5 T4ae Equivalence-Preserving Methods Gate

Date: 2026-07-25

## Exact decision

```text
YELLOW / EQUIVALENCE-PRESERVING METHODS EVIDENCE INCOMPLETE
```

The exact-fourteen implementation, frozen no-solver preflight, and the first
two optimized frequency artifacts pass their applicable code, identity,
equivalence, residual, warning, and atomic-transaction checks. The gate is
nevertheless fail-closed YELLOW because the original `kM=1.58125` runner's
exec-session handle was not retained, so its final exit code and stderr are
genuinely irrecoverable. A valid pair and a later independent audit do not
replace required control provenance.

The optimized matrix stopped before `kM=2.91875`. No aggregate performance
decision, full-image optimized regression, or T4ae GREEN is available.

## Frozen start gate

- Candidate commit:
  `39f89289c01af7ae1709ee1e4458450d0e88b49e`.
- Candidate parent / immutable legacy implementation:
  `8fb8608c187280dc39fd56a78b976e6cf75a6ada`.
- Candidate scope: exactly five added frozen files.
- T7cg decision:
  `REVIEW GREEN / T0 REPAIR PACKAGE APPROVED`.
- Frozen SHA-256 values:
  - T4ae prompt:
    `9e8a65679e4f04e23a24aa6a0bcc0df83dd77c53c76366e136a75518d6c0ec9a`;
  - T7cg prompt:
    `881e934f4b8ae4735a8106f369a5258b253d0619c6ac66cb03a4dada8dfb717c`;
  - T7ch prompt:
    `cee666db6adf78811d706d70b3e8e31793caf01cd13ff3161c84d97dd681ebfe`;
  - plan:
    `e6e65f9f3ffe478046ad73294fa40766918d47384a9f0f6851469162b8953d81`;
  - design:
    `99f6f22056ccf50f5d54a11d98f93151d67e747bf9f07909ffecb04151b1a78f`.
- The 87 failed-child midpoints remained diagnostic only and were never run
  inputs.

## Exact-fourteen implementation identity

The unique implementation commit is:

```text
45face32f52537ac4b8ab79cb1746d8ac78e9b82
```

Its parent is the frozen candidate `39f89289...`; it changes exactly the
fourteen paths frozen by the design. Commit and worktree blobs were
byte-identical after commit. The committed runner SHA-256 is:

```text
18e794353c80f9d161f234de399fc2cf1f3c471e3ae3b41ddcf5488bd866a768
```

The implementation supplies typed convention/provenance contracts, the
legacy scalar/RWZ facade, immutable exact-identity radial cache, certified
dense reuse, deterministic bounded ordering, no-overwrite atomic resume, and
strict artifact/provenance audits. It implements or validates no new spin-2
theory.

## Verification before optimized computation

- Focused frozen suite on final bytes:
  `717 passed, 73 warnings in 295.96 s`.
- The warnings were existing Weyl and deliberately exercised SciPy radial
  fail-closed paths; no test failed.
- Runner regression:
  `23 passed`.
- Exact-scope Ruff:
  PASS.
- `py_compile` and `git diff --check`:
  PASS.
- Independent docs, integration, and runner audits:
  no implementation blocker.
- No-solver matrix preflight:
  PASS with `solver_started=false`.
- Frozen optimized benchmark identity:

```text
46403a00663fc331a8b4c9941d66b2c597d2301f883c24804b5a01cd9bc06c48
```

The preflight independently revalidated all six immutable legacy/golden
pairs, exact source/config/gate/environment identities, idle global state,
and absent optimized outputs.

## Immutable clean legacy baseline

All five frequency pairs and the full `241x241` image pair remain complete
and immutable. None was recomputed.

| case | legacy NPZ SHA-256 | legacy sidecar SHA-256 |
|---|---|---|
| `kM=0.86875` | `cfe8d6cc99ddd0a6e8290eb3bfd6eb07a4bf3f7fec98b07cdf8af8efe2740c10` | `b3cf47671e119576ce142e5730b4f600137246a9fb354778ab0d92cb022d302d` |
| `kM=1.58125` | `caaa6f4a0c8c1320de6cf0d6c2431b20592a63b4796d2f3a8286170d18709b67` | `ec6c7d29d7bc96c5ed7147dfd1e5207fd89627380018e93c6f724e2b607ea39b` |
| `kM=2.91875` | `3bf0abfb7abc090918778012560a967ca91b6ec06bb934d529a7a758133402aa` | `d5c839cdf2e2a8b0cb1c5b87754a87898684bf0e433ef2bcc1d06f2529bb9788` |
| `kM=3.759375` | `21d51922bedd2685113494495053c6cf2096dfb51ee08d67bbb02e13ea3d690e` | `890299945ff79fa9b87f9ed080e083758146349dad49a16f25ded0fb15a03166` |
| `kM=3.89375` | `6a5aaf40484f4c2da9f01671e96cc2ad32a97542009d1fdecd1d5ffeab096ec6` | `1a1fa986692a4384c959a6d81b4220da880b4dadb7738e6935f31830671b241e` |
| full image | `210ae9715256ac9c26d53d869a5f3b8d46f92c254cf1df22825d3ef9ae171674` | `08f7a8e3e4ae508265b4f6480ebcb528289f3827202beff3cb12a22d67821b7a` |

Legacy aggregate resources were wall `14409.929398668 s`, CPU
`14304.987437 s`, `3668` ODE/oracle solves, and `20797684` reuses. The
frozen GREEN limits therefore remain: solves `<=2567`, wall
`<=12248.439989 s`, CPU `<=12159.239321 s`, peak RSS
`<=2507243520` bytes, and each case wall `<=1.05` times legacy.

## Completed optimized evidence

Both pairs below have exact schema/order/dtype/finite pattern, embedded and
sidecar identity, recomputed array fingerprints, exact warning records,
cache/solve accounting, residual hard gates, and complete atomic
transactions. They are bitwise equal to both legacy and immutable goldens.

| case | wall s | CPU s | peak RSS bytes | solves | reuses | NPZ SHA-256 | sidecar SHA-256 |
|---|---:|---:|---:|---:|---:|---|---|
| `kM=0.86875` | `56.47073904203717` | `56.41459` | `1512538112` | `166` | `3034` | `e95d28f4b3d0f35a770430ec074dc47f24b93dc7a2e1785e9646ff4581955f91` | `3a24f316c9c78c72d382fd68c3409f8833fd10c073e942a56d7e8509c4f595d6` |
| `kM=1.58125` | `339.4120677500032` | `339.303076` | `1426423808` | `286` | `6562` | `36ff7902d86762ad3f93bf1b386275cc7b5ca89c50c4f8b448a99592089e5bd9` | `d60277384e6edb1d2192d8a6e0a111cfd21a9be20ddbf976f05d7ec30a712c28` |

For these two cases:

- maximum absolute and normalized-relative differences are `0.0`;
- maximum guarded phase difference is
  `4.115895282222214e-17 rad`;
- raw and structured warning counts are both zero;
- cache rejection and oracle solve counts are zero;
- maximum normalized boundary residual is
  `9.550499576785472e-16`;
- maximum normalized Wronskian/flux residual is
  `9.99999588504931e-9`;
- every applicable equivalence and residual budget passes.

The second post-stop matrix preflight independently audited both complete
pairs, found the other four cases absent, and found no global/case lock,
temporary, partial, or quarantine file.

## Fail-closed control-provenance anomaly

The `kM=0.86875` run session was recovered normally: natural exit `0`,
stderr empty, final `optimized_case_complete`, followed by an independent
audit.

For `kM=1.58125`, the mechanical continuation launched the unique PID
`42492`, which remained healthy until natural disappearance. The outer tool
retained only the initial `optimized_case_start` text and elapsed time. It
discarded the inner exec result without exposing or saving a session ID,
exit code, or stderr and never called `write_stdin`/`wait` on the inner
session. Those records cannot be reconstructed after process reaping.

The active pair, released lock, complete transaction, and subsequent
`--mode audit` exit `0` establish a valid artifact, but they do not establish
the original runner's final control exit/stderr. This is provenance evidence
incomplete, not a demonstrated scientific or numerical failure.

## Stopped matrix and non-claims

The following optimized cases were not started and remain absent:

```text
kM=2.91875
kM=3.759375
kM=3.89375
full 241x241 image
```

Consequently aggregate solve/wall/CPU/RSS performance gates, full-image
equivalence, and fresh full-pytest closeout were not evaluated. The two
complete matching optimized pairs must not be recomputed by any future
authorized continuation.

No tolerance, lmax, required mode/frequency/point, resolution, scientific
threshold, independent validation, or provenance requirement was relaxed.
No T7ch, T8, new frequency, production, plot, fixture, Kirchhoff,
paper-style output, or GitHub action was started.
