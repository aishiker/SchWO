# T4aq Lazy-Oracle Loaded-Module Closure Repair Plan

## Goal

Keep the primary runner, source, runtime, complete-290 inputs, fixed
benchmark identity, existing canonical pairs, and scientific criteria
unchanged while stabilizing the reviewed Q018 oracle's legitimate lazy
module import in the loaded-module provenance check.

## Frozen Evidence

- Failed unit root:
  `t4ae_matrix_sequence_61c23880_20260727T162000p0800/unit_2p91875_attempt2`.
- Failure checkpoint:
  `bbd8ae0645b8b9c07de6b6c96df456e194b38619fd57cbcd8c69ead52f51ecaf`.
- Producer final:
  `e6afab6ffed105218b5d7b436ed42ecb8dbd4efea1a36b87acd76823ca583a24`.
- Runner:
  `18e794353c80f9d161f234de399fc2cf1f3c471e3ae3b41ddcf5488bd866a768`.
- Driver:
  `61c2388080e402586c2a2ef7be12f99400c35be4d9f2603c2d5bcf91b051d0bb`.
- Initial loaded-module manifest:
  `190176ee...81f5`.
- Raw post-oracle-import manifest:
  `9dc258e5...2fde`.
- Complete-290:
  `77927103...fd24`.
- Fixed benchmark identity:
  `46403a00...6c48`.
- Canonical four exact identities and all eight later-file absences.

## T7ct Review

1. Verify package candidate/ref/parent/primary and exact-four scope.
2. Rebuild the entire consumed attempt hash chain, terminal, streams,
   wait/reap, source/runtime/canonical and absence boundary.
3. Independently prove the `2.91875` legacy adapter count is `98` and trace
   the exact lazy import in `radial_solver.py`.
4. Reproduce the exact import-only 44-to-46 module change under the frozen
   Python/runtime/input view.
5. Prove all other frozen identity components remain equal.
6. Review the exact two-name stable projection and require original source
   verification before filtering.
7. Review transparent launcher/driver/process/argv provenance.
8. Verify existing-pair sidecar compatibility at normalized manifest
   `190176...`.
9. Verify all positive/negative tests and zero-science Phase-A boundary.
10. Verify no old attempt, canonical file, runner, source, runtime,
    complete-input meaning, or scientific criterion may change.

Only exact:

```text
REVIEW GREEN / T0 LAZY-ORACLE LOADED-MODULE CLOSURE REPAIR APPROVED
```

allows root T0 to dispatch T4aq Phase A.

## T4aq Phase A

1. Read all current handoffs, the reviewed package, failed unit records,
   runner, driver, runtime, complete inputs, witness and canonical guards.
2. Preserve the entire consumed sequence and old driver immutably.
3. Create a fresh no-overwrite evidence root.
4. Freeze one immutable file launcher and explicit launcher schema.
5. Keep the primary runner byte-identical and execute it as a named module.
6. Wrap only `_loaded_module_identity`, always calling the original
   verifier first.
7. Exclude only the exact reviewed lazy package/module records from the
   set digest after their source identities pass.
8. Require normalized 44/`190176...` both before and after exact oracle
   import; reject every other module-set change.
9. Extend only the artifact-local driver/helper/tests to bind physical
   launcher argv separately from unchanged logical runner argv.
10. Add exact import-only, source-integrity, alias, process, final/replay,
    producer/audit equality and existing-sidecar compatibility tests.
11. Run exact Python-3.14 static/AST/Ruff/diff checks and the full
    zero-science synthetic preflight.
12. Run one fresh real split-root `preflight/matrix` through the launcher.
13. Freeze new driver, launcher, test and preflight identities.
14. Return the exact checkpoint and pause before science.

## Local Self-Correction

Within T4aq Phase A, diagnosed pre-execution zero-science artifact-local
helper, fixture, manifest, serialization, path, launcher-binding or ordinary
test defects must be corrected in fresh immutable evidence roots. No blind
retry is allowed.

Return HOLD only if correction would change the scientific runner/source,
runtime dependency bytes, complete-input meaning, fixed identity, canonical
data, scientific criteria/order, or destructive/global state.

## Post-Checkpoint

Root T0 performs a full independent audit. Only a new explicit one-shot
authorization may then restart:

```text
2.91875 -> 3.759375 -> 3.89375 -> full 241x241
```

The prior failed attempt remains consumed and cannot be retried under its
old authorization.

## Prohibited

No old-root mutation, runner/source/runtime/complete-input/canonical change,
sidecar rewrite, output promotion, threshold/tolerance/mode/points/
resolution/`lmax` change, alternate order, parallelism, duplicate launch,
new frequency, destructive cleanup, global install, network action, new
task/subagent/proxy/descendant, max/ultra, production/plot/fixture/
Kirchhoff/paper/GitHub/downstream work.
