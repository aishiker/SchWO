# T4ao Terminal Filename-Set Closure Repair Plan

## Goal

Replace the consumed T4an control-only HOLD with one freshly reviewed,
zero-science Phase-A0 attempt whose terminal filename-set formula includes
the immutable launcher baseline and is fully exercised before child launch.

## Frozen Inputs

- T4ao design and T7cr/T4ao prompts from the exact candidate supplied by
  root T0.
- Primary compute HEAD `45face32f52537ac4b8ab79cb1746d8ac78e9b82`.
- Driver `057bb24beeb0ea90c6a2524b8ca43df05b611ffe252a1f0b631fab149d1259a9`.
- Runner `18e794353c80f9d161f234de399fc2cf1f3c471e3ae3b41ddcf5488bd866a768`.
- V10 manifest `0b2b37c0...a577`, raw index `f0d3a4a0...ad27`.
- Runtime `811d3840...db30`.
- Complete-290 contract `77927103...fd24`.
- Consumed witness `2566` files / `582625016` bytes / ledger
  `c2182870...bb97`.
- Immutable T4an helper/audit roots, 15 direct audit files, exact hashes,
  child terminal, pre/post evidence, and filename-set mismatch.
- Canonical four identities, eight later-file absences, external zero-byte
  identity, and zero relevant process/link/transient state.

## T7cr Review

1. Verify candidate/ref/parent/primary and exact-four added scope.
2. Independently reconstruct T4an helper/audit inventories and every raw
   child/terminal/guard fact.
3. Prove the only terminal mismatch is the omitted launcher baseline.
4. Review the baseline/common/index/manifest formula for both branches.
5. Check non-self-reference, duplicate detection, pre-manifest and final
   exact-set gates, fsync/close ordering, and post-close reporting.
6. Check mandatory pre-child closure fixtures use the exact live functions.
7. Confirm all T4an v10/process/runtime/input/provenance guards remain.
8. Confirm one-shot/no-retry/no-science/downstream boundaries.

Only exact:

```text
REVIEW GREEN / T0 TERMINAL FILENAME-SET CLOSURE REPAIR APPROVED
```

allows root T0 to consider T4ao Phase A0.

## T4ao Phase A0

1. Read all frozen coordination and package files.
2. Freshly verify every identity and immutable T4an HOLD record.
3. Select one fresh helper/audit/absent-execution triple.
4. Freeze one helper and one file launcher with direct exact constants.
5. Preserve the two non-interchangeable serializers and exact v10/process
   validators.
6. Implement the pure baseline-aware terminal-set functions.
7. Run all v10, process, and success/failure terminal-set fixtures.
8. Static-review that the live closure path uses those exact functions.
9. Freeze helper and launcher; require audit root contains only launcher.
10. Invoke one frozen-file `--first-attempt` audit-only child.
11. Capture raw streams, terminal/wait/reap, and complete pre/post state.
12. Publish the selected branch using baseline-aware exact names.
13. Close and fsync only after exact final set and every identity passes.
14. Reverify all frozen and zero-science guards.
15. Return the exact checkpoint and pause.

## Stop Conditions

Any mismatch, exception, nonzero/signal/stderr, malformed payload,
pre/post drift, process ambiguity, incorrect terminal set, publication
failure, root/link/transient drift, or scientific invocation stops
fail-closed. Preserve evidence. Do not repair or retry locally.

## Prohibited

No old-root mutation, replay or execute, driver edit, T4ai preflight,
scientific runner, official audit, solver, witness, matrix, pair reuse,
canonical/external mutation, cleanup, destructive action, environment
change, network/install, new agent/task, max/ultra, downstream work, new
frequency, production, plot, fixture, Kirchhoff, paper, or GitHub action.
