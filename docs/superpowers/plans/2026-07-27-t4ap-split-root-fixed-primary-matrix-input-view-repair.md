# T4ap Split-Root Fixed-Primary Matrix Input-View Repair Plan

## Goal

Preserve the unchanged primary runner, fixed primary benchmark identity,
canonical output transaction, complete-290 inputs, and all scientific
criteria while separating the canonical execution root from a detached
read-only Table-I input-working root.

## Frozen Inputs

- Primary compute HEAD `45face32f52537ac4b8ab79cb1746d8ac78e9b82`.
- Driver `ffe6871674167917f024f7e52ed8aa608762d91652b8c51f1a73e17f57d9e8d3`.
- Runner `18e794353c80f9d161f234de399fc2cf1f3c471e3ae3b41ddcf5488bd866a768`.
- Complete-290 contract `77927103...fd24`.
- Fixed primary benchmark identity `46403a00...6c48`.
- Witness checkpoint `2c448a70...cde9`.
- Frozen hermetic overlay content index `1803e0f7...9371`.
- Current primary T7 handoff `ef05071f...f1c0`.
- Archived T7ce source `4b801e46...90d1c`.
- Historical witness source/input manifests
  `dfb23566...32a3` / `e2cbf431...f9d` as immutable derivation evidence,
  never as byte-identical manifests for a fresh root.
- Canonical four exact identities and eight later-file absences.

## T7cs Review

1. Verify candidate/ref/parent/primary and exact-four added scope.
2. Reproduce the quota/systemError boundary and prove no matrix attempt.
3. Independently reproduce the current/archive T7ce hash and decision
   mismatch.
4. Inspect runner and Table-I path semantics and prove why primary cwd
   fails before the solver.
5. Re-run the zero-science split-root matrix preflight:
   primary runner and primary `src`, detached cwd/input view, canonical
   primary output, exact environment.
6. Confirm exit0, fixed primary identity, and `solver_started=false`.
7. Review split-root request/manifest/runtime schemas and all alias,
   containment, mutability, import-origin, and post-bind gates.
8. Confirm output remains the unchanged runner's direct canonical atomic
   transaction; no copy/promotion layer is added.
9. Confirm scientific criteria, code, inputs, order, and identities are
   unchanged.
10. Confirm T4 Phase A is zero-science and local control-only defects are
    self-remediable without weakening post-launch one-shot rules.

Only exact:

```text
REVIEW GREEN / T0 SPLIT-ROOT FIXED-PRIMARY MATRIX INPUT VIEW APPROVED
```

allows root T0 to dispatch T4ap Phase A.

## T4ap Phase A

1. Read all frozen coordination, package, witness, driver, runner, input,
   runtime, and canonical evidence.
2. Preserve the original matrix authorization as unmaterialized and
   superseded.
3. Snapshot the old driver and all before guards in fresh durable evidence.
4. Modify only the artifact-local driver and solver-free tests.
5. Introduce explicit canonical execution, detached input-working, runtime,
   and durable-control root fields.
6. Permit only primary runner/imported source/gates/config/output with
   detached cwd complete-290 inputs.
7. Prove the base checkout is clean before materialization, then require the
   exact seven-path reviewed Git-status delta and reject every other dirty
   path/status/content.
8. Derive fresh source/input manifests with new absolute target stat/inode
   identities: exact one-entry `t4af_matrix_source_manifest_v1` for the
   canonical primary runner and exact exhaustive/read-only
   `t4ap_matrix_input_manifest_v2` for all 290 detached targets. Preserve
   the exact three-field complete-290 contract `77927103...fd24`; never
   reuse the historical manifest bytes.
9. Preserve the fixed primary matrix identity and reject detached identity.
10. Add every required positive/negative/static test.
11. Run exact Python-3.14 zero-science validation, including one real runner
   `preflight/matrix` split-root check.
12. Recompute manifests/indexes and every old/new guard.
13. Return the exact checkpoint and pause before science.

## T4 Self-Correction

Diagnosed pre-execution zero-science artifact-local control defects must be
corrected in fresh immutable evidence roots during the same authorized
stage. Do not return HOLD for those small defects. Preserve failures and
continue with a newly diagnosed correction; never blind-retry.

Stop only when correction would cross into runner/source/runtime/input/
identity/canonical/scientific semantics or destructive/global state.

## Post-Checkpoint

Root T0 performs a complete independent audit. Only a fresh explicit matrix
authorization may then start:

```text
2.91875 -> 3.759375 -> 3.89375 -> full 241x241
```

one producer and matching official audit at a time. Any post-launch failure
consumes that unit and forbids retry.

## Prohibited

No primary T7 handoff mutation/swap, runner/source/runtime/complete-input
change, detached runner/import, detached output, output promotion/copy,
scientific threshold or matrix-order change, new frequency, recomputation,
parallel launch, canonical overwrite, cleanup/destructive/global action,
new task/subagent/proxy/descendant, max/ultra, production/plot/fixture/
Kirchhoff/paper/GitHub/downstream work.
