# T4as Legacy Reconstruction Durable-Supervision Repair Plan

## Goal

Add exact durable driver support for the already frozen legacy
`2.91875` reconstruction producer and matching audit, verify the new surface
without science, and freeze a new driver identity before issuing a new
one-shot authorization.

## Frozen HOLD

```text
decision
  HOLD / LEGACY RAW-WARNING WITNESS PRELAUNCH DRIVER PHASE-SURFACE BLOCKED
root
  t4ar_phase_b_prelaunch_hold_ee2b2960_20260727T204500p0800
manifest
  241584cc23907c0b97f63b5d1042272e99900edb99fcd38015fd1166ec31ec20
helper
  0b57be2b08da0ae905b5b8d831ad8002e61618ad5d7a28209d0edce9addf479e
authorization consumed
  false
producer/audit/solver/matrix
  0/0/0/0
```

The root is immutable. The old Phase-B authorization is not executable under
a new driver identity and must not be reused.

## T7cv Review

1. Reconstruct the immutable prelaunch HOLD, two-file root, canonical manifest
   bytes, zero-child counts, and unconsumed boundary.
2. Verify the exact frozen driver phase sets, command matcher, launcher
   binding, binding validator, runtime/audit/control paths, and optimized
   terminal parser.
3. Prove no existing phase can legally supervise the reconstruction launcher.
4. Audit the two new literal phase values and strict separation from optimized
   and synthetic surfaces.
5. Audit the exact reconstruction producer/audit argv, roots, bindings,
   invocation markers, output pair, and stdout schemas.
6. Audit durable request/prelaunch/running/receipt/final/control/failure
   closure and phase-specific post-child parsing.
7. Audit producer-to-audit same-root/pair/provenance binding, no-retry, and
   process ancestry/token semantics.
8. Verify all existing phases and T4ar scientific/warning rules remain exact.
9. Verify Phase A is zero-science and its prospective roots cannot be reused.
10. Freshly verify candidate/ref/HEAD, T4ar checkpoint, driver/launchers,
    runtime, complete inputs, witness, legacy/golden, canonical, absence,
    process, transient, and external guards.

Only exact:

```text
REVIEW GREEN / T0 LEGACY RECONSTRUCTION DURABLE SUPERVISION REPAIR APPROVED
```

permits T4as Phase A.

## T4as Phase A

1. Read all current coordination documents, the complete T4ar/T4as packages,
   immutable HOLD, Phase-A checkpoint, both consumed matrix attempts, runtime,
   inputs, legacy/golden, witness, canonical, and process state.
2. Preserve every old root immutable and freeze the old driver before edit.
3. Add exact producer/audit phase constants without changing old phase values.
4. Add a phase-specific reconstruction command and binding validator; do not
   route through optimized logical-runner or terminal schemas.
5. Extend runtime, source/input, process, durable audit/failure, output, and
   replay paths for the two phases.
6. Parse and validate exact reconstruction run/audit terminal JSON and the
   isolated pair.
7. Require complete producer-to-audit identity equality and no audit before
   producer PASS.
8. Add every positive/negative test in the design and prove old accepted
   surfaces unchanged.
9. Run exact Python 3.14 static/AST/dataflow/Ruff/diff checks and a full
   zero-science synthetic preflight.
10. Build one fresh exact-legacy prospective detached/input/output geometry,
    validate full producer/audit request templates without launching either,
    and freeze all identities.
11. Require reconstruction/runner/audit/official-audit/solver counts all zero,
    canonical unchanged, eight later files absent, and no related process or
    transient.
12. Return exact:

```text
CHECKPOINT / LEGACY RECONSTRUCTION DURABLE SUPERVISION FROZEN
```

## Phase B

After a full root-T0 audit, root T0 may issue one new exact legacy
reconstruction authorization. T4 must use fresh roots, never the Phase-A
prospective geometry.

The producer calls the unchanged exact legacy `_compute_frequency()` once and
publishes one isolated atomic pair. Only complete durable, pair, bitwise-array,
structured-warning, and raw-warning PASS may start the matching zero-solver
audit. Any launched failure consumes that authorization and forbids retry.

Success returns:

```text
CHECKPOINT / LEGACY RAW-WARNING WITNESS PASSED
```

## Later Work

Root T0 audits Phase B before separately authorizing the existing T4ar
zero-science Phase C. Phase C audit precedes any new matrix `2.91875`
authorization. Matrix order remains:

```text
2.91875 -> 3.759375 -> 3.89375 -> full 241x241
```

## Prohibited

No old-root mutation, prospective-root reuse, producer/audit retry, scientific
runner/source/runtime/input/identity/criteria/order change, warning relaxation,
missing-as-zero, legacy/golden/canonical rewrite, output promotion, parallel or
duplicate execution, alternate order, new frequency, destructive/global
action, network/install, task/subagent/proxy/descendant, max/ultra,
production/plot/scientific-fixture/Kirchhoff/paper/GitHub/downstream work.
