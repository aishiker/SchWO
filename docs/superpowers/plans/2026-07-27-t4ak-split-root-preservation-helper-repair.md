# T4ak Split-Root Preservation Helper Repair Plan

## Goal

Replace the exhausted T4aj helper with a reviewed helper whose four
authoritative roots are direct AST string constants and whose immutable
helper checkpoint is physically separate from its later preservation output.

## Task 1 — Independent Package Review

The existing T7 task reads the exact four-file T4ak package, all three
immutable failed-helper roots, the frozen T4ai/T4aj packages and reviews,
the live driver, T4ah v10 evidence, complete consumed witness, canonical
state, absent units, external object, and process/transient state.

Only exact:

```text
REVIEW GREEN / T0 SPLIT-ROOT PRESERVATION HELPER REPAIR APPROVED
```

permits Task 2.

## Task 2 — Phase A0 Helper Freeze

In the existing T4 task using `gpt-5.6-sol/high`:

1. fresh-verify candidate/ref/parent/exact-four and every frozen upstream
   identity;
2. create one fresh helper root and reserve one distinct absent execution
   root, both direct children of the exact control root;
3. embed project, control, helper, and execution roots as four direct full
   `ast.Constant` assignments;
4. publish one helper source exclusively and fsync it;
5. run the exact AST-semantic static gate, never a raw-substring count;
6. run audit-only exactly once under exact CPython 3.14;
7. prove audit-only exit zero, empty stderr, exact no-newline canonical
   stdout, no helper write, and execution-root absence;
8. atomically publish captures and close the helper root read-only.

Do not run execute mode, create the execution root, copy the driver, publish
a before manifest, edit the driver, run preflight, or invoke science.

Return exactly:

```text
CHECKPOINT / T4AK SPLIT-ROOT PRESERVATION HELPER FROZEN
```

Then pause.

## Task 3 — Root-T0 Helper Audit

Root T0 independently:

- reads the helper fully and parses its AST;
- checks the four direct exact constants and split-root geometry;
- rejects root derivation and every forbidden mutation surface;
- recomputes the complete frozen evidence and witness ledger;
- reruns audit-only once without writes;
- verifies exact stdout bytes/digest, empty stderr, exit zero, helper-root
  inventory equality, and execution-root absence.

Any mismatch stops. Complete PASS permits one digest-bound split-root
execute authorization.

## Task 4 — One-Shot Preservation Execution

T4 runs the frozen helper once with the exact reviewed execute
argv/cwd/environment and expected audit digest.

After the shared pre-write audit and digest match, the helper creates only
the reserved execution root, publishes the old-driver snapshot and before
manifest exclusively/atomically, verifies all bytes/hash/stat/inode/nlink
and guard identities, and leaves the immutable helper root unchanged.

The launcher durably captures exact execution records in the execution root
and closes it read-only. Any failure consumes authorization and stops.

## Task 5 — Resume T4ai Phase A

Only preservation PASS permits:

1. the already reviewed artifact-local durable-driver and solver-free-test
   repair;
2. deterministic relocated benchmark-identity binding with
   runner-default `ensure_ascii=True`;
3. Unicode-root positive and `ensure_ascii=False` negative coverage;
4. one fresh full CPython-3.14 zero-science preflight;
5. exact scientific runner CLI / official audit / solver counts `0/0/0`.

On PASS return exactly:

```text
CHECKPOINT / RELOCATED BENCHMARK IDENTITY CONTRACT FROZEN
```

Then pause for root T0.

## Safety And Budget

- Existing T4/T7 tasks only.
- T4 uses `gpt-5.6-sol/high`; no `max` or `ultra`.
- No task, subagent, proxy, or descendant.
- No retry of T4ai/T4aj roots or helper.
- No runner/science/runtime/complete-290/canonical/threshold change.
- No destructive cleanup, outside mutation, network/install/global change,
  new frequency, production, plot, fixture, Kirchhoff, paper, or GitHub
  action.
