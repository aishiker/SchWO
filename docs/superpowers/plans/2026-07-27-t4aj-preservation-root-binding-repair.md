# T4aj Pre-Edit Preservation Root-Binding Repair Plan

## Goal

Replace the exhausted, numerically indexed preservation-helper path logic
with an independently reviewed explicit-root helper, freeze that helper
before execution, and only then resume the already approved T4ai Phase A.

## Task 1 — Independent Package Review

The existing T7 task reads the exact four-file T4aj package, both immutable
failed helper roots, the frozen T4ai package and T7cl GREEN, the live driver,
consumed T4ah witness, canonical/absence/external/process guards, and this
two-checkpoint protocol.

Only exact:

```text
REVIEW GREEN / T0 PRE-EDIT PRESERVATION ROOT-BINDING REPAIR APPROVED
```

permits Task 2.

## Task 2 — Helper Construction Only

In the existing T4 task with `gpt-5.6-sol/high`:

1. fresh-verify T4aj candidate/ref/parent/exact-four, T4ai/primary
   identities, both immutable failed roots, live driver, complete T4ah
   witness ledger and critical map, canonical files, four absent units,
   external object, and zero unexpected process/transient state;
2. create one fresh no-overwrite evidence root in mode `0700`;
3. publish one helper source exclusively and fsync it;
4. reject all authoritative-root derivation through `Path.parents`,
   numeric parent ascent, cwd-relative discovery, or string stripping;
5. bind explicit absolute project/control/evidence roots in exact argv;
6. run the helper once in `--audit-only` mode and atomically capture its
   stdout/stderr/exit/argv/cwd/env and pre/post inventories;
7. prove audit-only exit zero, empty stderr, canonical payload exact, no
   helper write, no bytecode, and all frozen guards PASS.

Do not create a snapshot or before manifest. Do not edit the driver. Do not
run preflight or science.

Return exactly:

```text
CHECKPOINT / T4AJ PRESERVATION HELPER FROZEN
```

Then pause.

## Task 3 — Root-T0 Helper Audit

Root T0 independently:

- reads the helper fully and parses its AST;
- rejects parent-index root logic and every out-of-scope mutation surface;
- verifies exact absolute argv/cwd/environment and containment;
- independently reruns the no-write audit-only mode;
- recomputes the complete 2,566-file witness ledger and critical map;
- verifies both failed roots, candidate, live driver, canonical,
  absent-unit, external-object, process, transient, link, and bytecode
  guards;
- verifies pre/post evidence-root inventories and all capture hashes.

Any mismatch stops. Complete PASS permits exactly:

```text
AUTHORIZED / EXECUTE T4AJ PRESERVATION AND RESUME T4AI PHASE A
```

## Task 4 — One-Shot Preservation

T4 runs the frozen helper once with the reviewed execute argv and expected
audit payload SHA-256.

The helper must recompute the same payload, match its digest, publish the
old-driver snapshot and `before_manifest.json` exclusively and atomically,
fsync files and parents, prove nlink-1/non-alias/exact bytes, close
permissions only after success, and leave every external/frozen guard
unchanged.

Any failure is immutable and stops without another attempt.

## Task 5 — Resume T4ai Phase A

After preservation PASS only:

1. modify only the artifact-local durable driver and solver-free tests;
2. implement the already reviewed relocated benchmark-identity binding;
3. retain fixed primary matrix identity and runner-default
   `ensure_ascii=True`;
4. retain Unicode-root positive and `ensure_ascii=False` negative cases;
5. run exactly one fresh full CPython-3.14 zero-science preflight;
6. require scientific runner CLI / official audit / solver counts
   `0/0/0`;
7. freeze evidence and return exact
   `CHECKPOINT / RELOCATED BENCHMARK IDENTITY CONTRACT FROZEN`.

Any failure stops without retry.

## Task 6 — Root-T0 T4ai Checkpoint Audit

Root T0 performs the complete T4ai audit. Only a full PASS permits a
separate new one-shot existing-`kM=1.58125` producer plus matching official
audit authorization.

The consumed successful T4ah pair remains non-reusable. Matrix remains
forbidden until a new complete witness passes.

## Safety And Budget

- Existing T4/T7 tasks only.
- T4 uses `gpt-5.6-sol/high`; no `max` or `ultra`.
- No task, subagent, proxy, or descendant.
- No runner/science/runtime/complete-290/canonical/threshold change.
- No destructive cleanup, network/install/global mutation, new frequency,
  production, plot, fixture, Kirchhoff, paper, or GitHub work.
