# T4af Control-Provenance Witness Repair Plan

## Goal

Repair only the prospective process-control evidence missing from the
otherwise valid T4ae `kM=1.58125` optimized pair. Preserve the original
irrecoverable-exit fact, never overwrite a complete canonical pair, and
continue the frozen T4ae matrix only after an independently reviewed,
isolated witness passes.

## Frozen Start Gate

1. Fully read `project.md`, `status.md`, current T0/T4/T7/T8 handoffs, the
   frozen T4ae five-file package, T4ae note/artifacts, and this repair
   package.
2. Verify the repair candidate commit/parent/exact-four paths, all
   commit/worktree blobs and SHA-256 values, and exact matching
   `REVIEW GREEN / T0 REPAIR PACKAGE APPROVED`.
3. Verify implementation commit `45face32...`, exact-fourteen blobs, runner
   SHA `18e79435...`, benchmark identity `46403a00...`, the four existing
   optimized hashes, and the absence of a live runner or transient file.
4. Stop before any write or compute on mismatch.

## Task 1 — Phase-A Durable Driver Freeze

Using `gpt-5.6-sol/ultra`:

- create only the artifact-local driver and raw preflight records allowed by
  the design;
- make launch/final records atomic and independent of the Codex exec-session
  handle;
- bind exact argv/cwd/environment/source/output identities;
- run all synthetic normal/nonzero/signal/stdout/stderr/lock/duplicate/atomic
  tests;
- verify no scientific solver was invoked and no canonical output changed.

Return:

```text
CHECKPOINT / DURABLE CONTROL DRIVER FROZEN
```

Then pause for T0. Do not begin the witness.

## Task 2 — T0 Intermediate Gate

T0 independently reads the driver source and raw synthetic records, verifies
its SHA, process semantics, atomicity, path confinement, output separation,
current process state, and unchanged canonical hashes.

Only exact:

```text
AUTHORIZED / CONTROL-PROVENANCE WITNESS COMPUTE
```

permits Task 3. Any requested substantive driver change requires a new
driver SHA, full synthetic preflight, and a new intermediate checkpoint.

## Task 3 — Isolated `kM=1.58125` Witness

- Construct a detached clean source snapshot at exact implementation
  `45face32...`.
- Produce a content-addressed, read-only input provisioning manifest.
- Prove by resolved real paths that the witness output is isolated from the
  canonical optimized root.
- Launch exactly one `kM=1.58125` witness through the frozen durable driver.
- Wait for and validate the atomic final control record before inspecting
  output.
- Run the official audit through the same durable control path.
- Direct-load and independently compare the witness against canonical
  optimized, legacy, and accepted golden evidence.
- Recheck the four canonical hashes and all forbidden transient/output
  searches.

Stop on any mismatch. Do not retry.

## Task 4 — Conditional Original-Matrix Completion

Only after the witness passes and a fresh T0 verification authorizes
continuation:

1. preserve the canonical `0.86875` and `1.58125` pairs without any write;
2. run only `2.91875`, then `3.759375`, then `3.89375`, then the frozen full
   `241x241` image configuration;
3. use the frozen durable driver for every run and audit;
4. validate each final control record, artifact transaction, scientific
   equivalence, and resource/performance record before starting the next;
5. stop immediately on any anomaly.

## Task 5 — Full T4ae Verification

Run every verification still required by the frozen T4ae contract:

- direct equivalence, residual, lmax-delta, image, performance, and resource
  recomputation;
- serial/two-worker determinism;
- cache-identity rejection and checkpoint fault/resume;
- typed-interface and legacy-adapter audits;
- focused pytest, exact-scope Ruff, fresh full pytest, `git diff --check`,
  exact implementation/worktree/source checks, warning classification, and
  forbidden-output searches.

The witness timing is reported separately and excluded from the frozen
performance aggregate.

## Exact Scope

Before witness acceptance, writes are limited to the exact control-witness
artifact directory plus allowed T4 coordination records. After acceptance,
only the four originally absent T4ae work units and original methods
manifest/note may be added.

No implementation or frozen-file edit is authorized.

## Exact Final Decision

Return exactly one:

```text
GREEN / EQUIVALENCE-PRESERVING METHODS GATE READY
YELLOW / EQUIVALENCE-PRESERVING METHODS EVIDENCE INCOMPLETE
RED / EQUIVALENCE-PRESERVING METHODS GATE INVALID
```

Do not start T7ch, T8, a new frequency, production, plots, fixtures,
Kirchhoff, paper-style output, or GitHub action.
