# T4au Read-Only Legacy Reference-View Repair Plan

## Goal

Repair only the artifact-local reconstruction reference view that passed
the writable primary legacy sidecar directly to a launcher whose unchanged
safety contract requires read-only input objects.

The consumed PID-50124 producer attempt remains immutable and non-reusable.
Phase A is zero-science and stops at:

```text
CHECKPOINT / READ-ONLY LEGACY REFERENCE VIEW FROZEN
```

## Phase 0 — Start Gate

1. Read project/status/current T0/T4/T7/T8 handoffs and complete
   T4ar/T4as/T4at/T4au packages.
2. Verify candidate/ref/parent/primary HEAD and exact-four bytes.
3. Reconstruct request/prelaunch/running/receipt/streams/failure-audit/
   checkpoint and the full consumed root.
4. Verify child `50124`, exit64/no-signal/exact-wait/reaped/PG-empty,
   exact 294-byte stderr, empty stdout, no pair, audit0 and consumed
   authorization.
5. Prove from launcher source that sidecar read-only validation occurs
   before `_compute_frequency()`.
6. Verify all three primary artifacts are exact regular nlink-1 mode0644
   and preserve their full identities.
7. Reverify driver/launchers/runtime/complete290/legacy/golden/warning/
   canonical/absence/process/transient/external guards.

Any unexplained drift stops before edits.

## Phase 1 — Preserve Old Control

1. Create one fresh unique no-overwrite Phase-A evidence root.
2. Snapshot driver `efe256d2...e01bd`, old launcher
   `20bd7e78...79c6`, T4at checkpoint and the complete consumed failure.
3. Bind all prior failed roots and locks as immutable.
4. Snapshot primary legacy NPZ/sidecar/golden full identities before work.

## Phase 2 — Publish New Launcher And Driver Repair

1. Create a new launcher inside the fresh Phase-A root; never modify the old
   launcher.
2. Add only required `--legacy-reference-root`.
3. Derive the three fixed relative reference paths and thread them through
   static validation, compute, comparison, run and audit.
4. Preserve the read-only check and exact scientific call/warning/array
   semantics.
5. Close the isolated output permission lifecycle: both NPZ and JSON become
   exact mode0400 before atomic final publication and producer PASS.
6. Extend the durable driver to bind reference root/manifest/source/targets
   and add the new argument to exact argv.
7. Require one identical reference view and exact read-only pair for
   producer and matching audit.
8. Preserve every old phase and all T4at split-root behavior.

## Phase 3 — Reference Materialization

1. Create one fresh reference root outside source/input/output/HOME/run
   roots.
2. Copy exactly the three primary artifacts by no-follow exclusive create.
3. Fsync, chmod0400, reopen, rehash and verify fixed paths.
4. Require target/source and target/target inode disjointness.
5. Publish the exact reference manifest and three-field content index.
6. Close the root read-only and prove primary sources unchanged.

## Phase 4 — Zero-Science Tests

Run exact Python 3.14 AST/static/dataflow/style/diff checks and all positive
and negative fixtures frozen in the design. Build and validate prospective
producer and matching-audit requests without launching either.

No launcher `main`, reconstruction producer/audit, optimized runner,
official audit, `_compute_frequency()`, solver or matrix may run.

Diagnosed local pre-execution fixture/helper/manifest/path/serialization/
bookkeeping defects must be preserved in immutable failed roots and
minimally corrected in new roots. There is no blind retry.

## Phase 5 — Checkpoint

Freeze final driver/new-launcher identities and diffs; reference view and
manifest; tests/rejections; prospective requests; primary pre/post
identities; runtime/source/input/canonical/absence/process/transient/
external guards; and science counts all zero.

Return exact checkpoint and stop for root-T0 audit.

## Later Gates

Only a separate root-T0 authorization after Phase-A PASS permits one fresh
reconstruction producer and, after its complete PASS, one matching audit.
Any launched failure consumes the authorization and forbids retry.

Witness PASS still requires separate T4ar Phase-C authorization and audit
before matrix `2.91875`. Matrix order and all scientific criteria remain
unchanged.
