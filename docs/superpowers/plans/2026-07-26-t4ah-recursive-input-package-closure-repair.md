# T4ah Recursive Input-Package Closure Repair Plan

## Goal

Close the detached witness input contract over every file recursively
observed by the frozen Table-I package checks, while preserving the valid
T4ag RED evidence, hermetic runtime, durable supervisor, scientific runner,
canonical data, and all scientific acceptance criteria.

## Task 1 — Independent Package Review

T7 reads the exact four-file T4ah candidate, T4af/T4ag packages, the complete
T4ag RED witness, frozen driver/preflight/overlay, runner and Table-I input
code, six accepted package roots, canonical guards, absent units, and
current process/transient state.

Only exact:

```text
REVIEW GREEN / T0 RECURSIVE INPUT PACKAGE REPAIR APPROVED
```

permits Task 2.

## Task 2 — Start Gate And Old-Evidence Freeze

In the existing T4 task using `gpt-5.6-sol/high`:

1. verify candidate/ref/parent/exact-four and primary/exact-fourteen;
2. verify the T4ag RED request/prelaunch/running/final/streams/control audit;
3. verify exit1/no-signal/reaped/PG-empty, solver0/audit0, no pair, no retry;
4. verify the T4ag driver/preflight/overlay and all older evidence;
5. verify canonical hashes/stat/inodes, four absent units, and zero
   unexpected process/transient state;
6. preserve the old driver and RED evidence under fresh unique,
   no-overwrite, fsynced control evidence.

No scientific runner, audit, solver, witness, or matrix process may start.

## Task 3 — Reconstruct The Recursive Package Contract

Recompute the six exact active inventories:

```text
T8ao  23
T8ap  31
T8aq  53
T8ar  87
T4aa  17
T4ad  59
```

Require:

- 270 distinct active package members;
- 28 already covered by the 48-item semantic contract;
- 242 additional package members;
- 290 final distinct contract entries;
- package membership index
  `9f0a743db65793ff4aa28ea9f0890985914a0c3b4bc9a96622a5fefdea28b0d1`;
- final contract index
  `77927103da4d853e98dbe7c4d19a2198fb544349cf735bf63faaf02ca31dfd24`.

Reject any path/hash/count/index/link/type/transient/source-drift mismatch.
Do not copy quarantine or infer success from cardinality alone.
Use only the design's frozen Unicode code-point path order and exact
UTF-8 canonical-JSON schemas. Reject locale-aware, filesystem-order,
shell-sort, or Node `localeCompare` derivations.

## Task 4 — Bounded Driver Remediation

Modify only the artifact-local durable driver and solver-free tests:

- introduce a new witness input-manifest schema;
- preserve all 48 prior semantic entries;
- add deterministic package-member roles for the missing 242 files;
- validate all six inventories and the complete 290-entry index using the
  exact per-package, combined-content, membership, and contract payload
  schemas frozen in the design;
- enforce exact detached copy, read-only, source/target identity, no-alias,
  no-link, no-extra-file, and before/after source guards;
- preserve runtime overlay, process supervision, source manifest, official
  audit, matrix, canonical, and scientific semantics unchanged.

Do not edit runner, implementation, configs, accepted inputs, canonical
data, legacy/golden data, thresholds, tolerances, modes, points, resolution,
or `lmax`.

## Task 5 — Full Python-3.14 Zero-Science Preflight

Run one fresh unique preflight after static review:

1. all prior T4ag positive/negative/fault/lock/audit/runtime tests;
2. all prior CLI captures and rejection reasons;
3. new recursive-package positive and negative cases;
4. a solver-free detached fixture with exact recursive cardinalities;
5. AST, Ruff, `git diff --check`, raw/capture indexes, exact Python/runtime,
   old-evidence, canonical, absence, process/transient, link, and
   no-bytecode checks.

Scientific runner CLI, official audit, and solver invocation counts must be
zero. Any failure is immutable and stops without retry.

Return exactly:

```text
CHECKPOINT / RECURSIVE INPUT PACKAGE CONTRACT FROZEN
```

Then pause.

## Task 6 — Root-T0 Checkpoint Audit

T0 independently recomputes:

- old/new driver hashes and exact diff;
- all six inventories, per-file identities, package indexes, roles, and
  final 290-entry contract;
- manifest/raw/capture/test/rejection/runtime evidence;
- T4ag RED and overlay immutability;
- canonical, absent-unit, link, process, and transient guards.

Only complete PASS permits:

```text
AUTHORIZED / COMPLETE-INPUT CONTROL-PROVENANCE WITNESS COMPUTE
```

## Task 7 — New One-Shot Witness

Provision one fresh detached exact-45face project with the complete 290-file
input contract and the frozen hermetic overlay.

Run exactly one existing `kM=1.58125` producer. Only producer exit0,
empty stderr, reaped/PG-empty, runtime pre/post PASS, atomic pair PASS, and
unchanged canonical guards permit the matching official audit.

The matching audit must use the same driver, runtime, source, input, and
producer identities and remain read-only. Any failure consumes the
authorization and stops. No retry.

## Task 8 — Conditional Matrix And Final Gate

After root-T0 witness PASS, continue exactly:

```text
2.91875 -> 3.759375 -> 3.89375 -> full 241x241
```

one unit at a time, with durable producer plus official audit and all
canonical guards.

Only final T4 GREEN plus root-T0 full T4ae PASS permits frozen T7ch.

## Reasoning And Orchestration

- T4 code work defaults to `gpt-5.6-sol/high`.
- `gpt-5.6-sol/max` requires a root-T0 determination that `high` is
  insufficient for a genuinely complex code/science/safety/provenance
  interaction.
- T4 code work must not use `ultra`.
- Use existing T4 and T7 tasks only.
- Create no task, subagent, proxy, or descendant.
- No global install, network access, destructive cleanup, new frequency,
  production/plot/fixture/Kirchhoff/paper/GitHub work, or scientific
  criterion change is authorized.
