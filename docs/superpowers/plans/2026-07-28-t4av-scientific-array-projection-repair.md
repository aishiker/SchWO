# T4av Scientific-Array Projection Repair Plan

## Goal

Repair only the artifact-local comparison that incorrectly compares the
23-key full `_compute_frequency()` mapping, including `metadata_json`,
against a 22-key scientific-only legacy projection.

The consumed PID-60188 producer attempt remains immutable and non-reusable.
Phase A is zero-science and stops at:

```text
CHECKPOINT / SCIENTIFIC ARRAY PROJECTION CONTRACT FROZEN
```

## Phase 0 — Start Gate

1. Read project/status/current T0/T4/T7/T8 handoffs and complete
   T4ar–T4av packages.
2. Verify candidate/ref/parent/primary HEAD and exact-four bytes.
3. Reconstruct the complete PID-60188 request/setup/source/input/reference/
   prelaunch/running/receipt/streams/failure/audit/checkpoint chain.
4. Verify exit64/no signal/exact wait/reap/PG-empty, exact 170-byte stderr,
   empty stdout, pair absent, audit0 and consumed authorization.
5. Rehash the immutable 820-file/117-directory/531568940-byte root and root
   T0 canonical index.
6. Independently prove the deterministic 23-key versus 22-key comparison
   defect from launcher, exact-legacy source and legacy/golden NPZ order.
7. Reverify driver/launcher/checkpoint/preflight/runtime/complete290/
   reference/legacy/golden/warning/canonical/absence/process/transient/
   external guards.

Any unexplained drift stops before edits.

## Phase 1 — Preserve Old Control

1. Create one fresh unique no-overwrite Phase-A evidence root.
2. Snapshot the final T4au driver and launcher, checkpoint/preflight, and
   complete consumed PID-60188 root identities.
3. Bind all prior failed/consumed roots and locks as immutable.
4. Preserve canonical and external full identities before work.

## Phase 2 — Publish New Launcher And Driver Binding

1. Publish a new launcher derived from the consumed T4au launcher; never
   modify the old launcher.
2. Add the explicit producer-full and audit-scientific projection surfaces.
3. Require exact 23-key producer order and exact 22-key audit order.
4. Compare exactly the same 22 scientific arrays bitwise to legacy/golden.
5. Validate producer `metadata_json` as final scalar Unicode JSON without
   comparing its attempt-specific bytes as a scientific array.
6. Bind the new launcher and surface contract throughout the artifact-local
   durable driver.
7. Preserve every other launcher/driver/scientific/provenance behavior.

## Phase 3 — Zero-Science Tests

Run exact Python 3.14 AST/static/dataflow/style/diff tests and all positive
and negative fixtures from the design. Use only existing read-only NPZ
arrays and synthetic in-memory mappings. Do not call `_compute_frequency()`
or launcher `main`.

Build and validate prospective producer and matching-audit requests without
launching either child.

Diagnosed local pre-execution fixture/helper/manifest/path/serialization/
bookkeeping defects must be preserved in immutable failed roots and
minimally corrected in fresh roots. There is no blind retry.

## Phase 4 — Checkpoint

Freeze final driver/new-launcher identities and diffs, projection functions
and literal call sites, tests/rejections, prospective requests, consumed RED
root, runtime/source/input/reference/legacy/golden/warning/canonical/
absence/process/transient/external guards and all science counts zero.

Return exact checkpoint and stop for root-T0 audit.

## Later Gates

Only separate root-T0 authorization after full checkpoint audit permits one
new full reconstruction producer and, after producer PASS, one matching
zero-solver audit. Any launched failure consumes the authorization and
forbids retry.

Witness PASS still requires separately gated T4ar Phase C before the
optimized matrix. Matrix order and every scientific criterion remain
unchanged.
