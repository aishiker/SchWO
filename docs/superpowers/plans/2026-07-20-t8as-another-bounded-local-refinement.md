# T8as Another Bounded Local Refinement Plan

## Goal

After T7ce exact ACCEPT GREEN and T0 fresh verification, generate point-only
complex amplification evidence at exactly the 55 frozen T7cd failed-child
midpoints. T8as is an evidence producer, not the scientific reviewer.

## Start Gate

Require exact candidate identity and matching package-review GREEN, T4ad exact
GREEN, T7ce exact ACCEPT GREEN, T0 fresh verification, unchanged T7cd/T8ar and
earlier immutable sources, and the existing T8 task identity. Any missing or
ambiguous condition stops before edit or computation.

## Exact Implementation Scope

Before the first real frequency, create one commit changing exactly:

```text
scripts/phase5_run_another_bounded_local_refinement.py
src/schwgw/io/__init__.py
src/schwgw/io/tablei_another_bounded_local_refinement.py
tests/unit/test_tablei_another_bounded_local_refinement.py
tests/regression/test_another_bounded_local_refinement_script.py
```

No solver, radial envelope, source artifact, config, observable, fixture,
visualization, Kirchhoff, or unrelated path may change.

## Task 1 — Contracts And TDD RED

- Hard-code the exact 55 frequencies, tokens, lmax windows, five sequences,
  and literal 55-row parent map from the design.
- Define schema
  `phase5_t8as_another_bounded_local_refinement_v1_units_dtype_ordering`.
- Define canonical generation/metadata contracts binding physical inputs,
  implementation commit/blobs, sources/gate hashes, units, actual dtypes,
  ordering, flags, and atomic transaction rules.
- Add fake-compute RED tests for exact membership/order, masks,
  histories/final rows, sidecar/embedded equality, contracts, resume identity,
  115/114 cardinality, and forbidden-output isolation.

## Task 2 — Fake Compute, Ruff, And Pre-Run Commit

- Implement only enough to pass fake-compute tests.
- Run focused tests and Ruff on the exact five paths.
- Create the exact five-path implementation commit before real computation.
- Freeze commit parent/time, all five blobs/SHA-256, and generation/metadata
  contract identities. The commit must predate every real transaction.

## Task 3 — Atomic 55-Frequency Execution

- Write exactly one NPZ/JSON pair per frozen frequency under
  `runs/phase5/fig5_fig6_another_bounded_local_refinement/frequencies/`.
- Each pair stores exact kM/token, point order/geometry, lmax history/final
  row, plus/cross complex values, magnitudes, phases, masks, final-pair deltas,
  warnings/counts, units, actual dtypes, fingerprints, contracts, sources,
  gate hashes, and implementation identity.
- Publish a ledger entry only after both files and hashes are complete.
- Resume only a complete pair matching every scientific, implementation,
  contract, source, and gate identity. Never reuse across identity or
  recompute a complete matching frequency.
- Any final-pair failure at unchanged `1e-4`, nonfinite value, false mask,
  metadata mismatch, or scope/provenance error stops before aggregation.

## Task 4 — Aggregate And Diagnostic Audit

- Direct-stack all 55 transactions in frozen order and require exact key,
  shape, dtype, order, and value identity.
- Write exactly one ledger, aggregate NPZ, aggregate JSON, diagnostic audit,
  and non-self manifest.
- Require exactly 115 active files, 114 manifest records, and no
  temp/partial/quarantine file.
- Reconstruct exactly 2,352 phase, 1,760 hierarchy, and 80 summary records.
- Record values/failures/extrema/variation/cancellation for independent review,
  but set `diagnostic_only=true` and
  `acceptance_decision_emitted=false`; emit no scientific decision or next
  midpoint.

## Task 5 — Fresh Verification And Handoff

- Independently reload all pairs and verify hashes, arrays, metadata,
  fingerprints, ledger, contracts, aggregate, audit, and manifest.
- Run focused pytest, Ruff, fresh full pytest, `git diff --check`, exact-five
  scope/source checks, warning classification, and forbidden-output searches.
- Archive the prior T8 handoff byte-for-byte, update only allowed T8/status
  records, and return only to T0.

Exact decision:

```text
GREEN / ANOTHER BOUNDED LOCAL FREQUENCY EVIDENCE GENERATED
YELLOW / ANOTHER BOUNDED LOCAL FREQUENCY EVIDENCE INCOMPLETE
RED / ANOTHER BOUNDED LOCAL FREQUENCY ARTIFACT INVALID
```

T8as never dispatches T7cf.

## Recovery And Forbidden Work

Twelve hours is a soft notification. Confirmed capacity/system interruption
may resume the same T8 task only after exact process/transaction/implementation
identity verification. It cannot bypass genuine failures. No threshold
relaxation, lmax extension, automatic/recursive midpoint, uniform/full grid,
production, plot, fixture, Kirchhoff, paper-style output, task
creation/replacement, downstream dispatch, or GitHub action.
