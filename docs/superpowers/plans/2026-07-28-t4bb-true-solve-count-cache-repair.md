# T4bb True Solve-Count Cache Repair Plan

## Goal

Close the sole T7ch YELLOW blocker without changing science or the frozen
`0.70` performance threshold.

## Phase A — Zero Science

1. Reconstruct T7ch counts directly: `3668/3668`, limit `2567`.
2. Rehash the accepted six-pair matrix, T4ad oracle file, runtime,
   complete-290, fixed identity, and canonical/process guards.
3. Add the narrow real-execution cache adapter and strict T4ad/low-frequency
   cache loaders.
4. Add exact source/dataflow tests proving cache hits bypass the scientific
   solver and every miss/fallback is counted as a solve.
5. Add fixtures for all identity, radius, point, tolerance, ordering,
   duplicate, partial, alias, schema, content, origin, and consumer
   mismatches.
6. Freeze the arithmetic `986 + 166` cache coverage and online total `2516`.
7. Run exact Python 3.14 focused/full tests, Ruff, diff/scope checks, and
   synthetic plus real zero-science preflight.
8. Return `CHECKPOINT / TRUE SOLVE-COUNT CACHE DATAFLOW FROZEN`.

T4 may self-correct only diagnosed pre-execution artifact-local defects in
fresh roots. It must not launch science.

## Phase B — Separately Authorized Cache Producer

After root-T0 audit, run one producer and one audit for the exact
`kM=0.86875` cache:

- 166 unique `(sector,ell)` ordinary solves;
- 1328 exact point records;
- unchanged solver/config/runtime/source/input identity;
- immutable, indexed, fsynced, no-overwrite evidence.

Return a cache checkpoint only. Do not run the six-case benchmark yet.

## Phase C — Separately Authorized Benchmark

After root-T0 cache audit, run the exact six cases serially in a new isolated
root. Each case receives one producer and one official audit. Close the full
scientific, provenance, atomic, process, timing, RSS, and true-solve-count
gates. Existing canonical outputs remain read-only.

## Stops

- A pre-execution local control defect: preserve the failed root, repair in a
  fresh root, and continue.
- A scientific child failure: consume the authorization and return to T0.
- Any proposed science/runner/runtime/input/criteria change: return to T0
  before edit or launch.
