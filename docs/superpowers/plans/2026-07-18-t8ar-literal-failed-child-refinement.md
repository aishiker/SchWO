# T8ar Literal Failed-Child Refinement Plan

## Goal

After T7cc exact ACCEPT GREEN and T0 fresh verification, generate point-only
complex amplification evidence at exactly the 41 frozen failed-child
midpoints. T8ar is an evidence producer, not the scientific reviewer.

## Start Gate

Require all of the following before editing or computing:

- exact reviewed candidate identity and unchanged design/plan/prompt hashes;
- T4ac exact GREEN and T7cc exact ACCEPT GREEN;
- T0 fresh verification of 146,416 rows, 41 checkpoints, snapshots, direct
  oracle, literal envelope, tests, hashes, and five-path scope;
- unchanged T8aj/T8ao/T8ap/T8aq sources and exact T7cb handoff hash;
- existing T8 task identity; no replacement task.

Any missing or ambiguous condition stops.

## Exact Implementation Scope

Before the first real frequency, create one commit changing exactly:

```text
scripts/phase5_run_literal_failed_child_refinement.py
src/schwgw/io/__init__.py
src/schwgw/io/tablei_literal_failed_child_refinement.py
tests/unit/test_tablei_literal_failed_child_refinement.py
tests/regression/test_literal_failed_child_refinement_script.py
```

No solver, radial envelope, source artifact, config, observable, fixture,
visualization, Kirchhoff, or unrelated path may be changed.

## Task 1 — Contracts And TDD RED

- Hard-code the exact 41 frequencies, tokens, lmax windows, five sequences,
  and literal 41-row parent map from the design.
- Define schema
  `phase5_t8ar_literal_failed_child_refinement_v1_units_dtype_ordering`.
- Define canonical generation and metadata contract payloads binding physical
  inputs, implementation commit/blobs, source/gate hashes, units, actual
  dtypes, ordering, flags, and atomic transaction rules.
- Add fake-compute RED tests for frequency/token/lmax membership, exact point
  order, masks, histories/final rows, sidecar/embedded metadata, contracts,
  resume identity, 87/86 cardinality, and forbidden output isolation.

## Task 2 — Fake Compute, Ruff, And Pre-Run Commit

- Implement only enough to make fake-compute tests pass.
- Run focused tests and Ruff on the exact five paths.
- Create the exact five-path implementation commit.
- Record commit parent, commit time, all five blobs/SHA-256 values, and require
  that it predates the first real transaction.
- Recompute generation/metadata contract identities after the commit and
  freeze them before real work.

## Task 3 — Atomic 41-Frequency Execution

- Write exactly one NPZ/JSON transaction pair per frozen frequency under
  `runs/phase5/fig5_fig6_literal_failed_child_refinement/frequencies/`.
- Each pair stores exact kM/token, point geometry/order, lmax history,
  final-row complex plus/cross values, magnitudes, principal phases, masks,
  final-pair deltas, warnings/counts, units, actual dtypes, fingerprints,
  source/gate hashes, contracts, and implementation identity.
- Publish a ledger entry atomically only after both files and hashes are
  complete.
- Resume reuses only a complete pair matching every scientific,
  implementation, contract, and source identity. Never reuse across identity
  and never recompute a complete matching frequency.
- A final-pair failure at unchanged `1e-4`, nonfinite value, false mask,
  metadata mismatch, or scope/provenance error stops without aggregation.

## Task 4 — Aggregate And Diagnostic Audit

- Direct-stack all 41 transactions in frozen frequency order.
- Require exact array key/order/shape/dtype/value identity with direct rows.
- Write exactly one ledger, aggregate NPZ, aggregate JSON, diagnostic audit,
  and non-self manifest.
- Require exactly 87 active files and 86 manifest records, no temp/quarantine.
- Reconstruct exactly 1,472 phase records, 1,312 hierarchy records, and 80
  summaries from the frozen sequences/map.
- Record all values, failures, extrema, total variation, and cancellation for
  later independent review, but set `diagnostic_only=true` and
  `acceptance_decision_emitted=false`.
- Emit no scientific decision, next midpoint, or threshold.

## Task 5 — Fresh Verification And Handoff

- Independently reload every pair and verify hashes, arrays, metadata,
  fingerprints, ledger, contracts, aggregate, audit, and manifest.
- Run focused pytest, Ruff, fresh full pytest, `git diff --check`, exact
  five-path scope, source-tree cleanliness, and both forbidden-output searches.
- Archive the prior T8 handoff byte-for-byte, update status/T8 handoff, and
  return only to T0.

Exact decision:

```text
GREEN / LITERAL FAILED-CHILD FREQUENCY EVIDENCE GENERATED
YELLOW / LITERAL FAILED-CHILD FREQUENCY EVIDENCE INCOMPLETE
RED / LITERAL FAILED-CHILD FREQUENCY ARTIFACT INVALID
```

T8ar must not dispatch T7cd.

## Recovery

Twelve hours is a soft notification. On confirmed capacity/system
interruption, T0 may resume the same T8 task with `gpt-5.6-terra/high` only
after process, pre-run commit, 41 transaction pairs, ledger, contracts,
source hashes, tests, scope, and status establish exact safe identity. Model
switching never bypasses a scientific, numerical, nonfinite, test, scope, or
provenance failure.

## Forbidden Outputs

No lmax extension, automatic/recursive midpoint, uniform/full grid,
production, plot, fixture, Kirchhoff, paper-style work, threshold relaxation,
new task, downstream dispatch, or GitHub action.
