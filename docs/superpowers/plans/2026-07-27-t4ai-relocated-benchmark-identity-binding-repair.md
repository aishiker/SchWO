# T4ai Relocated Benchmark-Identity Binding Repair Plan

## Goal

Bind the exact benchmark identity produced by the frozen runner in a
content-identical detached project, without accepting arbitrary identities
or changing any scientific, runtime, input, canonical, supervision, or
matrix contract.

## Task 1 — Independent Package Review

The existing T7 task independently reads the exact four-file T4ai candidate,
the frozen T4ae–T4ah packages, the complete consumed T4ah witness, the
driver, runner identity code, runtime, complete 290-input contract,
canonical/absence guards, and current process/transient state.

Only exact:

```text
REVIEW GREEN / T0 RELOCATED BENCHMARK IDENTITY REPAIR APPROVED
```

permits Task 2.

## Task 2 — Start Gate And Evidence Freeze

In the existing T4 task using `gpt-5.6-sol/high`:

1. verify candidate/ref/parent/exact-four and primary/exact-fourteen;
2. verify all T4ah Phase-A and complete-input identities;
3. reconstruct the consumed witness request, final, streams, control audit,
   pair audit, start-gate failure, no official audit, and consumed
   authorization;
4. verify producer exit0/no-signal/empty-stderr/exact-wait/reaped/PG-empty;
5. verify the isolated pair, all 22 bitwise comparisons, runtime pre/post,
   canonical guards, four absent units, and zero process/transient state;
6. preserve the old driver and consumed witness immutably in fresh unique,
   no-overwrite, fsynced control evidence.

No scientific runner, official audit, solver, witness, or matrix process may
start.

## Task 3 — Reconstruct Both Exact Identities

Using the runner's exact `_json_safe` plus `json.dumps(..., sort_keys=True,
separators=(",", ":"), allow_nan=False)` canonical JSON, with the runner's
default `ensure_ascii=True` and then UTF-8 encoding:

1. reconstruct the primary five-file gate manifest
   `101823bb...4eb4`;
2. reconstruct the primary gate component `e1fb15b9...4626` and benchmark
   identity `46403a00...6c48`;
3. reconstruct the consumed detached gate manifest
   `b0c9b521...8a09`;
4. reconstruct its gate component `be637521...3f25` and benchmark identity
   `db7937fa...69b8`;
5. prove the seven-field benchmark payload differs only in `gate_sha256`;
6. prove that difference is caused only by the five absolute gate paths,
   with identical repository-relative paths, bytes, hashes, and sizes.

Any mismatch stops before driver edits.

## Task 4 — Bounded Driver Remediation

Modify only the artifact-local durable driver and solver-free tests:

- retain the fixed primary identity for matrix/canonical context;
- deterministically derive and bind one exact detached identity before a
  witness launch;
- publish the complete derivation and full payload in durable request and
  prelaunch records;
- validate producer stdout, embedded metadata, sidecar, durable records,
  producer audit, and matching official audit against that exact binding;
- require producer and official audit to use the same detached root and
  derived identity;
- fail closed on arbitrary observation, root/path/content/payload drift,
  phase confusion, alias/link/writeability, or producer/audit mismatch.

Do not edit the runner, implementation, configs, runtime overlay, 290
inputs, source manifest, scientific code, canonical/legacy/golden data,
thresholds, tolerances, points, modes, resolution, or `lmax`.

## Task 5 — Full Python-3.14 Zero-Science Preflight

Run one fresh unique preflight after static review:

1. all prior T4ah positive/negative/fault/lock/audit/runtime/input tests;
2. all prior CLI captures and rejection reasons;
3. primary and consumed-detached exact derivation fixtures;
4. synthetic fresh-root relocation positives, including one root with
   non-ASCII path components that is byte/digest identical to the runner;
5. a paired Unicode-root negative proving an `ensure_ascii=False`
   serialization differs and is rejected, plus every other negative listed
   in the design;
6. AST, Ruff, `git diff --check`, raw/capture indexes, exact Python/runtime,
   old-evidence, canonical, absence, process/transient, link, and
   no-bytecode guards.

Scientific runner CLI, official audit, and solver invocation counts must be
zero. Any failure is immutable and stops without retry.

Return exactly:

```text
CHECKPOINT / RELOCATED BENCHMARK IDENTITY CONTRACT FROZEN
```

Then pause.

## Task 6 — Root-T0 Checkpoint Audit

Root T0 independently recomputes:

- old/new driver hashes and exact diff;
- primary and detached benchmark-identity derivations;
- full phase-specific validator semantics and negative coverage;
- manifest/raw/capture/test/rejection/runtime evidence;
- T4ah HOLD evidence and pair immutability;
- complete-290, canonical, absent-unit, link, process, and transient guards.

Only complete PASS permits a fresh one-shot witness authorization.

## Task 7 — New Full One-Shot Witness

Provision one new detached exact-`45face32` project with the complete 290
inputs and frozen hermetic runtime. Derive and freeze that new root's exact
benchmark identity before launch.

Run one existing `kM=1.58125` producer. Only producer exit0, empty stderr,
exact reaping, PG-empty, runtime pre/post PASS, atomic pair PASS, exact
identity binding, and unchanged canonical guards permit the matching
official audit.

The audit uses the same root/runtime/identity and the same durable driver.
Any failure consumes the authorization and stops. Do not reuse the consumed
T4ah pair and do not retry.

## Task 8 — Conditional Matrix And Final Gate

After root-T0 witness PASS, continue exactly:

```text
2.91875 -> 3.759375 -> 3.89375 -> full 241x241
```

one unit at a time. Matrix context continues to require the fixed primary
benchmark identity.

Only final T4 GREEN plus root-T0 full T4ae PASS permits frozen T7ch.

## Safety And Budget

- Existing T4 and T7 tasks only.
- No task, subagent, proxy, or descendant.
- T4 code uses `gpt-5.6-sol/high`; no `max` or `ultra`.
- No network/install/global mutation/destructive cleanup.
- No new frequency, production/plot/fixture/Kirchhoff/paper/GitHub work.
