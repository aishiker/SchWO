# T4ad Another Bounded Local Radial Gate Plan

## Goal

Build a fail-closed radial/Q018 gate for exactly the 55 frequencies frozen in
`docs/superpowers/specs/2026-07-20-t4ad-t8as-another-bounded-local-refinement-design.md`.
This is radial-only prerequisite evidence and produces no amplification,
observable, plot, fixture, Kirchhoff, or paper artifact.

## Frozen Start Gate

- current `project.md`, `status.md`, and T0/T4/T7/T8 handoffs;
- exact candidate commit/blob/SHA identity and matching independent package
  review GREEN;
- T7cd handoff SHA-256
  `99b669d8f7b1000dd9f002c27d1234a286d00690ad82c84f1ebc159ec33bb3fc`;
- immutable T8ar 41-pair package, five roots, contracts, implementation commit,
  T4ac roots/snapshots, and earlier accepted sources;
- exact 55 frequencies/tokens/lmax windows, maximum lmax 360, and 239,120
  classification records;
- unchanged physical, boundary, solver, Q018, and tolerance conventions.

Any mismatch stops before edit or computation.

## Exact Implementation Scope

Create one pre-compute commit changing exactly:

```text
scripts/phase5_another_bounded_local_radial_gate.py
src/schwgw/numerics/q018_tablei_another_bounded_local_envelope.py
src/schwgw/numerics/radial_solver.py
tests/physics/test_q018_production_integration_design.py
tests/physics/test_radial_solver.py
```

The adapter label is `q018_tablei_another_bounded_local_transition`.

## Task 1 — Literal Derivation And TDD RED

- Independently derive 55 unique failed-child midpoints from all 253 T7cd
  hierarchy failures; require all five phase failures and zero extras.
- Recompute every token/lmax window and 239,120 Cartesian keys.
- Add focused failing tests for exact sector-aware membership, wrong-identity
  fail-closed behavior, default-covered zero-oracle calls, and snapshot bridge.
- Run focused RED tests before implementation.

## Task 2 — Classification Snapshot

- Classify both sectors, all eight points, and every `ell=2..ell_max`.
- Create exactly 55 frequency checkpoint identities.
- Bind candidate, T7cd/T8ar/T4ac sources, physical inputs, parent radial blob,
  and code hashes in a pre-adapter classification snapshot.
- Require exactly 239,120 unique complete Cartesian keys and zero unclassified
  default-error-other rows.

No adapter edit occurs before this snapshot is immutable.

## Task 3 — Direct Oracle And Literal Envelope

- Measure structured transitions; never predict their count.
- Direct-oracle only measured transitions using unchanged residual,
  normalization, log-derivative, condition, finiteness, and sensitivity gates.
- Generate an exact sector-aware literal envelope containing only measured
  frequency/sector/ell/point membership and saved oracle values.
- No run-file IO at import time, inference, interpolation, or public-default
  broadening.
- Every wrong frequency, point, ell, sector, boundary, tolerance, or
  implementation identity fails closed.

## Task 4 — Final Snapshot And Commit

- Run focused tests and Ruff on the exact five paths.
- Create the exact five-path implementation commit before final gate output.
- Freeze a final-adapter snapshot binding the commit and all five blobs.
- Prove the final snapshot bridges to unchanged classification membership.
- Validate integrated adapter anchors against direct saved values; require
  default-covered zero oracle calls.

## Task 5 — Atomic 55-Frequency Gate

- Write one complete atomic checkpoint per frequency under
  `runs/phase5/fig5_fig6_another_bounded_local_radial_gate/`.
- Reuse only a complete PASS checkpoint matching exact classification
  snapshot, contract, output hash, frequency, and implementation identity.
- Never recompute a complete matching frequency or reuse across identity.
- Capacity/system interruption stops at an atomic boundary and returns an
  exact safe-resume identity to T0.
- Scientific/numerical/nonfinite/test/scope/provenance failure returns
  YELLOW/RED and cannot be bypassed by model change.

## Task 6 — Fresh Verification And Handoff

- Require 55/55 complete/PASS checkpoints and exact manifest membership.
- Run direct-oracle preflight for every measured transition.
- Run focused pytest, Ruff, fresh full pytest, `git diff --check`, exact-five
  scope/source checks, warning classification, and forbidden-output searches.
- Archive the prior T4 handoff byte-for-byte, update only allowed T4/status
  records, and return only to T0.

Exact decision:

```text
GREEN / ANOTHER BOUNDED LOCAL RADIAL GATE READY
YELLOW / ANOTHER BOUNDED LOCAL RADIAL EVIDENCE INCOMPLETE
RED / ANOTHER BOUNDED LOCAL RADIAL GATE INVALID
```

T4ad never dispatches T7ce or T8as.

## Forbidden

No threshold relaxation, lmax extension, automatic/recursive midpoint,
uniform/full grid, amplification, production, plot, fixture, Kirchhoff,
paper-style output, task creation/replacement, or GitHub action.
