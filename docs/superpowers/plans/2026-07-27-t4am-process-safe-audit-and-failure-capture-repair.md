# T4am Process-Safe Audit And Failure-Capture Repair Plan

## Goal

Replace the exhausted T4al helper with a reviewed file-launched helper that
uses structured ancestry-aware process validation and durably closes every
safe audit attempt, including child failures.

## Task 1 — Independent T7cp Package Review

The existing T7 task fully reads and independently checks:

- exact T4am four-file candidate;
- all five immutable helper attempts, including the T4al helper-only root,
  empty audit root, and absent execution root;
- T4al source/AST and the epistemic limit caused by lost stderr;
- the proven reachable naive process-scanner defect without treating it as
  the established historical cause;
- driver, runner, v10, runtime, complete-290, consumed witness, canonical,
  absence, external, link, process, and transient guards.

Only exact:

```text
REVIEW GREEN / T0 PROCESS-SAFE AUDIT AND FAILURE-CAPTURE REPAIR APPROVED
```

permits Task 2.

## Task 2 — T4am Phase A0

In the existing T4 task using `gpt-5.6-sol/high`:

1. fresh-verify candidate/ref/parent/exact-four and every frozen guard;
2. choose fresh helper, audit-record, and reserved execution roots;
3. freeze one helper in the helper root and one file-based launcher in the
   audit-record root;
4. require direct exact AST constants and forbid implicit root discovery;
5. close helper root before the first child;
6. statically audit shared collection, stable payload, process classifier,
   failure capture, terminal branches, and forbidden surfaces;
7. run all zero-science positive/negative process-classifier tests;
8. launch the file-based launcher once with `--first-attempt`;
9. require pre/post no-write equality and execution-root absence before any
   record publication;
10. persist exact raw stdout/stderr/exit/wait plus common records on either
    child success or child failure;
11. close exactly one non-self-referential success or failure terminal
    branch;
12. preserve helper and launcher identities and zero scientific counts.

Failure returns HOLD with durable evidence. Success returns exactly:

```text
CHECKPOINT / T4AM PROCESS-SAFE AUDIT HELPER FROZEN
```

Then pause.

## Task 3 — Root-T0 Independent Replay

On a success checkpoint only, root T0:

- reads all sources and records fully;
- independently parses AST/dataflow and validates every classifier fixture;
- recomputes all evidence and frozen guards;
- invokes the frozen launcher once in no-write `--replay-only` mode using
  identical explicit argv/cwd/environment;
- requires byte-identical stdout/digest, empty stderr, exit zero,
  helper/audit roots unchanged, execution absent, and zero science.

Any mismatch stops. Complete PASS permits one later digest-bound execute
authorization.

## Task 4 — One-Shot Preservation And T4ai Resume

Only after separate root-T0 authorization:

1. run the exact reviewed helper execute once;
2. create only the reserved execution root;
3. publish the exact old-driver snapshot, before manifest, execution
   result, and durable records;
4. leave helper/audit roots untouched;
5. on preservation PASS, resume only the reviewed artifact-local T4ai
   driver/tests;
6. run one fresh exact-Python-3.14 zero-science preflight;
7. stop at
   `CHECKPOINT / RELOCATED BENCHMARK IDENTITY CONTRACT FROZEN`.

Any failure consumes its authorization without retry.

## Safety And Budget

- Existing T4/T7 tasks only.
- T4 uses `gpt-5.6-sol/high`; no `max` or `ultra`.
- No task, subagent, proxy, or descendant.
- No reuse, population, chmod, execution, rename, deletion, or mutation of
  any failed helper/audit root.
- Do not claim a specific T4al dynamic cause; exact stderr is unrecoverable.
- No runner/science/runtime/complete-290/canonical/threshold change.
- No destructive cleanup, outside mutation, network/install/global change,
  new frequency, production, plot, fixture, Kirchhoff, paper, or GitHub
  action.
