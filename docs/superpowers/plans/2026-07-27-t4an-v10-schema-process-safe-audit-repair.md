# T4an V10-Schema And Process-Safe Audit Repair Plan

## Goal

Freeze and validate the exact v10 manifest container schema before running
one process-safe, failure-durable Phase-A0 audit attempt.

## Task 1 — T7cq Independent Review

The existing T7 task reads the exact T4an package and independently:

- reconstructs T4al's immutable failure boundary;
- confirms exact stderr/cause remain unrecoverable;
- proves the list-versus-integer v10 validator defect and its position
  before `process_snapshot()`;
- recomputes the exact 1041-record raw index with the generating driver's
  explicit `ensure_ascii=False` serializer;
- proves the v10-index serializer is distinct from the helper replay
  payload's default `ensure_ascii=True` serializer using the frozen
  non-ASCII discriminator;
- reviews the v10 negative fixtures, process classifier, dual terminal
  branches, replay boundary, and all frozen guards.

Only exact:

```text
REVIEW GREEN / T0 V10-SCHEMA PROCESS-SAFE AUDIT REPAIR APPROVED
```

permits Phase A0.

## Task 2 — T4an Phase A0

In existing T4 using `gpt-5.6-sol/high`:

1. fresh-verify candidate/ref/parent/exact-four and every upstream guard;
2. choose one fresh helper/audit/reserved-execution triple;
3. freeze one helper and one file-based launcher;
4. statically audit direct root constants and forbidden surfaces;
5. implement one pure exact v10 validator with a dedicated
   generator-exact `ensure_ascii=False` raw-index serializer;
6. keep helper canonical stdout/replay on its separate default
   `ensure_ascii=True` serializer and statically reject cross-use;
7. run all in-memory zero-science v10 positive/negative fixtures,
   including the frozen non-ASCII serializer discriminator;
8. run all zero-science process-classifier fixtures;
9. launch exactly one frozen-file `--first-attempt` audit-only child;
10. require no-write pre/post equality before publication;
11. persist raw streams and terminal status on success or failure;
12. close exactly one non-self-referential terminal branch;
13. preserve execution absence and zero science.

Failure returns HOLD with durable evidence and no retry. Success returns:

```text
CHECKPOINT / T4AN V10-SCHEMA PROCESS-SAFE AUDIT HELPER FROZEN
```

Then pause.

## Task 3 — Root-T0 Independent Replay

On success only, root T0:

- reads every source/record byte;
- independently redoes AST/dataflow and all v10/process fixture logic;
- recomputes v10/raw/capture/test/runtime/complete-290 identities;
- invokes the frozen launcher once in no-write `--replay-only`;
- requires byte-identical stdout/digest, exit0, empty stderr, roots
  unchanged, execution absent, and zero science.

Only full PASS permits a separate one-shot digest-bound execute
authorization.

## Task 4 — Resume Reviewed T4ai Work

Preservation execute PASS may resume only:

- the already reviewed T4ai artifact-local driver/test repair;
- one fresh exact-Python-3.14 zero-science preflight;
- exact
  `CHECKPOINT / RELOCATED BENCHMARK IDENTITY CONTRACT FROZEN`.

No implicit retry exists at any stage.

## Safety

- T4am/T7cp GREEN is not sufficient; T4am was never dispatched.
- Do not mutate or reuse any failed root.
- Existing T4/T7 tasks only; no agent descendants.
- T4 uses sol/high; max and ultra forbidden.
- No runner/science/runtime/complete-290/canonical/threshold change.
- No destructive/outside/global/network/downstream/new-frequency/
  production/plot/fixture/Kirchhoff/paper/GitHub action.
