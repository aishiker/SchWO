# T4ba Full-Image Cache-Accounting Projection Repair Plan

## Goal

Freeze and verify one full-image-only artifact-local projection that replaces
the unchanged runner's wrong in-memory cache-hit literal `10367742` with the
independently derived and legacy-exact value `20735842`.

Phase A is zero-science and stops at:

```text
CHECKPOINT / FULL-IMAGE CACHE ACCOUNTING PROJECTION FROZEN
```

## Phase 0 — Start Gate

1. Read project/status/current T0/T4/T7/T8 handoffs and complete T4ba/T7da
   package and decision.
2. Verify candidate/ref/parent/primary HEAD and exact-four bytes.
3. Reconstruct the consumed full unit from raw durable files: 3234 files,
   363 directories inclusive, 627046494 bytes and tab-ledger
   `330dfdcf...520b`.
4. Verify PID 34989, exit1/no signal/exact wait/reap/PG-empty, start-only
   stdout, exact stderr/final and official-audit/checkpoint/full-pair
   absence.
5. Independently reproduce `197/57884/48/57932/358/20735842` from the
   frozen config and execution order.
6. Verify the immutable legacy raw record contains exact
   `20735842/57932/358`.
7. Reverify accepted frequency checkpoints, runner/driver/shim,
   implementation, runtime, complete290, fixed identity, canonical 5/6,
   process/transient/link guards.

Any unexplained drift stops before edits.

## Phase 1 — Freeze Wrapper Surface

1. Create one fresh unique no-overwrite Phase-A evidence root.
2. Preserve the consumed full root and all accepted frequency units
   immutable.
3. Add one immutable full-image-only wrapper above exact T4az.
4. Intercept only exact T4ar adapter loading and wrap only
   `adapter._load_runner`.
5. Call original runner loader first, verify every frozen identity/formula,
   replace only the loaded module constant and delegate unchanged main.
6. Update only the artifact-local driver's physical launcher path/SHA.

## Phase 2 — Zero-Science Tests

Run all static, AST, dataflow, formula, positive and negative fixtures in
the frozen design under exact Python3.14.

Validate fresh prospective producer and audit requests, physical/logical
argv, source/input/runtime/fixed identity and process provenance without
launching either child.

Run at most the frozen synthetic and real split-root zero-science
preflights, both requiring `solver_started=false`, empty stderr and zero
output/canonical mutation.

## Phase 3 — Checkpoint

Freeze:

- consumed unit inventory and durable identities;
- old/new wrapper and driver hashes plus exact driver diff;
- wrapper source/AST/dataflow and pure formula record;
- config/legacy raw identities and derived counts;
- all positive/negative tests and rejection reasons;
- prospective producer/audit requests;
- runner/runtime/complete290/fixed identity;
- accepted five pairs/checkpoints and full-pair absence;
- process/transient/link guards and all science invocation counts zero.

Return the exact checkpoint and stop for root-T0 audit.

## Later Gate

Only a separate root-T0 authorization permits one new full producer and,
after producer PASS, one matching official audit and atomic closeout.
Launched failure consumes the unit and forbids retry.
