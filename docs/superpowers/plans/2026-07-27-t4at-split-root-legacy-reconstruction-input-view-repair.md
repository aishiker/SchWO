# T4at Split-Root Legacy Reconstruction Input-View Repair Plan

## Goal

Repair only the artifact-local durable reconstruction root geometry that
collapsed the exact-clean legacy source worktree, complete-290 input view,
and process cwd into one directory.

The consumed T4as producer attempt remains immutable and non-reusable.
Phase A is zero-science and stops at:

```text
CHECKPOINT / LEGACY RECONSTRUCTION SPLIT-ROOT INPUT VIEW FROZEN
```

## Phase 0 — Start Gate

1. Read project/status/current T0/T4/T7/T8 handoffs and the complete
   T4ar/T4as/T4at packages.
2. Verify candidate/ref/parent/primary HEAD and exact-four bytes.
3. Reconstruct the consumed T4as request/setup/prelaunch/running/receipt/
   streams/failure-audit/checkpoint chain and root ledger.
4. Verify PID/SID/PGID `37999`, exit64/no-signal/exact-wait/reaped/PG-empty,
   exact stderr, empty stdout, no pair, no audit, no matrix, and consumed
   authorization.
5. Prove from raw manifests and source that the source and input roots are
   the same and the ten exact Git-status paths are complete-input targets.
6. Reverify driver/launcher/runtime/complete290/legacy/golden/warning/
   canonical/absence/process/transient/external guards.

Any drift stops before edits.

## Phase 1 — Preserve Old Control

1. Create a fresh unique no-overwrite Phase-A root.
2. Snapshot exact driver
   `883375d9...318ef`, unchanged launcher `20bd7e78...79c6`, T4as
   checkpoint, consumed failure evidence, canonical and global guards.
3. Preserve every failed/prospective root and both failure locks unchanged.

## Phase 2 — Implement The Split

Modify only the artifact-local durable driver and local zero-science
tests/helpers.

1. Keep the two T4as reconstruction phases and all older phases unchanged.
2. Treat reconstruction `detached_root` as the clean exact-legacy source
   root.
3. Make request cwd and allowed cwd root equal a separate complete input
   root.
4. Bind runtime project root and `PYTHONPATH` to clean source root/src.
5. Require source/input/output/HOME/producer-run/audit-run roots distinct,
   direct-child, non-aliased, and non-overlapping.
6. Require exact empty Git status and exact 48 tracked-source inventory in
   the clean source worktree.
7. Require exact 290-entry input inventory and contract below the separate
   input root.
8. Route post-child source validation to the bound source root, never cwd.
9. Require exact producer/audit equality for both roots and all derived
   manifests/runtime records.
10. Leave the reconstruction launcher byte-identical.

## Phase 3 — Zero-Science Tests

Run exact Python 3.14 static, AST, dataflow, format/diff, and focused tests.
Exercise:

- positive prospective producer and audit requests;
- clean source plus separate complete input view;
- import-only exact legacy module origins under detached cwd;
- all root equality/containment/alias/link/writability/status/commit/input/
  environment/origin/producer-audit/post-drift negatives;
- unchanged old-phase behavior;
- unreachable launcher/runner/solver/science during Phase A.

Diagnosed helper/fixture/manifest/path/serialization/bookkeeping/test defects
must be preserved in immutable failed roots and corrected in fresh roots.
There is no blind retry and no fixed correction limit.

## Phase 4 — Real Zero-Science Prospective Geometry

1. Create one fresh exact-clean legacy source worktree.
2. Create one separate empty input root and materialize exact complete-290.
3. Derive fresh source/input manifests.
4. Use exact isolated HOME and frozen overlay.
5. Run one import-only/input-identity probe with cwd=input root and
   source imports from clean legacy root/src.
6. Build and validate producer plus matching-audit templates without
   launching either child.
7. Verify source/input pre/post equality, output/run absence, canonical
   unchanged, eight later files absent, no process/transient.

Freeze all prospective evidence as non-reusable.

## Phase 5 — Checkpoint

Freeze:

- new driver SHA and exact old/new diff;
- unchanged launcher SHA;
- source/input/root/runtime manifests and indexes;
- all test and rejection results;
- import-only stdout/stderr/exit and module-origin records;
- science invocation counts all zero;
- canonical/absence/process/transient/external guards.

Return exact checkpoint and stop for root-T0 audit.

## Later Gates

Only root-T0 Phase-A PASS permits one new reconstruction producer and,
after producer PASS, one matching reconstruction audit. Any launched failure
consumes that authorization and forbids retry.

After witness PASS and root audit, T4ar Phase C remains a separate
zero-science authorization. Only Phase-C PASS and root audit may authorize
the next matrix `2.91875` unit.
