# T4ar Legacy Raw-Warning Reconstruction Repair Plan

## Goal

Reconstruct the missing legacy `2.91875` raw-warning observation under the
exact legacy implementation, then bind that independently audited witness
to an artifact-local launcher while leaving all scientific and canonical
bytes unchanged.

## Frozen Failure

- Root:
  `t4ae_matrix_unit_2p91875_t4aq_053ffd06_20260727T181202p0800/unit_2p91875`.
- Checkpoint:
  `0d485dcd3be9153a41e4183666f98f703dbd4e38c5ddb7a10f5ea0d7706389db`.
- Final:
  `09f909759edb464707102e567758faf1c0f4dcdc5dcbb401ac66bcbe60fd49a6`.
- Control audit:
  `56f990a7dc6ea8f0e4d9e9aa73cd2f60384781af653b021a19f29ebf338b39bd`.
- Child `76752`: exit1, no signal, exact wait/reap, PG empty.
- Official audit and later units: not started.
- Canonical: unchanged; all eight later files absent.

## T7cu Review

1. Rebuild the consumed failure hash/process/stream/tree boundary.
2. Verify the three exact rejected warnings and frozen overlay file.
3. Prove exact overlay-relative normalization yields diagnostic record
   `624 / e9ce10c6...15d97`.
4. Verify the legacy `2.91875` sidecar truly lacks the raw-warning key and
   was produced by baseline v1; do not treat absence as zero.
5. Verify v3 later-frequency sidecars demonstrate the intended raw-warning
   schema without importing their counts into `2.91875`.
6. Audit the exact detached legacy-commit reconstruction geometry and
   unchanged `_compute_frequency` call.
7. Require bitwise equality for all 22 scientific arrays against immutable
   legacy and golden NPZs.
8. Audit strict warning-source mapping, supplemental witness schema, and
   original-legacy-audit-first dataflow.
9. Audit one-shot supervision, matching reconstruction audit, and
   Phase-A/B/C root-T0 gates.
10. Verify no runner/source/runtime/input/identity/criteria/order or
    legacy/golden/canonical artifact can change.

Only exact:

```text
REVIEW GREEN / T0 LEGACY RAW-WARNING RECONSTRUCTION REPAIR APPROVED
```

permits T4ar Phase A.

## T4ar Phase A

1. Read all handoffs, the reviewed package, both consumed `2.91875`
   attempts, T4aq checkpoint, driver/launcher, runner, legacy/golden pair,
   runtime overlay, complete inputs, witness, canonical and process guards.
2. Preserve all old roots and attempts immutably.
3. Freeze one legacy reconstruction launcher and one matrix adapter
   launcher in fresh artifact-local evidence.
4. Extend only the durable driver and solver-free tests.
5. Implement exact overlay-file source normalization with content/stat/
   manifest binding and no broad matching.
6. Implement original-legacy-audit-first supplementation for exact
   `2p91875` only.
7. Make the witness path/hash explicit in request, producer, audit, final,
   replay, and checkpoint bindings.
8. Freeze a one-shot detached-`8fb8608c...` reconstruction protocol that
   calls unchanged legacy `_compute_frequency()` exactly once.
9. Add all positive/negative tests from the design, including field
   absence versus explicit empty/null/malformed and exact 22-array checks.
10. Run exact Python-3.14 static/AST/dataflow/Ruff/diff checks, full
    zero-science synthetic preflight, and one real zero-science split-root
    matrix preflight with synthetic witness data.
11. Freeze identities and return:

```text
CHECKPOINT / LEGACY RAW-WARNING RECONSTRUCTION CONTROL FROZEN
```

No solver, reconstruction, producer, official audit, or canonical write is
authorized in Phase A.

## Phase B

After separate root-T0 authorization, run exactly one isolated legacy
`2.91875` reconstruction producer. Require exit0, empty stderr, exact
wait/reap, PG empty, immutable runtime/input/source/canonical guards, atomic
isolated pair, exact structured warnings, and bitwise equality of all 22
scientific arrays to both immutable references.

Only after producer PASS, run one matching zero-science audit of that pair.
Any launch failure consumes the authorization with no retry. Success
returns:

```text
CHECKPOINT / LEGACY RAW-WARNING WITNESS PASSED
```

## Phase C

After root-T0 independently accepts the real witness, bind its exact
path/hash to the final artifact-local driver and matrix launcher. Run only
solver-free tests and one real matrix preflight. Require existing two-pair
audits PASS, fixed identity unchanged, `solver_started=false`, canonical
unchanged, and eight later files absent.

Success returns:

```text
CHECKPOINT / BOUND RAW-WARNING MATRIX ADAPTER FROZEN
```

Root T0 then performs a final audit before any new `2.91875` one-shot.

## Local Self-Correction

Diagnosed pre-execution zero-science artifact-local defects in Phase A or
Phase C must be corrected in fresh immutable roots. No unchanged blind
retry. A launched reconstruction or matrix child failure always consumes
its authorization and returns to root T0.

## Prohibited

No old-root mutation, warning suppression, missing-as-zero interpretation,
sidecar rewrite, legacy/golden/canonical mutation, runner/source/runtime/
input/identity/criteria/order change, output copy/promotion, parallel or
duplicate execution, alternate matrix order, new frequency, destructive or
global action, network/install, task/subagent/proxy/descendant, max/ultra,
production/plot/scientific-fixture/Kirchhoff/paper/GitHub/downstream work.
