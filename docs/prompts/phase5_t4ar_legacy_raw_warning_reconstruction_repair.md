# T4ar — Legacy Raw-Warning Reconstruction Repair

Work only in existing T4 task
`019f5fa6-1288-7c01-8a87-4c4370cf5517` using
`gpt-5.6-sol/high`. Never use `max` or `ultra`. Do not create a task,
subagent, proxy, or descendant.

## Start Gate

Fully read project/status/current T0/T4/T7/T8 handoffs, the exact T4ar
design/plan/T7cu prompt, T4aq/T4ap/T4ai packages, both consumed
`2.91875` attempts and complete records, current driver/launcher, unchanged
runner, immutable legacy/golden artifacts, exact legacy commit/source,
runtime overlay, complete-290 manifests, witness, canonical output, and
absence/process/transient state.

Require the exact candidate supplied by root T0 and exact:

```text
REVIEW GREEN / T0 LEGACY RAW-WARNING RECONSTRUCTION REPAIR APPROVED
```

Earlier review is not execution authority.

## Preserve The Failure

Freshly verify and preserve:

- failure checkpoint `0d485dcd...389db`;
- request/prelaunch/running/receipt/final/control audit and streams;
- child `76752` exit1/no-signal/exact-wait/reaped/PG-empty;
- driver `053ffd06...c6edd`;
- T4aq launcher `07270d15...37df`;
- runner `18e79435...a768`;
- primary HEAD `45face32...e9b82`;
- complete-290 `77927103...fd24`;
- fixed identity `46403a00...6c48`;
- runtime overlay `f3ebf3db...ab6ca`;
- witness checkpoint `2c448a70...cde9`;
- canonical four exact identities and all eight later-file absences;
- no related process, lock, tmp, partial, quarantine, link, or alias.

Do not mutate, chmod, rename, delete, populate, reuse, repair, promote, or
reinterpret either consumed `2.91875` attempt.

## Phase A Only

1. Create fresh unique no-overwrite durable evidence.
2. Preserve the old driver/launcher and all before guards.
3. Freeze one regular non-symlink nlink-1 read-only legacy reconstruction
   launcher and one final matrix adapter launcher.
4. Keep every scientific runner/source/runtime/input/canonical byte
   unchanged.
5. Implement only the exact overlay-file warning source mapping frozen in
   the design. Reject all broader or aliased mappings.
6. Extend the T4aq launcher without weakening original module verification
   or the exact two-module stable projection.
7. Wrap the original legacy audit only after calling it successfully.
   Supplement exact missing-field `2p91875` only, from one explicitly bound
   witness path/hash, and replace only the returned raw-warning record.
8. Freeze the detached exact-`8fb8608c...6ada` reconstruction protocol,
   but do not execute it in Phase A.
9. Require exact legacy/golden/source/input/runtime identities and exact
   22-array bitwise comparison in the future reconstruction output.
10. Extend only artifact-local durable driver/helper/launcher/tests so the
    witness and both physical launchers are bound across request,
    prelaunch, producer, audit, final, replay, and checkpoint records.
11. Add all positive/negative cases from the design, including:
    - exact 624-warning diagnostic derivation;
    - missing versus explicit empty/null/malformed sidecar field;
    - original-audit-first dataflow;
    - wrong witness scope/hash/runtime/input/array/structured/raw warning;
    - overlay path/hash/manifest/link/mode/alias failures;
    - unaffected other tokens;
    - T4aq 44/46 module closure;
    - producer/audit/final/replay mismatch;
    - no reachable legacy or canonical write.
12. Run exact Python-3.14 static/AST/dataflow/Ruff/diff/targeted tests and
    one full zero-science synthetic preflight.
13. Run one fresh real split-root `preflight/matrix` through the final
    launcher with a synthetic non-scientific witness.
14. Require exit0, empty stderr, exact wait/reap, PG-empty, existing two
    complete-pair audits PASS, fixed identity `46403a...`,
    `solver_started=false`, canonical unchanged, all eight later files
    absent, and zero residual state.
15. Recompute and freeze all identities.

Only complete PASS returns exactly:

```text
CHECKPOINT / LEGACY RAW-WARNING RECONSTRUCTION CONTROL FROZEN
```

Report old/new driver diff, both launcher identities and full source,
exact warning mapping and test cases, synthetic manifest/captures/results,
real preflight argv/cwd/env/streams/result, complete inputs/runtime/
witness/canonical/absence/process/transient guards, and exact science
counts `0/0/0`.

Then pause for root-T0 audit. Phase A does not authorize a reconstruction
producer, reconstruction audit, scientific runner, official audit, solver,
matrix unit, or canonical write.

## After Separate Authorization

Only exact root-T0:

```text
AUTHORIZED / LEGACY 2P91875 RAW-WARNING RECONSTRUCTION WITNESS
```

permits one detached exact-legacy reconstruction producer. It must call
unchanged legacy `_compute_frequency()` once for existing `2.91875`, use
the exact frozen overlay/runtime/inputs, capture every warning, and publish
only one isolated atomic pair. It must prove all 22 scientific arrays
bitwise equal to immutable legacy and golden NPZs and all structured
warnings exact.

Only durable producer exit0/no-signal/empty-stderr/exact-wait/reaped/
PG-empty PASS may start one matching `--mode audit`. A launched producer or
audit failure consumes the authorization and forbids retry.

Complete PASS returns exactly:

```text
CHECKPOINT / LEGACY RAW-WARNING WITNESS PASSED
```

and pauses for root-T0 audit.

Only later exact:

```text
AUTHORIZED / BIND LEGACY RAW-WARNING WITNESS TO MATRIX ADAPTER
```

permits zero-science Phase C binding of the accepted witness and one final
real matrix preflight. Complete PASS returns:

```text
CHECKPOINT / BOUND RAW-WARNING MATRIX ADAPTER FROZEN
```

and pauses before matrix science.

## Local Self-Remediation

Within Phase A or Phase C, do not return HOLD merely for a diagnosed
pre-execution zero-science artifact-local launcher/helper/test/manifest/
path/serialization/bookkeeping defect. Preserve each failed root, correct
the exact diagnosis in a fresh root, and continue without blind retry.

After any reconstruction or matrix scientific child starts, failure is
one-shot and must return to root T0 with no retry.

## Not Authorized

No old-root mutation, warning suppression/filtering/count adjustment,
missing-as-zero interpretation, legacy/golden/canonical rewrite, output
copy/promotion, runner/source/runtime/input/identity/criteria/order change,
alternate order, duplicate/parallel launch, new frequency, task/subagent/
proxy/descendant, max/ultra, destructive/global/network/install action,
production/plot/scientific-fixture/Kirchhoff/paper/GitHub/T7ch/downstream
work.
