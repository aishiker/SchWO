# T7cs — Split-Root Fixed-Primary Matrix Input-View Review

Work only in existing T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed` using
`gpt-5.6-sol/high`. Do not use `max` or `ultra`. Do not create a task,
subagent, proxy, or descendant.

## Scope

Perform one strict read-only review of the exact T4ap candidate supplied by
root T0. Read project/status/current T0/T4/T7/T8 handoffs, T4ap
design/plan/prompts, frozen T4ai/T4ao packages, relocated witness evidence,
driver, runner, complete-290 manifests, runtime overlay, current/archive
T7 records, canonical output, and absence/process/transient state.

Do not modify files, refs, artifacts, status, handoffs, permissions,
environment, or processes. Do not launch run/audit mode, solver, witness, or
matrix science. A read-only exact runner `preflight/matrix` reproduction is
permitted only with `PYTHONDONTWRITEBYTECODE=1` and the frozen environment.

## Required Independent Checks

1. Verify candidate/ref/parent/primary, exact-four added scope,
   commit/worktree byte equality, exact blobs/SHA/sizes, and
   `git diff --check`.
2. Verify all T4 model dispatches failed before sampling with
   `usage_limit_exceeded`; no matrix request/root/process/output or
   scientific attempt exists.
3. Recompute current `docs/handoffs/T7_current.md` SHA
   `ef05071f...f1c0` and archived T7ce SHA `4b801e46...90d1c`; verify their
   exact YELLOW/GREEN decisions.
4. Inspect unchanged runner/Table-I source and prove:
   runner path fixes primary gate/output identity, imported source must be
   primary, and relative Table-I input paths resolve from cwd.
5. Confirm a primary-cwd preflight rejects the drifted current record before
   solver start.
6. Independently run the exact split-root zero-science preflight using:
   primary runner, primary `src`, frozen overlay, detached clean cwd and
   complete-290 target view.
7. Require natural exit0, `optimized_matrix_preflight_passed`, fixed primary
   identity `46403a00...6c48`, unchanged existing-pair audits, and
   `solver_started=false`.
8. Verify detached `src`, detached runner, detached output, or detached
   benchmark identity remain rejected.
9. Review explicit canonical/input/runtime/control root schemas and require
   unambiguous source origins, containment, disjointness, no aliases,
   read-only complete inputs, and producer/audit pre/post equality.
10. Verify “clean detached project” is two-stage and non-contradictory:
    clean exact-`45face32` base before materialization, then exactly the
    reviewed seven-path modified/untracked status delta implied by the
    complete input view. Reject both an arbitrary dirty tree and an
    impossible post-materialization empty-status requirement.
11. Verify the historical witness source/input manifests
    `dfb23566...32a3` / `e2cbf431...f9d` are evidence-only. A fresh matrix
    root must derive new absolute path/dev/inode bindings and new full-file
    manifest hashes while independently retaining the exact three-field
    complete-290 contract `77927103...fd24`. Require exactly one
    canonical-primary-runner entry under
    `t4af_matrix_source_manifest_v1`, plus exactly 290 exhaustive read-only
    role/path entries under `t4ap_matrix_input_manifest_v2`; no
    discretionary source set or legacy non-exhaustive matrix-input schema is
    acceptable.
    Trace validator dataflow: benchmark/runtime/source checks must receive
    the canonical root; cwd/allowed-cwd/input-target checks must receive the
    detached root. Reject any surviving matrix call that substitutes
    undifferentiated `cwd` for both.
12. Verify primary T7 handoff is never written, swapped, linked, renamed,
    chmodded, copied over, or used as a matrix target.
13. Verify the unchanged runner writes directly through its original
    canonical output transaction; no pair copy/promotion layer is added.
14. Verify all positive/negative tests cover wrong cwd/root/commit/import/
    output/manifest/role/path/hash/mutability/link/alias/overlap/post-drift
    and producer/audit mismatches, including stale historical manifest
    absolute identities and unexpected Git-status entries.
15. Verify runner, imported scientific source, runtime bytes, complete-290
    meaning, fixed identity, legacy/golden/canonical data, thresholds,
    tolerances, frequency order, modes, points, resolution, and `lmax` do
    not change.
16. Verify T4 local self-remediation is limited to diagnosed
    pre-execution zero-science artifact-local control defects. Post-launch
    producer/audit failures remain one-shot and non-retryable.
17. Freshly verify witness checkpoint, canonical four, eight later-file
    absence, and zero related process/lock/tmp/partial/quarantine state.

## Decision

Return exactly one:

```text
REVIEW GREEN / T0 SPLIT-ROOT FIXED-PRIMARY MATRIX INPUT VIEW APPROVED
REVIEW YELLOW / T0 SPLIT-ROOT FIXED-PRIMARY MATRIX INPUT VIEW CHANGES REQUIRED
REVIEW RED / T0 SPLIT-ROOT FIXED-PRIMARY MATRIX INPUT VIEW UNSAFE
```

Bind the decision to the exact candidate and list independent evidence,
blockers, and prohibited actions. GREEN authorizes only root-T0 fresh
verification and possible later T4ap Phase A. It does not authorize driver
edits, matrix run/audit children, solver, canonical writes, T7ch, or
downstream work.
