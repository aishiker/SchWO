# Formal T7 prompt — V3.1-Y real-Wolfram compatibility terminal review

Review the single immutable compatibility root produced by the exact one-use
T0 dispatch.  Independently rebuild every predicate frame, event, ACK, stage
checkpoint, raw stream, lifecycle record, manifest and source/runtime
identity.  Require every frozen compatibility case exactly once, all positive
and adversarial expected outcomes, real target WolframKernel identity, static
`StringNormalize` diagnosis confirmed as a new compatibility observation,
explicit Boolean/Unicode/string/integer/RawJSON/frame/ACK/process semantics,
and all external API/radial/solver/boundary/overlap/science counters zero.
Require exactly 151 ordered predicates: 118 explicit fixture cases plus 33
common predicates, and exactly 82 child frames/ACKs, 151 prefixes, 18 finals,
17 opens, one seed and one exit authority. Rebuild the seven exact P08--P14
compatibility barriers and the revision-3 typed schemas/digest DAG. Rebuild
all four added ACK mutations and five stage-edge cases; every negative inner
reject must yield an outer expected-reject PASS without corrupting the live
FSM. Stage17 must be transitively bound to the child-exit and lifecycle
receipt.

No row may be missing, added, retried or supplied by a fake child.  A failure
root is immutable and cannot be retried under the same authority.

If closed, return exactly:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: NOT_ASSESSED
GATE_LABEL: ACCEPT GREEN / V3.1-Y REAL-WOLFRAM PROTOCOL COMPATIBILITY SUFFICIENT FOR ONE-USE SOURCE-LOAD MICRO DISPATCH
```

Write only
`docs/handoffs/archive/T7_2026-08-13_v3_1_y_wolfram_protocol_compatibility_review.md`.
This GREEN is non-scientific and authorizes only a different one-use micro
dispatch.  No sentinel/official/V3.2.
