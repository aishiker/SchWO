# Formal T7 prompt — V3.1-X source-load micro-sentinel terminal review

Use the existing formal T7 task `019f5ed1-b421-7ec2-9bac-8d134855a1ed`
with high reasoning effort.  This prompt activates only after formal T7 has
accepted the repair-cycle-2 implementation and Root T0 has consumed the single
frozen micro dispatch into one fresh immutable micro root.

Perform a read-only, archive-only review.  Independently rebuild the entire
micro root, manifest, dispatch consumption, implementation/review/source
identities, exact child lifecycle and RawJSON-to-Wolfram source ledger.  Do not
edit implementation/science/tests/status/current handoffs, launch Wolfram,
create a dispatch/root, or start a full sentinel, official run or V3.2.

The only acceptable micro success has all of these properties:

- exact operation `source_load_micro_sentinel` and exact
  `V3A-MODE-BHPT-RW-001/P1` request;
- one consumed one-use dispatch and one fresh root; no alternate authority;
- exactly one real WolframKernel process under the frozen executable, absolute
  argv, cwd and clean environment;
- canonical duplicate-free request, exact overlay/snapshot/Paclet/context and
  eight ordered `FindFile` records;
- expected, actual-start and actual-end ledgers independently normalized to
  `[context,path,sha256,size,mode,nlink]` and exactly equal;
- rc0, empty stderr, exact stdout/result protocol, wait/reap/process-group
  closure, immutable canonical manifest and no live writer;
- external API, `ReggeWheelerRadial`, solver, boundary-solution, overlap and
  scientific call counts all exactly zero;
- `scientific_evidence=false`, no promotion/reuse as full-sentinel or official
  evidence, and no source/protected identity drift.

Fake-child or synthetic roots can test implementation but cannot satisfy this
formal boundary.  Any missing/extra/torn/unknown artifact, source alias/drift,
nonzero science counter, child/lifecycle failure or authority mismatch is
terminal failure.  The micro dispatch/root is never retryable or reusable.

If and only if every criterion passes, output the exact machine envelope:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: NOT_ASSESSED
GATE_LABEL: ACCEPT GREEN / V3.1-X SOURCE-LOAD MICRO SENTINEL SUFFICIENT FOR ONE-USE FULL SENTINEL DISPATCH
```

That verdict authorizes Root T0 only to bind this archive's exact later digest
into the separately frozen attempt-0003 full-sentinel dispatch.  It does not
accept any numerical science.

For any terminal micro failure, output exactly:

```text
ADVANCE_DECISION: ESCALATE
CLAIM_STATUS: FAIL
GATE_LABEL: ESCALATE / T0 ADJUDICATION REQUIRED
```

There is no retry, repair cycle 3 or alternate micro.  Write only
`docs/handoffs/archive/T7_2026-08-13_v3_1_x_source_load_micro_sentinel_terminal_review.md`
and report its SHA-256, inventories, findings, counters and nonclaims.  No full
sentinel, official run, V3.2 or global GREEN is authorized by this prompt.

