# T4au — Read-Only Legacy Reference-View Repair

Work only in existing T4 task
`019f5fa6-1288-7c01-8a87-4c4370cf5517` using
`gpt-5.6-sol/high`. Never use `max` or `ultra`. Do not create a task,
subagent, proxy or descendant.

## Start Gate

Fully read project/status/current T0/T4/T7/T8 handoffs and the complete
T4ar–T4au design/plan/prompts and T7cu–T7cx decisions. Read the live driver,
old launcher, T4at Phase-A checkpoint and complete consumed Phase-B root.

Require the exact candidate supplied by root T0 and exact:

```text
REVIEW GREEN / T0 READ-ONLY LEGACY REFERENCE VIEW REPAIR APPROVED
```

Freshly verify all frozen identities and global guards before edits.

## Preserve The Consumed RED

Reconstruct and freeze:

- T4at Phase-B root, its 787 files/96 directories/529261027 bytes;
- setup/request/source/input/prelaunch/running/receipt/streams/failure audit
  and checkpoint;
- child `50124`, exit64/no-signal/exact-wait/reaped/PG-empty;
- exact writable-sidecar stderr and empty stdout;
- no pair, audit0, solver0, matrix0 and consumed authorization;
- three earlier prelaunch-only roots;
- primary legacy/golden full identities;
- canonical four, eight later absences and all external/process/transient
  guards.

Never mutate, chmod, unlock, rename, delete, populate, repair, reuse or
promote an old root or primary artifact.

## Phase A Only

1. Create one fresh unique no-overwrite evidence root and preserve exact old
   driver and launcher snapshots.
2. Publish a new reconstruction launcher in the new root; leave the old
   launcher byte-identical.
3. Add only one required `--legacy-reference-root` argument.
4. Derive the three exact fixed relative reference paths and pass them
   explicitly through validation, compute, comparison, run and audit.
5. Preserve `regular_file(..., read_only=True)` and validate all three
   reference files before any scientific call.
6. Preserve the exact single `_compute_frequency()` call and all scientific,
   warning, array, output and audit semantics.
7. Repair only the isolated pair permission lifecycle so both NPZ and JSON
   are exact mode0400 before atomic final publication and producer PASS;
   bind those exact identities into matching audit.
8. Extend only the artifact-local durable driver to bind the new launcher,
   reference root/manifest/source/targets and exact argv.
9. Keep all old phases and T4at clean-source/complete-input split unchanged.
10. Create one fresh reference view containing exactly three exclusive
   mode0400 byte copies at the frozen relative paths.
11. Verify target/target and source/target inode disjointness, exact
    hashes/sizes, source pre/post equality, exact inventory and deterministic
    manifest/index.
12. Add all positive/negative/static/dataflow tests from the design,
    including writable/pre-chmod/post-drift pair rejection.
13. Build and validate prospective producer and matching-audit requests
    under exact Python3.14 without launching either child.
14. Freeze launcher/driver/diffs/manifests/tests/requests and all guards.

Phase A must not execute launcher `main`, `_compute_frequency()`,
reconstruction producer/audit, optimized runner, official audit, solver or
matrix.

Diagnosed pre-execution zero-science artifact-local helper/fixture/manifest/
path/serialization/bookkeeping/ordinary-test defects must be preserved in
immutable failed roots, minimally corrected in fresh roots and continued
until checkpoint. Blind retry is forbidden.

Only complete PASS returns exact:

```text
CHECKPOINT / READ-ONLY LEGACY REFERENCE VIEW FROZEN
```

Report old/final driver and launcher hashes/diffs, exact reference view and
manifest/index, source/target stat/inode/hash records, all tests/rejections,
prospective producer/audit requests, runtime/source/input/legacy/golden/
warning/witness/canonical/absence/process/transient/external guards, and
science counts all zero. Then stop for root-T0 audit.

## Later Boundary

Only a separate later root-T0 authorization permits one new producer and,
after full producer PASS, one matching zero-solver audit. Any launched
failure consumes that authorization and forbids retry. The isolated pair
cannot be promoted or reused outside the separately reviewed Phase-C path.

No primary chmod/mutation, read-only relaxation, old-root repair, scientific
source/runtime/complete-input/fixed-identity/criterion/order change,
canonical write, matrix, new frequency, production/plot/fixture/Kirchhoff/
paper/GitHub/T7ch/downstream work is authorized.
