# Phase 6 V3.1 — formal T7 independent mode-greybody review

Date frozen by T0: 2026-08-11

Scientific stage: `Phase 6 / V3.1`

Artifact revision: `r1`

Review class: independent identity-bound scientific review

You are the existing formal **T7 independent review task** for SchWO. Work in
the shared repository
`/Volumes/JohnnyTforGR/ResearchWork/GW_Wave_Optics/SchWO`.

This prompt freezes the review criteria before V3.1 execution. Root T0 will
later supply the exact terminal candidate root and hashes. Do not review an
active/staging root and do not implement repairs or dispatch V3.2.

## 1. Frozen review basis

Read completely, in order:

1. `project.md`
2. the YAML current-state header and latest V3 entries in `status.md`
3. `docs/handoffs/T0_current.md`
4. `docs/handoffs/T4_current.md`
5. `docs/handoffs/T7_current.md`
6. `docs/handoffs/archive/T7_2026-08-11_v3_0_contract_review.md`
7. `docs/review_gate_liveness_protocol.md`
8. `docs/templates/t7_gate_verdict_template.md`
9. `docs/prompts/phase6_v3_master_prompt.md`
10. `docs/prompts/phase6_t4_v3_1_mode_greybody.md`
11. all eight accepted V3.0 authority files;
12. the terminal V3.1 candidate root, T4 checkpoint/handoff/archive and every
    implementation/runtime/source identity supplied by T0;
13. existing current V1/V2 authorities only to check provenance and protected
    boundaries, not to substitute for V3.1 evidence.

Freeze these review inputs:

- T4 V3.1 prompt SHA-256
  `7e377f64782b8311e111469f53eeb182190d30347ffe0d9853a41b96deeafab6`;
- V3.0 T7 archive SHA-256
  `b672c7f33d2f3cc891f455c6d8123ed846309a07244696da34236f8e254c0126`;
- V3.0 accepted authority hashes exactly as recorded in the T4 prompt;
- V3.0 domain SHA-256
  `803c02efeaa89d83222d8663de0c3f2da223bab285d46f331e476793a44c263b`;
- V3.0 threshold SHA-256
  `91fbe1a758bbb2bbf0e3ec3e67e89b400e900674feee727af8f1cff00732ac4a`;
- V3.0 external-anchor SHA-256
  `06580c6801a4f75104a2018e7873afbe4ec6c6ed900a11c0860ca23f15a44485`;
- D-union plan and seven protected radial source identities.

At review start and end, independently rehash all of them, the candidate root,
T4 science sources and the frozen runtime/contract. If evidence/identity is
missing, inconsistent, mutable, concurrently written or unreadable, use HOLD
only under the exact liveness protocol and state reason, owner, unblock
condition, minimum action and downstream allowance.

## 2. Bounded claim and exact acceptance scope

The only claim under review is:

> On the frozen V3.1 496-mode domain, SchWO independently solves odd/even
> Schwarzschild radial equations and establishes mode S matrices, direct
> horizon greybody probabilities, S-unitarity probabilities, flux closure,
> numerical ladders, parity probability/phase relations, selected independent
> arbitrary-precision agreement and selected external odd-sector BHPT
> agreement within every pre-frozen threshold.

Full-domain V3, total absorption, angular scattering, glory, Li figures,
finite-radius response and global GREEN are outside this claim.

The exact successful gate label is:

`ACCEPT GREEN / V3.1 MODE GREYBODY EVIDENCE READY FOR V3.2`

An accepted initial verdict requires:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: PASS
```

The common absolute phase may remain a convention-budget PARTIAL/nonclaim; it
does not lower the mode-probability claim. Missing or failed blocking Route A,
Route B, Route C, ladder, flux, parity or domain evidence cannot be excused as
full-domain incompleteness and cannot be averaged into PASS.

## 3. Independent inventory reconstruction

Rebuild the V3.1 inventory directly from the frozen domain config, not from
the candidate summary:

- exact 11 frequencies;
- every integer multipole through the frozen formula;
- 248 `(kM,ell)` pairs;
- 496 keys, exactly one independent odd and one independent even record per
  pair;
- deterministic frequency/ell/parity ordering;
- exact ordinary-transmitted, critical-turning, ordinary-reflected and
  evanescent/high-ell stratum labels.

Independently rebuild the AP anchor union and all membership labels from the
three frozen AP selectors, including the deterministic nearest-critical rule.
Independently rebuild the exact odd-only BHPT anchor inventory. Reject missing,
extra, duplicate, parity-derived-as-independent or out-of-domain records.

Confirm the V2 sparse 30-key set is diagnostic provenance only and did not
populate or reduce the V3.1 domain.

## 4. Raw physics reconstruction

For all 496 Route-A records, recompute from raw stored operands at independent
high precision where arithmetic conditioning requires it:

- `S=(-1)^(ell+1)A_out/A_in`;
- signed incident/outgoing/horizon currents and their signs;
- positive physical fluxes;
- normalized direct flux balance;
- `R=|S|^2`;
- direct `Gamma_flux=F_H/F_in`;
- independent `Gamma_S=1-|S|^2`;
- ordinary and log-domain route differences;
- physical-probability bound violations.

Verify that direct Gamma originates from a raw horizon amplitude/current and is
not copied from S. Inspect implementation call paths and injected tests; a
field-name distinction alone is insufficient.

For every parity pair, independently recompute probability equality and the
wrapped relative phase predicted by `V3-F05`. Confirm even modes are genuine
even-equation solves and not parity-derived data.

Reconstruct every `r_in`, outer/Jost and tolerance ladder comparison from raw
node records. Verify exact node inventories, deterministic baseline selection,
deduplication without lost comparisons and failure propagation.

For all AP anchors, verify 80/120/180-digit raw arbitrary-exponent records,
direct flux and S routes, precision/boundary ladders, odd/even independence and
the prohibition on importing/calling protected SchWO radial algorithms.

For all external anchors, verify fresh external BHPT ReggeWheeler runtime/source
identity, method, raw outputs and convention conversion. Confirm the route did
not fall back to internal SchWO code and did not claim independent even-sector
coverage.

## 5. Frozen threshold reconstruction

Independently evaluate all 16 V3.1 threshold entries from the frozen threshold
JSON against raw records. Do not trust stored booleans or only call the
candidate validator. Recompute:

- complex-S independent-route error;
- log-Gamma AP error;
- flux balance;
- ordinary/log Gamma route agreement;
- probability bounds;
- parity probability and phase;
- AP precision S/log-Gamma ladders;
- Route-A `r_in`, outer/Jost and tolerance S/log-Gamma ladders.

Check exact operators, units and conditioning domains, especially the
`1e-12`, `1e-10`, `1e-8` signal/domain boundaries. Underflowed zero,
nonfinite operands, clipping, fitted phases, omitted nodes or absent blocking
comparisons are failures. Verify that no threshold was changed after producer
start and no loose asymptotic/model allowance entered V3.1.

Report extrema with exact key/node provenance, not only aggregate maxima.

## 6. Certificates, budgets and provenance

Independently rebuild exactly five ordered certificates:

1. `V3_MODE_GREYBODY_NUMERICAL`
2. `V3_MODE_GREYBODY_FLUX_VS_S`
3. `V3_MODE_GREYBODY_EXTERNAL`
4. `V3_MODE_PARITY_PROBABILITY`
5. `V3_MODE_DOMAIN_COVERAGE`

Each candidate PASS requires complete applicable blocking evidence. Verify each
certificate binds exact domain, threshold IDs, evidence file hashes, route
independence, separate numerical/convention budgets, protected identities and
nonclaims. Missing budget components must be NOT_ASSESSED/FAIL, never zero.

Check route-independence statements:

- A/B share equations/potentials/conventions but not integration/matching;
- A/C share physics/conversion but not code/runtime;
- B/C share physics/conventions but not numerical code;
- external odd-only and AP independent-even scopes are not conflated.

The candidate must preserve:

```json
{
  "global_status": null,
  "global_green_permitted": false,
  "independent_review_state": "NOT_ASSESSED"
}
```

before this review. Reject any publisher-created claim upgrade, global status,
Li-figure gate, finite-radius observer claim or V3.2 result.

## 7. Artifact and execution integrity

Require a fresh UTC-`Z` `r1` root under
`runs/phase6/classic_scattering/`, with exact run contract, implementation/
runtime/source hashes, single-writer/exact-resume provenance, canonical
serialization, complete manifest and no overwrite.

Independently verify:

- root `0555`;
- every direct file regular `0444`/nlink1;
- no symlink, hardlink, staging residue, live writer or related process;
- every manifest identity by fresh hashing;
- in-place and distinct-temporary-copy reload;
- raw-record-to-summary/report/certificate deterministic rebuild;
- start/end accepted/protected identities;
- official root was produced after implementation freeze and not rewritten
  around scientific failures.

Review T4 tests and independently run focused validator/reconstruction tests,
all Phase-6/V3 tests, Ruff check/format, compileall and scoped diff-check. Before
acceptance ensure the full repository suite has a fresh passing result as
required by the V3 master prompt; it may be T4's identity-bound terminal run or
an independently repeated T7 run. Unit tests that exercise bounded solver code
are allowed, but do not run/restart the V3 producer or generate new science.

## 8. Finding and liveness governance

Use the frozen dual-axis protocol exactly:

- only `A. BLOCKING_CURRENT_GATE` permits `REPAIR`;
- every A finding contains `blocker_id`, violated item, exact evidence,
  expected/observed value, bounded repair, allowed files, recheck command and
  unblock condition;
- metadata/path/permission/wording/observer-only issues are
  `C. CONTROL_PLANE_REPAIR` when science bytes, formulas, thresholds, runtime,
  inputs and solver behavior do not change;
- full-domain V3, future V3.2–V3.5, Li agreement and common absolute phase are
  B/D limitations, not retrospective blockers;
- freeze `passed_items`, `failed_items`, `partial_allowed_items` and
  `not_assessed_items` after initial review;
- delta review checks only failed items, protected identities and passed
  invariants affected by a repair;
- obey the two bounded-repair-cycle limit and escalate after the second failed
  recheck.

## 9. Allowed writes and terminal response

The scientific review is read-only. You may write only:

- `docs/handoffs/T7_current.md`;
- one new dated `docs/handoffs/archive/T7_*v3_1*.md` record.

Do not edit candidate evidence, V3.0 authorities, prompts, configs, code, tests,
status, T0/T4 handoffs, immutable artifacts or protected files. Do not dispatch
T4/V3.2 or any repair task. Return only to Root T0.

Your terminal response must include:

```text
ADVANCE_DECISION: ADVANCE|REPAIR|ESCALATE
CLAIM_STATUS: PASS|PARTIAL|FAIL|NOT_ASSESSED
GATE_LABEL: <exact bounded label>
```

and the complete finding classes, incremental-review sets, reviewed root/file
hashes, inventory/route/threshold/certificate counts, extrema, test results,
protected identities, uncertainties and nonclaims.

An accepted GREEN means only that the frozen V3.1 mode-greybody evidence may
enter a separately frozen V3.2 task. It is not total absorption PASS,
full-domain V3, angular-scattering/glory PASS, Li equivalence, finite-radius
observer validation or global GREEN.
