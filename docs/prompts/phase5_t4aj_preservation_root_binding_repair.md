# T4aj — Pre-Edit Preservation Root-Binding Repair

Work only in existing T4 task
`019f5fa6-1288-7c01-8a87-4c4370cf5517`. Do not create or reuse a task,
subagent, proxy, or descendant.

## Reasoning Budget

Use `gpt-5.6-sol/high`. Do not use `max` or `ultra`.

## Start Gate

Fully read:

```text
docs/superpowers/specs/
  2026-07-27-t4aj-preservation-root-binding-repair-design.md
docs/superpowers/plans/
  2026-07-27-t4aj-preservation-root-binding-repair.md
docs/prompts/
  phase5_t7cm_preservation_root_binding_review.md
```

Also read project/status/current handoffs, the complete frozen T4ai package,
T7cl GREEN, both failed helper roots, T4ah v10 evidence, and the complete
consumed T4ah witness.

Require the exact T4aj candidate identity supplied by root T0 and exact:

```text
REVIEW GREEN / T0 PRE-EDIT PRESERVATION ROOT-BINDING REPAIR APPROVED
```

Freshly verify candidate/ref/parent/exact-four, T4ai/primary identities,
both immutable failed roots, unchanged driver, complete witness ledger and
critical map, v10/runtime/complete-290, canonical hash/stat/inodes, external
object, four absent units, and zero unexpected process/transient/link/
bytecode state. Stop on mismatch.

## Phase A0 — Helper Freeze Only

Create one fresh unique no-overwrite evidence root below the exact control
root. This turn may publish only:

- one final helper source;
- atomic/fsynced audit-only request, stdout, stderr, exit, argv/cwd/env, and
  pre/post inventory records.

The helper must:

1. accept explicit absolute `--project-root`, `--control-root`, and
   `--evidence-root`;
2. verify `__file__`, direct parent containment, exact cwd, exact
   `git rev-parse --show-toplevel`, candidate/ref/HEAD, and every path
   containment rule;
3. contain no `Path.parents`, numeric parent ascent, authoritative
   repeated-parent logic, cwd-relative discovery, directory-name search, or
   root string stripping;
4. expose `--audit-only` and
   `--execute --expected-audit-sha256`;
5. call one shared pure `collect_before()` in both modes;
6. bind both failed roots, unchanged driver, T4ai package, the complete
   2,566-file/582,625,016-byte witness ledger
   `c21828703400ae65b52d516fe7d41e52f2d7255de1f3a6bdd22f5c3cdb13bb97`,
   all eight exact critical filename/hash mappings, v10/runtime/
   complete-290, canonical/absence/external/process/transient/link/
   bytecode guards;
7. use runner-compatible canonical JSON with default
   `ensure_ascii=True`;
8. write nothing in audit-only mode and emit one canonical object to
   stdout with empty stderr and exit zero.

Before running audit-only, perform an AST/static review that rejects root
arithmetic, eval/exec, shell execution, deletion/unlink, wildcard cleanup,
outside mutation, network/install/global mutation, bytecode, or forbidden
science surfaces.

Prove with full pre/post inventories that audit-only made no change. Do not
create `preserved_sources`, a driver snapshot, `before_manifest.json`, or
any preflight artifact. Do not edit the driver. Do not invoke scientific
runner CLI, official audit, solver, witness, or matrix.

On complete PASS freeze the helper and captures, then return exactly:

```text
CHECKPOINT / T4AJ PRESERVATION HELPER FROZEN
```

Report helper SHA/stat/inode, exact argv/cwd/environment, audit-only
stdout/payload hashes, empty stderr, exit zero, pre/post inventories, both
failed-root inventories, unchanged driver, complete witness ledger,
canonical/absence/external/process/transient guards, and scientific counts
`0/0/0`. Then pause.

## Phase A1 — Separate Root-T0 Authorization Required

Do not execute the helper unless root T0 later sends exact:

```text
AUTHORIZED / EXECUTE T4AJ PRESERVATION AND RESUME T4AI PHASE A
```

That authorization will bind the frozen helper, exact execute argv/cwd/env,
and expected audit payload SHA-256.

After authorization, run execute mode once only. It must recompute the same
payload, match the expected digest, exclusively publish the exact old-driver
snapshot and atomic/fsynced before manifest, verify bytes/hash/stat/inode/
nlink/non-alias/permissions, and leave every outside guard unchanged.
Failure is immutable and stops without another attempt.

Only preservation PASS permits resuming the already reviewed T4ai Phase A:
artifact-local durable-driver/solver-free-test changes and one fresh full
exact-Python-3.14 zero-science preflight. On that later complete PASS return
exact:

```text
CHECKPOINT / RELOCATED BENCHMARK IDENTITY CONTRACT FROZEN
```

## Stop Conditions

Stop on any package, evidence, path, identity, helper, write-scope,
process, transient, bytecode, canonical, test, or scientific anomaly.

No witness, official scientific audit, solver, matrix, pair reuse,
canonical write, runner/science/runtime/complete-290 edit, threshold/
tolerance/mode/point/lmax change, cleanup, destructive action, external
mutation, network/install/global mutation, new frequency, production/plot/
fixture/Kirchhoff/paper/GitHub work, or new agent is authorized.
