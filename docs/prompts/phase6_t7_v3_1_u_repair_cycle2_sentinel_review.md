# Formal T7 prompt — V3.1-U repair-cycle-2 Route-C sentinel review

Run only after Root T0 supplies the one terminal immutable sentinel root and
exact manifest identity.  Review read-only in formal T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed`.

Read/re-hash the repair-2 package and reviews, implementation evidence, one-use
sentinel dispatch/consumption, stable snapshot authority, exact root and every
artifact.  Do not run new science, retry an anchor, create an official
dispatch/root, mutate evidence, or start V3.2.

Independently verify:

1. dispatch was consumed exactly once before one Wolfram child launch;
2. request is the frozen ordered 23 anchors and exact MST/90/45/45 method;
3. each ordinal/key has exactly one fresh external call and exactly one PASS
   record, with no error, retry, drop, duplicate, reorder or selection;
4. child exit is 0, stderr is empty, stdout/payload/receipt identities close,
   and the child is reaped with no active writer/process;
5. WLS/kernel/audit/snapshot/25-file and loaded-source start/end identities are
   exact and equal;
6. the terminal root is canonical, immutable, complete and independently
   rebuilds from its manifest;
7. sentinel records are explicitly non-reusable and no V3.1-U threshold or
   certificate is claimed by this root;
8. all frozen protected/domain/threshold/convention identities remain exact.

If and only if all checks pass, return exactly:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: PASS
GATE_LABEL: ACCEPT GREEN / V3.1-U ROUTE-C SENTINEL SUFFICIENT FOR ONE-USE OFFICIAL DISPATCH
```

Persist at
`docs/handoffs/archive/T7_2026-08-13_v3_1_u_repair_cycle2_sentinel_review.md`
with exact identities, recomputed records/process/provenance checks, and
nonclaims.  This bounded PASS authorizes only Root T0 to publish one separate
official dispatch.  Full V3.1-U remains `NOT_ASSESSED` by the sentinel.

If the sentinel is not an exact PASS, return:

```text
ADVANCE_DECISION: ESCALATE
CLAIM_STATUS: FAIL
GATE_LABEL: ESCALATE / T0 ADJUDICATION REQUIRED
```

with a complete class-A finding.  The sentinel cannot be retried and there is
no repair cycle 3.  No V3.2 or global GREEN.
