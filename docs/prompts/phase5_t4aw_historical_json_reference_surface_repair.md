# T4aw — Historical-JSON Reference-Surface Repair

Work only in existing T4 task
`019f5fa6-1288-7c01-8a87-4c4370cf5517` using
`gpt-5.6-sol/high`. Never use `max` or `ultra`. Do not create a task,
subagent, proxy or descendant.

## Start Gate

Fully read project/status/current T0/T4/T7/T8 handoffs and complete
T4ar–T4aw design/plan/prompts and T7cu–T7cz decisions. Read the live driver,
T4av launcher/checkpoint/preflight and both complete T4av Phase-B roots.

Require the exact candidate supplied by root T0 and exact:

```text
REVIEW GREEN / T0 HISTORICAL JSON REFERENCE SURFACE REPAIR APPROVED
```

Freshly verify all frozen identities and guards before edits.

## Preserve The Consumed One-Shot

Reconstruct and freeze:

- the failed pre-child setup root;
- the consumed v2 root with exact 823 files/117 directories/531851545
  bytes/final index `1c1f88da...74d1`;
- every setup/request/source/input/reference/prelaunch/running/receipt/
  stream/supervisor-failure/checkpoint identity;
- PID 2090 exit0/no signal/exact wait/reap/PG-empty;
- the isolated pair as present mode0400 but permanently non-reusable;
- matching audit0 and consumed authorization;
- the `audit-failure` return2/output-collision record without calling it a
  successful failure audit;
- every preceding failed/consumed/Phase-A root and lock;
- canonical four, eight later absences and all external/process/transient
  guards.

Never mutate, chmod, unlock, rename, delete, populate, repair, reuse, audit,
scientifically load or promote the consumed pair or old evidence.

## Independently Prove The Defect

Read `_read_json()`, `_reconstruction_pair_snapshot()`,
`_post_scientific_audit_snapshot()` and the exact source/target historical
sidecar. Prove the canonical-control reader was applied to a strict valid,
finite, duplicate-free, pretty-printed historical object whose raw
SHA/stat/content identity is exact.

Do not reinterpret any unaccepted array/warning value as PASS.

## Phase A Only

1. Create one fresh unique no-overwrite Phase-A evidence root and preserve
   the exact old driver/launcher/checkpoint/preflight/consumed roots.
2. Leave generic canonical-control `_read_json()` byte-identical.
3. Leave canonical produced-reconstruction-witness reading byte-identical.
4. Add one dedicated immutable-historical-legacy-sidecar reader.
5. Bind it only to the exact validated legacy-sidecar path/role/hash/size/
   stat/mode/link/inode/reference-index identity.
6. Reject invalid UTF-8/JSON, duplicate keys, `NaN`/`Infinity` and
   non-object top levels.
7. Do not require raw historical bytes to equal any reserialization.
8. Do not rewrite, normalize, chmod, replace or publish historical bytes.
9. Change only the literal legacy-sidecar call site inside
   `_reconstruction_pair_snapshot()`.
10. Add every positive/negative/static/dataflow fixture from the design.
11. Prove canonical/historical/produced-witness reader cross-use and
    arbitrary/fallback/argument/environment-controlled selection fail
    closed.
12. Preserve the reconstruction launcher and every scientific/runtime/
    input/reference/warning/canonical/process criterion unchanged.
13. Build and validate completely fresh prospective producer and matching
    audit requests without launching either child.
14. Freeze driver/diff/readers/call sites/tests/requests and all old/new
    guards.

Phase A must execute zero launcher main, `_compute_frequency()`,
reconstruction producer/audit, optimized runner, official audit, solver or
matrix.

Diagnosed pre-execution zero-science artifact-local helper/fixture/manifest/
path/serialization/bookkeeping/ordinary-test defects must be preserved in
immutable failed roots, minimally corrected in fresh roots and continued
until checkpoint. There is no fixed correction count. Blind retry is
forbidden.

If any change to scientific source, reconstruction launcher, runtime,
complete-input meaning, fixed identity, legacy/reference content,
canonical artifact, scientific criterion or matrix order would be needed,
return fail-closed to root T0.

Only complete PASS returns exact:

```text
CHECKPOINT / HISTORICAL JSON REFERENCE SURFACE FROZEN
```

Report old/final driver hash and diff, exact reader functions and literal
call sites, all tests/rejections, prospective producer/audit requests,
complete consumed evidence, runtime/source/input/reference/legacy/golden/
warning/witness/canonical/absence/external/process/transient/link guards,
and science counts all zero. Then stop for root-T0 audit.

## Later Boundary

Only a separate later root-T0 authorization permits one completely new
producer and, after full producer PASS, one matching zero-solver audit. Any
launched failure consumes that authorization and forbids retry. No old
result, request, root, reference view or pair may be reused.

No scientific source/launcher/runtime/dependency/complete-input/reference
content/fixed-identity/criterion/order change, canonical write, Phase C,
optimized matrix, new frequency, production/plot/fixture/Kirchhoff/paper/
GitHub/T7ch/downstream work is authorized.
