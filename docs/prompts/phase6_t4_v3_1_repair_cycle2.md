# Phase 6 V3.1 — T4 final bounded repair cycle 2 implementation

Date frozen by T0: 2026-08-11

Scientific stage: `Phase 6 / V3.1`

Artifact revision: `r3` / `artifact_rev=3`

You are the existing formal T4 implementation/numerics task in
`/Volumes/JohnnyTforGR/ResearchWork/GW_Wave_Optics/SchWO`.

This prompt is inactive until Root T0 supplies a formal T7 package verdict
with exact label
`ACCEPT GREEN / V3.1 REPAIR CYCLE 2 PACKAGE READY FOR T4`, bound to the
unchanged package manifest. Do not infer approval from this file alone.

## Mandatory start gate

Read completely:

1. `project.md` and the status YAML/current V3.1 entries;
2. T0, T4 and T7 current handoffs;
3. the liveness protocol and verdict template;
4. the V3 master prompt and eight accepted V3.0 authorities;
5. the initial T7 failure, cycle-1 package approval, T4 cycle-1 blocker and T7
   delta-review-1 archives;
6. the exact cycle-2 design and package manifest;
7. current V1/V2 manifests, D-union plan, seven protected radial files and
   external Wolfram/BHPT identities;
8. existing Phase-6 pole-safe/AP/BHPT code only as audited implementation
   references.

At start, rehash every package/upstream/protected identity and require the
formal T7 archive to bind the same package SHA. Any drift is a pre-science
HOLD. Do not repair or replace accepted inputs.

## Required implementation

Implement `docs/phase6_v3_1_repair_cycle2_design.md` exactly:

- Route A uses one protected public scaled-tortoise call per logical node and
  a V3-local, deterministic numerically continued Jost adapter when `n_aux>0`;
- the original requested radius remains the final matching radius;
- Route B independently implements AP RW/Zerilli plus AP Jost continuation;
- Route C executes 23 fresh external odd BHPT MST anchors through the exact
  external-SSD WolframKernel;
- full production and validation paths cover exact
  `496/9920/102/458/23`, all 16 thresholds and exactly five certificates;
- compact JSONL and complete source start/end ledgers are mandatory.

Do not modify protected/frozen files, thresholds, selectors, formulas,
conventions or consumed roots. Do not add fallback, clipping, fitted values,
parity-derived independent even, cached-node substitution or post-hoc
auxiliary selection.

You may iteratively repair implementation defects before official science
only in the exact V3-local allowed files and only while the reviewed algorithm
is unchanged. A needed algorithm/package/protected/threshold change stops the
task and returns exact evidence to T0. This is the final bounded repair cycle.

## Pre-execution and official gates

Complete every gate in design section 8: full static/injected coverage,
threshold and fail-closed tests, exact first-key 20-node sentinel, fixed
stratum/AP/external sentinels, direct-versus-shadow continuation check,
synthetic full-route publish/reload, focused/all-V3 tests, Ruff, compileall,
diff-check, resource projection, and immediate prelaunch identity rehash.

No official root may exist until all gates pass. Non-authoritative outputs
remain under `/tmp` with false science flags.

Then create once, with no overwrite:

```text
runs/phase6/classic_scattering/v3_1_mode_greybody_r3_<UTC-Z>_py314
```

Use `/opt/homebrew/bin/python3.14`, project-local dependencies, the exact
external kernel, one coordinator writer and exact checkpoint/resume rules.
After science begins, a scientific/threshold/nonfinite/provenance/validator
failure is terminal and may not be retried. Only a clean system interruption
with an exact contiguous checkpoint may resume.

On terminal success, independently rebuild all records, extrema, budgets,
summary and certificates; seal and reload in place and from a distinct copy;
run the full repository suite; rehash protected inputs; update T4 current, one
new T4 archive and status; return:

```text
CHECKPOINT / V3.1 MODE GREYBODY EVIDENCE FROZEN
```

Include exact root/file hashes, counts, extrema, runtimes, tests, source map,
uncertainty budgets and nonclaims. Do not dispatch T7 or start V3.2.

On terminal failure, seal truthful evidence if any official root exists,
update T4/status and return to T0. Never overwrite/retry/promote it and never
claim global GREEN.
