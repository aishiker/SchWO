# T7cj — Hermetic Runtime-Provenance Repair Package Review

Work only in existing T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed` as an independent read-only package
reviewer. Do not create a task, subagent, or descendant.

## Required Reading And Identity

Fully read:

- `project.md`, `status.md`, current T0/T4/T7/T8 handoffs;
- frozen T4ae and T4af design/plan/prompts;
- the exact T4af RED witness request, detached manifests, durable records,
  streams, and root-T0 failure audit;
- the frozen driver and successful/failed preflight evidence;
- the committed runner import graph and required `schwgw` modules;
- the exact four-file T4ag candidate supplied by T0.

Verify the supplied candidate commit, parent, ref, exact four added paths,
commit/worktree blobs, sizes, and SHA-256 values. Review only that identity.

## Independent Review

Check that:

1. T4af RED and its consumed authorization remain immutable and are not
   reinterpreted as a package or science success.
2. The established diagnosis is correct: isolated HOME hides user-site
   SciPy/PyYAML while accepted NumPy remains visible.
3. The exact runner import graph needs SciPy and PyYAML and no omitted
   required third-party runtime package is hidden.
4. Copying exact installed SciPy 1.17.1 and PyYAML 6.0.3 into a read-only
   control-root overlay is technically feasible, including compiled
   extensions and relative Mach-O dependencies.
5. The package forbids network/global installs and mutable user-site
   fallback.
6. Builder copy, source-drift, no-link, no-bytecode, fsync, atomic publish,
   content-addressing, failure, and no-overwrite rules are complete.
7. The driver binding proves exact overlay bytes and module origins before
   and after producer and matching audit without weakening existing durable
   supervision or scientific contracts.
8. NumPy's accepted Homebrew origin is sufficiently bound without copying
   its symlink farm.
9. The import-only and full Phase-A tests invoke neither the scientific
   runner CLI nor a solver.
10. The new witness is a separately reviewed prospective attempt under a new
    runtime identity, not an implicit retry under the consumed T4af
    authorization.
11. Canonical, legacy, golden, implementation, runner, config, input,
    threshold, frequency, matrix-order, downstream, and non-claim guards are
    unchanged.
12. The existing single-proxy/no-new-subagent policy is retained.

Independently inspect current package sources, processes, canonical files,
remaining absent units, and transients. Any mutable dependency path, missing
import, unbounded copy, alias, implicit retry, weakened criterion, or
incomplete failure behavior is a blocker.

## No Writes Or Dispatch

Do not edit status, handoffs, candidate files, implementation, driver,
artifacts, packages, or environment. Do not build the overlay, run a
preflight/witness/matrix, dispatch T4/T7ch/T8, or perform GitHub actions.

## Exact Decision

Return only to root T0 with exactly one:

```text
REVIEW GREEN / T0 RUNTIME REPAIR PACKAGE APPROVED
REVIEW YELLOW / T0 RUNTIME REPAIR PACKAGE CHANGES REQUIRED
REVIEW RED / T0 RUNTIME REPAIR PACKAGE INVALID
```

Then report candidate identity, independent evidence, and bounded blockers
if any.
