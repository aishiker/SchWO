# Formal T7 prompt — V3.1-X implementation/sentinel/science reviews

Use only in formal T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed`, and only after Root T0 identifies the
exact substage and immutable candidate identities. Apply the frozen V3.1-X
package and `docs/review_gate_liveness_protocol.md` incrementally.

## Substage A — zero-science implementation review

Review only the exact six implementation paths and T4 temporary evidence.
Rebuild domain/node/overlay graphs, inspect AST/source call restrictions,
rerun zero-science tests/adversaries, prove zero Wolfram/solver/dispatch/root
creation, rehash all package/protected/source identities and ensure no passed
package invariant changed.

If ready, return:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: NOT_ASSESSED
GATE_LABEL: ACCEPT GREEN / V3.1-X IMPLEMENTATION READY FOR ONE-USE SENTINEL DISPATCH
```

## Substage B — immutable sentinel review

Only after a one-use T0 sentinel dispatch and terminal immutable root, rebuild
the root from raw records. Require exact `35` calls, `70` In/Up solutions,
`105` overlaps, all 23 selected keys plus six extra nodes for ordinals 3 and
22, zero retry/fallback/reuse/MST/internal solver, exact overlay/source/runtime
receipts, every mandatory sentinel gate PASS, complete budgets, immutable
publication and a resource projection satisfying the frozen caps.

If scientifically sufficient, return:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: PASS
GATE_LABEL: ACCEPT GREEN / V3.1-X SENTINEL SUFFICIENT FOR ONE-USE OFFICIAL DISPATCH
```

Sentinel failure is an initial science failure of the distinct gate and must
use a complete class-A finding; sentinel data are never reusable officially.

## Substage C — immutable official scientific review

Only after a different one-use T0 official dispatch and terminal root,
independently rebuild exact fresh Route A/U/B/C science and require:

- `496/9920`, `318/954`, `102/458`, and Route-C `23/161/322/483` exact;
- all 16 frozen thresholds evaluated without weakening and all five
  certificates present;
- all 23 direct-route keys and every node complete, no fallback/retry/cache or
  predecessor/sentinel science reuse;
- separate numerical and convention budgets;
- exact source/protected/runtime/dispatch/publication identities;
- explicit odd-only external and full-domain nonclaims.

If the bounded V3.1 claim closes, return:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: PASS
GATE_LABEL: ACCEPT GREEN / V3.1-X BOUNDED V3.1 SUFFICIENT FOR V3.2 ENTRY
```

Only this identity-bound official `ADVANCE` permits Root T0 to freeze/start
V3.2. Package/implementation/sentinel GREEN never does. No full-domain V3,
Li-figure equivalence, finite-radius observer claim or global GREEN.

For each substage, write a distinct UTC archive under
`docs/handoffs/archive/`, update `docs/handoffs/T7_current.md`, report exact
hashes, dual-axis verdict, item inventories, findings and repair-cycle state.
