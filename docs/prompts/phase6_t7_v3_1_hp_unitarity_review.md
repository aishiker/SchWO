# Formal T7 prompt — V3.1-U scientific review

You are the existing formal SchWO T7 task.  Run this prompt only after Root T0
supplies one terminal immutable V3.1-U root and its exact manifest identity.
This is the initial scientific review of the distinct gate
`phase6_v3_1_hp_unitarity_deficit_replacement_v1`; it is not delta review 3 of
the exhausted predecessor gate.

Review read-only.  Do not edit files, alter artifacts, run new science solves,
send work to another task or start V3.2.

Read `project.md`, the current-state header, T0/T4/T7 handoffs, the liveness
protocol/template, the frozen V3.0 authorities, final predecessor ESCALATE
archive, the exact replacement package/design/T4 prompt, T4 terminal archive,
and every file in the supplied root.

Independently verify at minimum:

1. the gate is a distinct T0-adjudicated replacement, not repair cycle 3;
2. V3.0 domain/formulas/conventions, 16 threshold bytes, five certificate IDs
   and seven protected source hashes are unchanged;
3. the root is fresh and contains no result/cache/resume dependency on r1/r3;
4. exact Route-A `496/9920`, Route-B `102/458`, Route-C `23`, threshold `16`
   and certificate `5` inventories rebuild in frozen order;
5. the 496-entry route map was frozen before any Route-U call and each decision
   is exactly direct Route-A `log_Gamma_flux < log(1e-8)` without access to S,
   Gamma_S, mismatch/residual, threshold status, AP/external output or regime;
6. Route-U has exactly `N_U` routed keys and `3*N_U` fresh ordered nodes; its
   precision schedule, guard margin and admission gates rebuild exactly;
7. Route U independently solves odd RW/even Zerilli, imports no Route-A or
   protected numerical code, does not expand float64 S, infer Gamma_S from
   flux, or reuse Route-B records;
8. from original Route-U decimal strings, independently recompute complex S,
   `log_abs_S`, `Gamma_S=-expm1(2 log_abs_S)`, all precision changes and every
   small-Gamma log-route threshold operand;
9. large-Gamma modes retain the original absolute float64 branch and every
   mode belongs to exactly one threshold branch;
10. all remaining flux, ladder, parity, AP and external threshold extrema,
    separate numerical/convention budgets and all five certificates rebuild;
11. start/end source maps, manifests, permissions, nlink, runtime identities,
    tests and absence of writers/transients are exact;
12. all non-claims remain explicit: common absolute phase PARTIAL, no
    full-domain V3, Li equivalence, finite-radius observer result or global
    GREEN.

Use the mandatory dual-axis finding schema.  Freeze passed/failed/
partial-allowed/not-assessed items.  Only a complete class-A finding may block
advance; new broader advice is follow-up debt.

If and only if the entire frozen V3.1-U gate passes, return exactly:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: PASS
GATE_LABEL: ACCEPT GREEN / V3.1-U MODE GREYBODY SUFFICIENT FOR BOUNDED V3.2 ENTRY
```

This bounded GREEN authorizes T0 to design/dispatch V3.2 only.  It is not
full-domain or global GREEN.  Otherwise return the governance-compliant
`REPAIR` or `ESCALATE` verdict with complete findings and no downstream action.

Persist a durable T7 archive and update only `docs/handoffs/T7_current.md`, then
return exact hashes to T0.
