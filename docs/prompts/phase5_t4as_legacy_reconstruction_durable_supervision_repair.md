# T4as — Legacy Reconstruction Durable-Supervision Repair

Work only in existing T4 task
`019f5fa6-1288-7c01-8a87-4c4370cf5517` using
`gpt-5.6-sol/high`. Never use `max` or `ultra`. Do not create a task,
subagent, proxy, or descendant.

## Start Gate

Fully read project/status/current T0/T4/T7/T8 handoffs, the complete
T4ar/T4as designs/plans/review prompts, exact T7cu/T7cv decisions, T4ar
Phase-A checkpoint, immutable prelaunch HOLD, both consumed `2.91875`
attempts, current driver and launchers, unchanged runner, exact legacy commit
and artifacts, runtime overlay, complete-290 manifests, relocated witness,
canonical output, and absence/process/transient state.

Require the exact candidate supplied by root T0 and exact:

```text
REVIEW GREEN / T0 LEGACY RECONSTRUCTION DURABLE SUPERVISION REPAIR APPROVED
```

Earlier review is not execution authority.

## Preserve The Boundary

Freshly verify:

- HOLD root
  `t4ar_phase_b_prelaunch_hold_ee2b2960_20260727T204500p0800`;
- manifest `241584cc...ec20`;
- helper `0b57be2b...479e`;
- root/helper/manifest modes `0500/0400/0400`;
- exact two-file root and canonical manifest bytes;
- authorization unconsumed but not reusable under a new driver;
- child/producer/audit/solver/matrix `0/0/0/0/0`;
- detached/output pair absent;
- driver `ee2b2960...7ed5`;
- reconstruction launcher `20bd7e78...79c6`;
- T4ar checkpoint `2c6be479...2f26`;
- runner/runtime/complete-input/fixed-identity/witness/legacy/golden;
- canonical four exact identities and eight later-file absences;
- no related process, lock, tmp, partial, quarantine, link, or alias.

Never mutate, chmod, rename, delete, populate, reuse, repair, or reinterpret
the HOLD or any older root.

## Phase A Only

1. Create fresh unique no-overwrite durable evidence and preserve the old
   driver plus all before guards.
2. Add exact phases:

   ```text
   t4as_legacy_raw_warning_reconstruction_producer
   t4as_legacy_raw_warning_reconstruction_audit
   ```

3. Preserve all six old phase values and semantics.
4. Add a distinct reconstruction command/binding/terminal path. Do not route
   reconstruction through optimized logical-runner, matrix-adapter, artifact,
   start/terminal, or official-audit schemas.
5. Require producer markers `scientific_runner=false`,
   `scientific_solver=true`, `reconstruction_launcher=true`, `mode=run`;
   require audit markers `false/false/true/audit`.
6. Bind exact launcher/argv/cwd/roots/commit/runtime/complete inputs/
   legacy/golden/Phase-A checkpoint/warning record/isolated pair/canonical
   guards across request, prelaunch, running, receipt, streams, final, control
   audit, failure audit, and replay.
7. Parse exact one-line reconstruction run/audit JSON and independently reload
   the pair. Preserve all T4ar 22-array and warning gates.
8. Require producer durable PASS before constructing one matching audit and
   require full producer/audit provenance equality.
9. Extend token/ancestry process guards for the exact reconstruction launcher;
   reject duplicates and observer-prose false positives.
10. Add every positive/negative/static/dataflow case frozen in the design and
    prove old accepted requests unchanged.
11. Run exact Python 3.14 AST/dataflow/Ruff/diff/targeted tests and one full
    zero-science synthetic preflight.
12. Build one fresh prospective exact-legacy detached/input/empty-output
    geometry. Validate complete producer and matching-audit request templates
    without launching either child. Freeze all source/target/root/stat/hash/
    inode/mode/manifest/runtime identities.
13. Prove launcher/optimized-runner/reconstruction-audit/official-audit/solver
    invocation counts are all zero, canonical unchanged, eight later outputs
    absent, and no residual process/transient.
14. Freeze the new driver and all manifests/captures/tests.

The exact reconstruction launcher should remain byte-identical. If a newly
demonstrated pre-child artifact-local launcher-control defect makes the exact
command impossible, minimally self-remediate only its CLI/provenance/atomic
control in a fresh root while preserving its exact single
`_compute_frequency()` call, warning capture, imports, scientific arguments,
constants, comparison, and dataflow. Report any such change explicitly.

Only complete PASS returns:

```text
CHECKPOINT / LEGACY RECONSTRUCTION DURABLE SUPERVISION FROZEN
```

Report old/new driver diff and identity, phase/command/binding/dataflow,
launcher identity, all tests and rejected cases, synthetic manifest/captures,
prospective request/audit templates and exact roots, complete inputs/runtime/
legacy/golden/witness/canonical/absence/process/transient guards, and exact
science counts `0/0/0/0/0`.

Then stop for root-T0 audit. Phase A does not authorize launcher execution,
reconstruction producer, reconstruction audit, optimized runner, official
matrix audit, solver, matrix unit, or canonical write.

## After Separate Authorization

Only a new exact root-T0:

```text
AUTHORIZED / LEGACY 2P91875 RAW-WARNING RECONSTRUCTION WITNESS
```

permits one fresh reconstruction producer. Never reuse the Phase-A prospective
roots or the old prelaunch authorization.

The producer must use fresh roots, call unchanged exact-legacy
`_compute_frequency()` exactly once, capture all warnings, and publish one
isolated atomic pair. Only full durable and scientific PASS may start one
matching zero-solver audit. Any launched producer or audit failure consumes
the authorization and forbids retry.

Complete PASS returns:

```text
CHECKPOINT / LEGACY RAW-WARNING WITNESS PASSED
```

and pauses for root-T0 audit.

Only later exact:

```text
AUTHORIZED / BIND LEGACY RAW-WARNING WITNESS TO MATRIX ADAPTER
```

permits the existing zero-science T4ar Phase C. Matrix science remains
separately gated after Phase C audit.

## Local Self-Remediation

Within Phase A, do not return HOLD merely for a diagnosed pre-execution
zero-science artifact-local driver/helper/fixture/manifest/path/serialization/
bookkeeping/ordinary-test defect. Preserve failed roots, correct the exact
diagnosis in fresh roots, and continue to the checkpoint without blind retry
or a fixed correction limit.

Return to root T0 only if correction would change runner/scientific source/
runtime dependency/complete-input meaning/exact legacy computation/fixed
identity/canonical/warning rule/scientific criteria/order or require a
destructive/global action.

## Not Authorized

No old-root mutation, prospective-root reuse, warning suppression/filter/count
adjustment, missing-as-zero, legacy/golden/canonical rewrite, output copy or
promotion, runner/source/runtime/input/identity/criteria/order change,
producer/audit retry, alternate/parallel/duplicate execution, new frequency,
task/subagent/proxy/descendant, max/ultra, destructive/global/network/install,
production/plot/scientific-fixture/Kirchhoff/paper/GitHub/T7ch/downstream work.
