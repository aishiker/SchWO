# T7cc — Literal Failed-Child Radial Gate Independent Review

Work only in existing T7 task `019f5ed1-b421-7ec2-9bac-8d134855a1ed` as a
read-only reviewer. Do not create a task, repair implementation/artifacts, or
start T8ar. Normal review uses `gpt-5.6-sol/high`.

## Start Gate And Required Reading

Fully read `project.md`, `status.md`, current T0/T4/T7/T8 handoffs, the exact
reviewed candidate design/plans/prompts, T4ac gate note and artifacts, T7cb
evidence, and immutable T8aq/T4ab sources. Verify the candidate identity and
require T4ac exact `GREEN / LITERAL FAILED-CHILD RADIAL GATE READY`.

Before replacing T7 handoff, archive its exact bytes at:

```text
docs/handoffs/archive/T7_2026-07-18_pre_t7cc_literal_failed_child_radial_review.md
```

The pre-review bytes must have SHA-256
`57cae192228d9d29b1cc39c82cc8c7bd3844811e3a0b35faaa1dbdc2c799bd01`.

## Allowed Writes

Only:

```text
status.md
docs/handoffs/T7_current.md
docs/handoffs/archive/T7_2026-07-18_pre_t7cc_literal_failed_child_radial_review.md
```

## Independent Checks

1. Re-derive the literal 41-frequency set from all 153 T7cb hierarchy
   failures and confirm both phase failures are included and no passing child
   interval is added.
2. Recompute every token/lmax window and exact 146,416 classification count.
3. Require exactly 41 complete/PASS checkpoints and canonical contract/output
   hashes, no temp/quarantine, and exact manifest membership.
4. Independently reconstruct classification membership, all 41 sector-aware
   transition sets, direct-oracle keys/values/residuals, anchor coverage,
   sensitivity, and preflight.
5. Verify the pre-adapter classification snapshot, final-adapter snapshot,
   immutable input hashes, parent radial blob, exact five commit blobs, and
   snapshot bridge.
6. Expand the literal envelope without importing its helper; require exact
   key identity, both sectors, all points/tokens/ells/boundaries, no IO loading,
   no inferred membership, and strict fail-closed behavior.
7. Require default-covered zero oracle calls and integrated-adapter anchor
   identity with direct saved values.
8. Run focused tests, Ruff on the exact five paths, fresh full pytest,
   `git diff --check`, exact scope/source checks, known-warning
   classification, and empty forbidden-output searches.

## Exact Decision

Return exactly one:

```text
ACCEPT GREEN / LITERAL FAILED-CHILD RADIAL GATE ACCEPTED
ACCEPT YELLOW / LITERAL FAILED-CHILD RADIAL EVIDENCE INCOMPLETE
REJECT RED / LITERAL FAILED-CHILD RADIAL GATE INVALID
```

GREEN requires every artifact, oracle, snapshot, adapter, test, scope, and
provenance check. Report exact counts/hashes/maxima and changed review-only
paths. Return only to T0. Never start T8ar or any downstream work.

## Forbidden

No implementation/artifact/source/config change, threshold relaxation, lmax
extension, automatic/recursive midpoint, uniform/full grid, amplification,
production, plot, fixture, Kirchhoff, paper-style work, task creation, or
GitHub action.
