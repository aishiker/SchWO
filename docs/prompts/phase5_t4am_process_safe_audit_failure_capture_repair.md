# T4am — Process-Safe Audit And Failure-Capture Repair

Work only in existing T4 task
`019f5fa6-1288-7c01-8a87-4c4370cf5517`. Do not create or reuse a task,
subagent, proxy, or descendant.

## Reasoning Budget

Use `gpt-5.6-sol/high`. Do not use `max` or `ultra`.

## Start Gate

Fully read:

```text
docs/superpowers/specs/
  2026-07-27-t4am-process-safe-audit-and-failure-capture-repair-design.md
docs/superpowers/plans/
  2026-07-27-t4am-process-safe-audit-and-failure-capture-repair.md
docs/prompts/
  phase5_t7cp_process_safe_audit_failure_capture_review.md
```

Also fully read project/status/current T0/T4/T7/T8 handoffs, frozen T4ai
through T4al packages/reviews, every immutable failed-helper/audit root,
v10/runtime/complete-290/runner evidence, complete consumed witness,
canonical/absence/external/process/transient state.

Require the exact T4am candidate identity supplied by root T0 and exact:

```text
REVIEW GREEN / T0 PROCESS-SAFE AUDIT AND FAILURE-CAPTURE REPAIR APPROVED
```

Freshly verify candidate/ref/parent/exact-four, primary/exact-fourteen,
all failed roots, unchanged driver, complete witness ledger and mappings,
v10/runtime/complete-290/runner, canonical hash/stat/inodes, external
object, absent units, and zero unexpected process/transient/link/bytecode
state. Stop on mismatch.

## Frozen T4al Epistemic Boundary

Independently verify exact T4al helper/audit/execution roots and counts.
State only:

- one audit-only child exited `1`;
- no-write pre/post equality and execution-root absence passed;
- audit root is empty and no success/failure record was published;
- exact stderr and exact dynamic cause are unrecoverable;
- source proves a reachable naive-substring process-scanner defect;
- that defect is not established as the exact historical cause.

Never execute, populate, chmod, rename, delete, repair, or reuse any old
helper/audit root.

## Phase A0 — Fresh Process-Safe Helper Only

1. Choose fresh unique pairwise-distinct helper, audit-record, and reserved
   execution paths, direct children of the exact control root.
2. Create helper and audit-record roots only. Execution root and aliases
   remain absent.
3. Freeze one helper and one launcher using exclusive/no-follow atomic
   publication, file fsync, reload, hash/stat/inode/nlink verification, and
   parent fsync.
4. Helper and launcher contain project/control/helper/audit/execution as
   direct full exact `ast.Constant` strings. Launcher additionally freezes
   exact helper path, Python executable, and helper SHA.
5. Static AST/dataflow review rejects implicit root construction,
   `Path.parents`, cwd/name inference, eval/exec, shell evaluation,
   destructive surfaces, scientific surfaces, and variable process IDs in
   canonical payload.
6. Close helper file/root `0400/0500`; freeze launcher `0400`. Before first
   child the audit root contains exactly the launcher.
7. Invoke only the frozen launcher file under exact Python3.14:
   `python launcher.py --first-attempt ...`. Stdin, heredoc, `-c`, and
   dynamic launcher text are forbidden.
8. Helper `--audit-only` and future `--execute` share one pure
   `collect_before()`. This turn authorizes audit-only once and forbids
   execute.
9. Preserve the exact stable audit-root projection and runner-compatible
   canonical JSON with default `ensure_ascii=True` and no newline.
10. Pass exact current launcher PID only as validation input. It must never
    appear in the canonical payload/digest.
11. Parse one structured `pid/ppid/pgid/sid/state/command` snapshot. Require
    unique self and direct launcher relationship. Exempt only exact self and
    direct launcher from relevant-process rejection.
12. Check every other row by normalized executable/script/argument tokens.
    Reject unrelated scientific/control runner paths, forbidden script
    tokens, aliases, malformed/ambiguous rows, duplicate/missing IDs, wrong
    parent, and ancestry cycles.
13. Run the complete zero-science positive/negative process-classifier
    suite before the real audit attempt. It must cover expected ancestry,
    launcher marker tolerance, unrelated/sibling runner rejection,
    basename-token rejection, harmless prose, wrong/missing/duplicate IDs,
    malformed rows, and cycles. Real science invocation remains zero.
14. Collect complete prelaunch inventories, run exactly one first-attempt
    audit-only child, capture raw stdout/stderr/return/signal/wait/reap, and
    collect complete post inventories before publication.
15. If no-write equality, frozen root identities, or execution absence
    fails, publish nothing and HOLD because safe attribution is lost.
16. If equality passes, publish exact common attempt records regardless of
    child success or failure, including raw zero-or-nonzero stream bytes,
    terminal status, argv/cwd/env, pre/post inventories, and guards.
17. On exit0/no-signal/empty-stderr/exact canonical stdout/all guards PASS,
    publish `records_index.json` and
    `phase_a0_final_manifest.json`.
18. On any child or semantic failure, publish
    `failure_records_index.json` and
    `phase_a0_failure_manifest.json`, close evidence, return HOLD, and stop.
19. The selected terminal manifest binds branch/index/exact filename
    set/publication/fsync/reload/modes but never its own hash/stat or
    post-close timestamp. Report those independently after close.
20. Reverify helper/launcher unchanged, execution absent, failed roots and
    all frozen guards exact, no bytecode/cache/transient/link anomaly, and
    scientific runner CLI/official audit/solver counts `0/0/0`.

On complete success return exactly:

```text
CHECKPOINT / T4AM PROCESS-SAFE AUDIT HELPER FROZEN
```

Report all three roots, helper/launcher identities, AST/dataflow review,
classifier test identities/results, exact argv/cwd/env, stdout/payload/
record/index/terminal hashes, empty stderr, exit0, pre/post equality,
helper/launcher immutability, execution absence, every frozen guard, and
zero science counts. Then pause.

Any failure consumes authorization. Do not create a second helper, launcher,
root triple, or audit attempt.

## Later Replay And Execute Require Separate Root-T0 Authorization

Do not run `--replay-only` or helper `--execute` in Phase A0. Root T0 will
independently read/AST/dataflow-audit all success evidence and invoke the
frozen launcher once in no-write `--replay-only` mode.

Only byte-identical replay PASS can support a later digest-bound one-shot
execute authorization. Preservation PASS may then resume only the already
reviewed T4ai artifact-local driver/tests and one fresh exact-Python-3.14
zero-science preflight.

## Stop Conditions

Stop on any package, root, identity, process, ancestry, tokenization,
failure-capture, publication, replay, write-scope, transient, bytecode,
canonical, test, evidence, or scientific anomaly.

No retry, cleanup, destructive action, outside mutation, driver edit or
preflight in this turn, scientific runner, official audit, solver, witness,
matrix, pair reuse, canonical write, runner/science/runtime/complete-290
edit, threshold/tolerance/mode/point/lmax change, network/install/global
mutation, new frequency, production, plot, fixture, Kirchhoff, paper,
GitHub work, or new agent is authorized.
