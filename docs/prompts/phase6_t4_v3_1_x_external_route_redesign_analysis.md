# Formal T4 prompt — V3.1-X external-route redesign analysis

Date frozen by Root T0: 2026-08-13

Scientific stage: `Phase 6 / V3.1-X design analysis`

Gate candidate: `phase6_v3_1_x_external_direct_route_redesign_v1`

This is a **zero-science, read-only algorithm-design task** after the formal
T7 terminal verdict

```text
ADVANCE_DECISION: ESCALATE
CLAIM_STATUS: FAIL
GATE_LABEL: ESCALATE / T0 ADJUDICATION REQUIRED
```

for the exhausted V3.1-U repair-cycle-2 Route-C sentinel.  It is not repair
cycle 3, a retry of the sentinel, an official run, or V3.2.

Work only in the existing formal T4 task
`019f5fa6-1288-7c01-8a87-4c4370cf5517`.

## 1. Required authorities

Read completely and re-hash:

1. `project.md`;
2. the current-state header and latest V3 entries of `status.md`;
3. `docs/handoffs/T0_current.md`, `T4_current.md`, and `T7_current.md`;
4. `docs/review_gate_liveness_protocol.md`;
5. `docs/prompts/phase6_v3_master_prompt.md`;
6. all accepted V3.0 contract/domain/threshold/formula/convention authorities;
7. `configs/phase6_v3_0_external_anchor_matrix.json`, especially
   `V3A-MODE-BHPT-RW-001`;
8. the complete immutable failed sentinel root
   `runs/phase6/classic_scattering/v3_1_u_route_c_sentinel_repair2_v1_20260812T221323Z_py314`;
9. formal T7 terminal review
   `docs/handoffs/archive/T7_2026-08-13_v3_1_u_repair_cycle2_sentinel_review.md`,
   SHA-256
   `e51f32f1f7d633ff78a653d9ab2a0a49d5c1ef0d32c57f84cb9fda63abc80b55`;
10. the stable BHPT snapshot and authority under
    `runs/phase6/external_sources/bhpt_reggewheeler_2e012092_v1_20260813`;
11. `scripts/phase6_v3_1_bhpt_mst_cycle2.wls` and the exact loaded BHPT
    source graph;
12. the existing bounded direct-integration implementation, tests,
    documentation, and immutable evidence rooted at
    `runs/phase6/radial_validation/v1_external_bhpt_direct_bounded_selected_v1_20260810_py314`.

The failed sentinel, its dispatch, all predecessor V3.1/V3.1-U roots, and
their numerical records are evidence only.  They may not supply accepted
science or be resumed/copied into a future candidate.

## 2. Frozen T0 adjudication

Root T0 selects liveness option 2: **redesign the external algorithm/gate**.

- Do not narrow the 23-key domain to the 14 observed successes.  Such a
  result-conditioned subset is forbidden.
- Preserve the exact V3.0 domain, formulas, conventions, 16 thresholds, five
  certificate IDs, protected radial identities, and odd-only external claim.
- Do not change the radial backend.
- Do not reinterpret the T7 failure as a resource or network failure.
- V3.2 remains blocked because the master prompt explicitly requires V3.1 T7
  `ADVANCE`.

The candidate redesign should replace the failed MST-only blocking route with
a predeclared, fresh Black Hole Perturbation Toolkit
`ReggeWheelerRadial[..., Method -> {"NumericalIntegration", ...}]` route for
**all 23 frozen odd anchors**, using independent In/Up solves and controlled
overlap matching.  Any future exact-input MST calculation is diagnostic only
unless a later T0 package and formal T7 review explicitly assign it a blocking
role.  There may be no per-key fallback, favorable method selection, or reuse
of the 14 prior MST successes.

This paragraph is a design target, not execution authority.  T4 must determine
whether it can be made scientifically and operationally sufficient without
weakening any frozen acceptance item.

## 3. Required read-only analysis

Using source inspection and already immutable evidence only, report:

1. a precise failure taxonomy for all nine sentinel errors, separating the
   raw BHPT `MST_FAILED` outcomes from the wrapper-side amplitude parser error;
2. whether decimal-frequency construction in the failed WLS preserved the
   requested arbitrary precision for `0.1` and `0.5`, with exact source-line
   evidence and no new Wolfram evaluation;
3. whether the remaining `kM=2, ell=18` failure has the same demonstrated
   cause or must remain `cause_not_proven`;
4. the exact 23-key ordered inventory rebuilt independently from V3.0;
5. the direct-NumericalIntegration equations, boundary conditions, potential,
   source files, normalization, amplitude extraction, overlap matching, and
   conversion to V3-F02;
6. independence accounting relative to Route A, Route U, Route B/AP, and the
   failed MST route;
7. whether the unmodified stable BHPT snapshot is sufficient for odd RW, or
   whether an overlay is needed; if an overlay is proposed, enumerate every
   changed byte/file and explain why that does not convert the route into a
   SchWO-internal solver;
8. a predeclared precision/boundary/match-radius ladder suitable for all 23
   keys, including separate numerical and convention budgets;
9. a no-selection acceptance rule: all 23 keys, every mandatory ladder node,
   exact fresh call counts, error propagation, and no method fallback;
10. a bounded sentinel plan that is not itself post-hoc, plus one-use
    dispatch, immutable publication, failure terminalization, provenance, and
    source start/end closure;
11. conservative runtime/storage estimates from existing immutable timing
    evidence, with assumptions and upper bounds;
12. the exact future implementation/test file scope and a proposed package
    graph for later Root-T0 freeze and formal T7 package review.

Explicitly distinguish established evidence from inference.  Do not claim
that direct integration will pass before it is executed under a separately
reviewed package.

## 4. Hard prohibitions

Do not:

- launch WolframKernel, wolframscript, BHPT, AP, Route A/U/B/C, or any solver;
- create a dispatch, sentinel root, official root, staging root, or numerical
  output;
- edit source code, tests, configs, V3.0 authorities, thresholds,
  conventions, protected files, or immutable roots;
- retry any failed anchor or inspect a newly generated numerical value;
- start V3.2 or claim bounded/global GREEN;
- dispatch T7.  Root T0 owns package freeze and review dispatch.

## 5. Allowed writes and completion

Write only:

- `docs/phase6_v3_1_x_external_route_redesign_analysis.md`;
- `docs/handoffs/archive/T4_2026-08-13_v3_1_x_external_route_redesign_analysis.md`;
- prepend the corresponding current snapshot to `docs/handoffs/T4_current.md`.

Run documentation/static checks and re-hash every authority at start/end.
Return exact output hashes and end with:

```text
CHECKPOINT / V3.1-X EXTERNAL DIRECT-ROUTE REDESIGN ANALYSIS READY FOR T0 PACKAGE FREEZE
```

This checkpoint is analysis readiness only.  V3.1/V3.1-U remains `FAIL`, the
new gate remains `NOT_ASSESSED`, and no implementation, numerical execution,
formal T7 acceptance, V3.2, or global GREEN is authorized.
