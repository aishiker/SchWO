# T4ai — Relocated Benchmark-Identity Binding Repair

Work only in existing T4 task
`019f5fa6-1288-7c01-8a87-4c4370cf5517`. Do not create or reuse a task,
subagent, proxy, or descendant.

## Reasoning Budget

Use `gpt-5.6-sol/high`. Do not use `max` or `ultra`.

## Start Gate

Fully read:

```text
docs/superpowers/specs/
  2026-07-27-t4ai-relocated-benchmark-identity-binding-repair-design.md
docs/superpowers/plans/
  2026-07-27-t4ai-relocated-benchmark-identity-binding-repair.md
```

Also read project/status/current handoffs, frozen T4ae–T4ah packages, the
complete T4ah Phase-A evidence, and the complete consumed T4ah witness.

Require the exact T4ai candidate identity supplied by root T0 and exact:

```text
REVIEW GREEN / T0 RELOCATED BENCHMARK IDENTITY REPAIR APPROVED
```

Verify candidate/ref/parent/exact-four, primary/exact-fourteen, runner,
driver/preflight/runtime/complete-290 contract, consumed witness, isolated
pair, canonical hash/stat/inodes, external object, remaining absent units,
and zero unexpected process/transient state. Stop on mismatch.

## Phase A — Identity Contract Freeze

1. Preserve the old driver and complete T4ah HOLD evidence immutably under
   fresh unique no-overwrite/fsynced control evidence.
2. Independently reconstruct the primary and consumed-detached gate
   manifests, gate components, seven-field benchmark payloads, and final
   benchmark identities using the runner's exact `_json_safe` plus
   `json.dumps(..., sort_keys=True, separators=(",", ":"),
   allow_nan=False)` canonical JSON, with the default
   `ensure_ascii=True`, followed by UTF-8 encoding.
3. Prove the payload difference is only `gate_sha256`, caused only by the
   five absolute gate paths while their repository paths, hashes, and sizes
   remain exact.
4. Modify only the artifact-local durable driver and solver-free tests.
5. Retain the fixed primary identity for matrix/canonical context.
6. Before any detached witness launch, deterministically derive and publish
   a complete exact benchmark-identity binding for that one bound detached
   root.
7. Require producer start/terminal, embedded metadata, sidecar, durable
   records, producer audit, and matching official audit to equal that exact
   binding.
8. Require producer and audit to use the same detached root/runtime.
9. Reject arbitrary observed identity, wrong root/path/content/field/phase,
   gate or Table-I drift, producer/audit mismatch, alias/link/writeability,
   and post-prelaunch mutation.
10. Preserve all runtime, process, fault, source, complete-290, scientific
    audit, matrix, canonical, and scientific semantics.
11. Run one fresh full exact-Python-3.14 zero-science preflight with every
    prior test/capture/rejection plus all new identity-binding cases.  This
    must include a non-ASCII detached-root positive whose canonical
    bytes/digest equal the frozen runner and a paired negative proving that
    `ensure_ascii=False` differs and is rejected; do not restrict detached
    roots to ASCII.

Scientific runner CLI, official audit, and solver invocation counts must all
remain exactly zero. Do not provision or launch a real witness in Phase A.
Any failed preflight remains immutable and stops without retry.

On complete PASS return exactly:

```text
CHECKPOINT / RELOCATED BENCHMARK IDENTITY CONTRACT FROZEN
```

Include old/new driver hashes and exact diff; reconstructed primary and
detached identities and payload comparison; preflight manifest/raw/capture/
test/rejection/runtime identities; zero-science counts; T4ah HOLD/pair,
complete-290, canonical/absence/process/transient guards. Then pause.

## Phase B — New Exact Authorization Required

Proceed only after root T0 sends the separately reviewed exact authorization
for a new full one-shot witness.

Provision one fresh detached clean exact-`45face32` project and complete 290
read-only inputs under a fresh isolated root. Derive and freeze that root's
exact benchmark identity before launch. Use the frozen hermetic runtime and
newly frozen driver.

Run exactly one existing `kM=1.58125` producer. Only exit0, empty stderr,
exact wait/reap, PG-empty, runtime pre/post PASS, atomic isolated pair,
source/input/canonical guards, and exact identity binding permit the
matching official audit through the same driver and same detached root.

Any failure consumes authorization. Do not reuse the T4ah pair, retry,
duplicate, relaunch, recompute canonical matching work, or start matrix.

On complete PASS return:

```text
CHECKPOINT / CONTROL-PROVENANCE WITNESS PASSED
```

and pause for root T0.

## Conditional Matrix

Only exact:

```text
AUTHORIZED / ORIGINAL T4AE MATRIX CONTINUATION
```

permits:

```text
2.91875 -> 3.759375 -> 3.89375 -> full 241x241
```

one unit at a time. Matrix context must require the fixed primary benchmark
identity, not a detached identity.

## Stop Conditions

Stop on any identity, path, source, dependency, process, stderr,
scientific, canonical, test, or evidence anomaly.

No cleanup, destructive action, global install, network access, criterion
relaxation, frozen-file edit, new frequency, production/plot/fixture/
Kirchhoff/paper/GitHub work, or new agent is authorized.
