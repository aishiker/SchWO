# T7cr — Terminal Filename-Set Closure Repair Review

Work only in existing T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed` using
`gpt-5.6-sol/high`. Do not use `max` or `ultra`. Do not create a task,
subagent, proxy, or descendant.

## Scope

Perform one strict read-only review of the exact T4ao candidate supplied by
root T0. Read project/status/current T0/T4/T7/T8 handoffs, the T4ao
design/plan/prompts, T4an package/review, immutable T4an helper/audit roots,
and all frozen driver/v10/runtime/complete-input/witness/canonical evidence.

Do not modify files, refs, artifacts, status, handoffs, roots, permissions,
environment, or processes. Do not execute helper, launcher, replay,
preflight, scientific runner, official audit, solver, witness, or matrix.

## Required Independent Checks

1. Verify candidate/ref/parent/primary, exact-four added scope,
   commit/worktree byte equality, exact blobs/SHA/sizes, and
   `git diff --check`.
2. Rebuild the exact T4an 15-file audit inventory and direct-child
   identities. Verify helper/launcher hashes and root modes.
3. Independently verify child exit0/no signal/empty stderr/canonical stdout,
   exact wait/reap/PG-empty, pre/post equality, execution absence, and
   science0/0/0.
4. Prove the terminal manifest expects 14 names, actual has 15, and the
   only extra name is `t4an_audit_launcher.py`.
5. Confirm the embedded success decision is invalid, no failure branch
   exists, root remains open, attempt is consumed, and old roots cannot be
   repaired/replayed/reused.
6. Review the T4ao terminal model:
   `baseline + common + branch index + terminal manifest`.
7. Verify launcher baseline identity is separately bound by index/manifest,
   appears exactly once in both branch final sets, and is never treated as
   a newly published record.
8. Verify pre-manifest and final exact-set checks detect missing, extra,
   duplicate, cross-branch, and self-reference errors.
9. Verify terminal manifest excludes own hash/stat/post-close timestamp and
   records index excludes itself/terminal manifest.
10. Verify all success/failure closure fixtures run before child launch and
    use the exact live closure functions.
11. Verify T4an dual serializers, v10 schema/index/discriminator,
    ancestry/token classifier, immutable launcher, raw failure capture,
    complete pre/post guards, and zero-science counts are unchanged.
12. Freshly verify driver, runner, v10, runtime, complete290, witness,
    canonical four, later-unit absence, external object, process/link/
    transient state.
13. Verify Phase A0 is one-shot, artifact-local, and cannot authorize
    replay, execute, driver work, science, matrix, pair reuse, standard
    relaxation, agents, max/ultra, or downstream work.

## Decision

Return exactly one:

```text
REVIEW GREEN / T0 TERMINAL FILENAME-SET CLOSURE REPAIR APPROVED
REVIEW YELLOW / T0 TERMINAL FILENAME-SET CLOSURE REPAIR CHANGES REQUIRED
REVIEW RED / T0 TERMINAL FILENAME-SET CLOSURE REPAIR UNSAFE
```

Bind the decision to the exact candidate and list independent evidence,
blockers, and prohibited actions. GREEN authorizes only root-T0 fresh
verification and possible later T4ao Phase-A0 dispatch. It does not itself
authorize T4, replay, execute, science, witness, matrix, or downstream work.
