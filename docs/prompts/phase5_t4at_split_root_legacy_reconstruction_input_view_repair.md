# T4at — Split-Root Legacy Reconstruction Input-View Repair

Work only in existing T4 task
`019f5fa6-1288-7c01-8a87-4c4370cf5517` using
`gpt-5.6-sol/high`. Never use `max` or `ultra`. Do not create a task,
subagent, proxy, or descendant.

## Start Gate

Fully read project/status/current T0/T4/T7/T8 handoffs, complete
T4ar/T4as/T4at design/plan/review prompts, T7cu/T7cv/T7cw decisions, T4ar
and T4as checkpoints, the consumed T4as producer failure, current driver,
unchanged reconstruction launcher, runtime overlay, complete290,
legacy/golden/warning evidence, relocated witness, canonical output, locks,
external guards, and absence/process/transient state.

Require the exact candidate supplied by root T0 and exact:

```text
REVIEW GREEN / T0 SPLIT-ROOT LEGACY RECONSTRUCTION INPUT VIEW APPROVED
```

Earlier review or the old authorization is not execution authority.

## Preserve The RED

Freshly reconstruct and freeze:

- T4as attempt root and 787-file ledger;
- setup/request/prelaunch/running/receipt/streams/supervisor failure/failure
  audit/index/checkpoint hashes;
- child `37999`, exit64/no-signal/exact-wait/reaped/PG-empty;
- exact empty stdout and exact 169-byte clean-source stderr;
- source/input manifests sharing one root;
- exact ten expected input-target Git-status records;
- no pair, no audit, no runner, no matrix;
- consumed authorization and immutable locks;
- canonical four and eight later absences.

Never mutate, unlock, chmod, rename, delete, populate, reuse, or repair the
failed root or any older root.

## Phase A Only

1. Create fresh no-overwrite durable evidence and snapshot driver
   `883375d9...318ef`, T4as checkpoint `45459dbf...f63c`, unchanged
   launcher `20bd7e78...79c6`, and all guards.
2. Modify only the artifact-local durable driver and solver-free
   tests/helpers.
3. Preserve every old phase and both T4as reconstruction phase values.
4. Keep the reconstruction launcher byte-identical.
5. Treat reconstruction `detached_root` as one exact-clean legacy source
   worktree at `8fb8608c...6ada`.
6. Introduce one separate complete input root and require request
   `cwd == allowed_cwd_root == input_root`.
7. Bind runtime project root and `PYTHONPATH` to clean source root/src, not
   cwd/input root.
8. Require source/input/output/HOME/producer-run/audit-run roots distinct,
   direct-child, non-aliased and disjoint.
9. Require exact empty source Git status and exact tracked-source inventory.
10. Require all 290 input targets below input root and exact contract
    `77927103...fd24`; require no target below source root.
11. Route every post-child source check through the bound source root rather
    than audit cwd.
12. Require producer and audit to share exact source/input roots, manifests,
    runtime, cwd, pair and durable provenance.
13. Add every positive/negative/static/dataflow test frozen in the design.
14. Under exact Python 3.14, create one fresh clean source root plus separate
    complete input root and run one import-only/input-identity probe with
    cwd=input root and source imports from source root/src.
15. Build and validate prospective producer and audit requests without
    launching either child.
16. Freeze the driver, diff, manifests, captures, tests and all guards.

The import-only probe must not call the reconstruction launcher,
`_compute_frequency()`, optimized runner, official audit, or solver.

Diagnosed pre-execution zero-science artifact-local helper/fixture/manifest/
path/serialization/bookkeeping/ordinary-test defects must be preserved in
immutable failed roots, minimally corrected in fresh no-overwrite roots, and
continued until the checkpoint. There is no fixed correction count; blind
retry is forbidden.

Only complete PASS returns:

```text
CHECKPOINT / LEGACY RECONSTRUCTION SPLIT-ROOT INPUT VIEW FROZEN
```

Report exact old/new driver diff and SHA, unchanged launcher identity,
source/input/root/runtime manifests, import-only module origins and input
records, all test/rejection results, prospective requests, complete290,
legacy/golden/warning/witness/canonical/absence/process/transient/external
guards, and exact science counts all zero.

Then stop for root-T0 audit. Phase A authorizes no reconstruction producer,
matching audit, optimized runner, official audit, solver, matrix, Phase C,
or canonical write.

## After Separate Root Authorization

Only a later new exact:

```text
AUTHORIZED / LEGACY 2P91875 RAW-WARNING RECONSTRUCTION WITNESS
```

permits one fresh producer under the split-root contract. Never reuse the
consumed T4as or Phase-A prospective roots.

The producer may call the unchanged exact-legacy `_compute_frequency()`
exactly once. Only full durable/scientific/pair PASS may start one matching
zero-solver audit. Any launched producer or audit failure consumes the new
authorization and forbids retry.

Complete PASS returns:

```text
CHECKPOINT / LEGACY RAW-WARNING WITNESS PASSED
```

and stops for root-T0 audit. The separately gated zero-science T4ar Phase C
must still pass before any new matrix `2.91875` authorization.

## Not Authorized

No old-root/lock mutation, clean-source relaxation, input materialization
under source root, launcher/scientific-call/warning/array change,
runner/source/runtime/complete-input/fixed-identity/legacy/golden/canonical/
criteria/order change, output promotion, retry, duplicate/parallel/alternate
execution, new frequency, production/plot/scientific-fixture/Kirchhoff/
paper/GitHub, destructive/global/network/install action, task/subagent/proxy/
descendant, `max`, or `ultra`.
