# T4ah — Recursive Input-Package Closure Repair

Work only in existing T4 task
`019f5fa6-1288-7c01-8a87-4c4370cf5517`. Do not create or reuse a task,
subagent, proxy, or descendant.

## Reasoning Budget

Use `gpt-5.6-sol/high` for this code-writing turn. Do not use `ultra`.
Do not self-upgrade to `max`; pause for root T0 only if a genuinely complex
code/science/safety/provenance interaction cannot be resolved at `high`.
Mechanical inventory, manifest, hash/path, formatting, and ordinary test
work remain at `high`.

## Start Gate

Fully read:

```text
docs/superpowers/specs/
  2026-07-26-t4ah-recursive-input-package-closure-repair-design.md
docs/superpowers/plans/
  2026-07-26-t4ah-recursive-input-package-closure-repair.md
```

Also read project/status/current handoffs, frozen T4ae/T4af/T4ag packages,
the complete T4ag Phase-A runtime evidence, and the complete failed T4ag
witness.

Require the exact T4ah candidate identity supplied by root T0 and exact:

```text
REVIEW GREEN / T0 RECURSIVE INPUT PACKAGE REPAIR APPROVED
```

Verify candidate/ref/parent/exact-four, primary/exact-fourteen, runner,
benchmark/environment/Python, T4ag driver/preflight/overlay, failed witness,
six package roots, canonical hash/stat/inodes, remaining absent units, and
zero unexpected process/transient state. Stop on mismatch.

## Phase A — Input Contract Freeze

1. Preserve the T4ag driver, preflight, overlay, and RED witness immutably in
   fresh unique no-overwrite/fsynced evidence.
2. Recompute all six active package inventories and exact indexes.
3. Modify only the artifact-local durable driver and solver-free tests.
4. Preserve all 48 existing semantic input records.
5. Add exactly 242 deterministic package-member records so the complete
   contract contains 290 distinct entries.
6. Require exact package membership index
   `9f0a743db65793ff4aa28ea9f0890985914a0c3b4bc9a96622a5fefdea28b0d1`
   and contract index
   `77927103da4d853e98dbe7c4d19a2198fb544349cf735bf63faaf02ca31dfd24`.
   Implement the exact per-package, combined-content, membership, and
   final-contract UTF-8 canonical-JSON schemas frozen in the design, with
   Python Unicode code-point path ordering only. Reject locale-aware,
   filesystem-order, shell-sort, or Node `localeCompare` derivations.
7. Reject every missing/extra/quarantined/wrong-hash/wrong-role/wrong-path/
   wrong-count/source-drift/link/alias/special/transient/duplicate/writable
   target or index mismatch.
8. Retain all existing runtime, process, fault, source, audit, matrix,
   canonical, and scientific semantics.
9. Run one fresh full exact-Python-3.14 zero-science preflight with all prior
   tests/captures/rejections plus the new recursive-package cases.

Scientific runner CLI, official audit, and solver invocation counts must all
remain exactly zero. Do not provision or launch a real witness in Phase A.

Any failed preflight remains immutable and stops without retry.

On complete PASS return exactly:

```text
CHECKPOINT / RECURSIVE INPUT PACKAGE CONTRACT FROZEN
```

Include old/new driver hashes and exact diff; all six inventory counts,
bytes, indexes, and hazards; complete 290-entry contract/index; preflight
manifest/raw/capture/test/rejection/runtime identities; zero-science counts;
T4ag RED/overlay/canonical/absence/process guards. Then pause for root T0.

## Phase B — New Exact Authorization Required

Proceed only after root T0 sends exactly:

```text
AUTHORIZED / COMPLETE-INPUT CONTROL-PROVENANCE WITNESS COMPUTE
```

Provision one fresh detached clean exact-45face source snapshot and copy all
290 exact input targets read-only under their repository-relative paths.
Use the frozen hermetic runtime and newly frozen driver.

Run exactly one existing `kM=1.58125` producer. Only exit0, empty stderr,
exact reaping, PG-empty, runtime pre/post PASS, atomic isolated pair, source/
input guards, and unchanged canonical guards permit the matching official
audit through the same durable driver.

Apply all frozen scientific equivalence and metadata checks. Any failure
consumes the authorization. Do not retry, duplicate, relaunch, recompute the
canonical matching case, or start matrix work.

On complete PASS return:

```text
CHECKPOINT / CONTROL-PROVENANCE WITNESS PASSED
```

and pause for root T0.

## Conditional Matrix

Only exact:

```text
AUTHORIZED / ORIGINAL T4AE MATRIX CONTINUATION
```

permits:

```text
2.91875 -> 3.759375 -> 3.89375 -> full 241x241
```

one unit at a time with durable producer, matching official audit, atomic
pair, and canonical guards.

## Stop Conditions

Stop on any package, source, dependency, driver, process, stderr,
scientific, identity, path, canonical, test, or evidence anomaly.

No cleanup, destructive action, global install, network access,
criterion relaxation, frozen-file edit, new frequency, production/plot/
fixture/Kirchhoff/paper/GitHub work, or new agent is authorized.

Only final T4 exact GREEN plus root-T0 full T4ae PASS permits frozen T7ch.
