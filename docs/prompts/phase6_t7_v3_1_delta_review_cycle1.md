# Phase 6 V3.1 — formal T7 bounded delta review after repair cycle 1

Date frozen by T0: 2026-08-11

Scientific stage: `Phase 6 / V3.1`

Expected artifact revision: `r2`

You are the existing formal T7 independent review task. Root T0 may dispatch
this prompt only after formal T4 returns a terminal repair-cycle-1 result and
T0 supplies its exact immutable root and hashes.

Read the liveness protocol, the complete initial V3.1 T7 archive, the approved
repair package and its package-review archive, T4's cycle-1 handoff/archive,
the terminal candidate and all bound source/runtime/input identities.

This is an incremental review. Recheck only:

1. failed item `v31_route_a_first_required_node`;
2. failed item `v31_complete_producer_route_coverage`;
3. whether repair changes broke any frozen passed invariant;
4. all accepted/protected identities;
5. the two class-C publication repairs because they affect evidence reload.

Do not reopen previously passed V3.0/domain/inventory/formula/convention items
without exact causal evidence that the repair changed or invalidated them.

For a successful `r2`, independently rebuild from raw records:

- exact 496 Route-A mode keys and 9,920 unique ladder nodes;
- exact 102 AP keys and 458 AP node records;
- exact 23 fresh odd-only BHPT records;
- all raw amplitudes, signed currents, positive fluxes, S, direct/S Gamma and
  log Gamma routes;
- all 16 frozen thresholds and extrema;
- all 248 independent odd/even parity comparisons;
- exactly five ordered certificates and separate uncertainty budgets;
- JSONL line validity, complete source start/end ledgers, manifest identities,
  root/file permissions and in-place/distinct-copy deterministic reload;
- full-suite result and protected hashes at start/end.

Run independent focused/V3 tests and the necessary read-only reconstruction;
do not rerun the producer or create science. The common absolute phase remains
a permitted PARTIAL/nonclaim. V3.2/full-domain V3/Li/finite-radius/global GREEN
remain outside scope.

Accept only with:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: PASS
GATE_LABEL: ACCEPT GREEN / V3.1 MODE GREYBODY EVIDENCE READY FOR V3.2
```

If either original substantive blocker remains or a new defect directly
invalidates the bounded claim, return `REPAIR / FAIL` with complete class-A
fields. That consumes repair cycle 1 and permits at most one separately frozen
cycle 2. Do not design/dispatch repair or start V3.2.

The review is read-only except for T7 current and one new dated T7 archive.

