# T7cm — Pre-Edit Preservation Root-Binding Repair Review

Work only in existing T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed` as an independent read-only
reviewer. Use `gpt-5.6-sol/high`. Do not create a task, subagent, proxy, or
descendant.

## Required Reading

Fully read:

- `project.md`, `status.md`, current T0/T4/T7/T8 handoffs;
- the exact four-file T4aj candidate supplied by root T0;
- the frozen T4ai design/plan/T7cl/T4ai prompt and T7cl GREEN;
- both T4ai failed helper roots and every file in them;
- the live driver, T4ah v10 preflight, complete-290 contract, runtime,
  complete consumed T4ah witness, and immutable successful-but-unusable
  isolated pair;
- canonical/absence/external/process/transient state.

Verify candidate commit, parent, ref, exact four added paths,
commit/worktree blobs, sizes, SHA-256 values, and `git diff --check`.
Review only that exact identity.

## Independent Review

Check that:

1. The first failed root contains exactly the two frozen helper files and no
   snapshot/manifest/preflight/science.
2. The second failed root contains exactly one helper with SHA
   `b6f4ad36...7ec0`, and no other path.
3. `ROOT.parents[5]` deterministically resolves to the upper
   `GW_Wave_Optics` directory while `ROOT.parents[4]` is the exact project;
   the failure therefore occurred at the first wrong-root canonical
   `lstat`.
4. The live driver is still `057bb24b...59a9`; no T4ai driver edit,
   preflight, witness, official audit, solver, or matrix occurred.
5. The previous execution authorization is exhausted and cannot be reused.
6. The new package is a new bounded repair, not an implicit retry.
7. The helper accepts exact absolute project/control/evidence roots and
   never derives an authoritative root using `Path.parents`, numeric parent
   ascent, cwd-relative search, name search, or string stripping.
8. Exact-root verification includes `__file__`, direct containment,
   explicit cwd, `git rev-parse --show-toplevel`, candidate/ref/HEAD, and
   repository-relative lexical/physical containment.
9. AST/static gates reject path arithmetic, shell/eval/exec, mutation
   outside the fresh root, deletion/unlink, glob cleanup, network/install,
   and global-environment changes.
10. `--audit-only` and `--execute` call the same pure `collect_before()`;
    audit-only writes nothing and emits one runner-compatible canonical
    JSON payload with default `ensure_ascii=True`.
11. Launcher captures are atomic, no-overwrite, fsynced, and pre/post
    inventories prove no helper write during audit-only.
12. T4 must stop at
    `CHECKPOINT / T4AJ PRESERVATION HELPER FROZEN`; T7 GREEN does not
    authorize helper execution.
13. Root T0 must independently read/AST-check/rerun audit-only before the
    exact execution authorization.
14. Execute mode requires the exact reviewed audit digest, publishes only
    one old-driver snapshot and before manifest with exclusive/no-follow/
    atomic/fsync semantics, and makes no outside mutation.
15. The 2,566-file/582,625,016-byte witness ledger
    `c2182870...bb97` and all eight exact critical filename/hash mappings are
    bound without relabeling.
16. Both failed roots, T4ai package, driver/v10/runtime/complete-290,
    canonical four, four absent units, external object, and zero unexpected
    process/transient/link/bytecode guards remain exact.
17. One successful preservation may resume only the already reviewed T4ai
    artifact-local driver/test repair and one zero-science preflight.
18. The package does not authorize witness, scientific audit, solver,
    matrix, pair reuse, canonical write, scientific/runtime/input/threshold
    change, destructive action, new agent, or downstream work.
19. T4 uses sol/high only; max and ultra are forbidden.

Any implicit helper execution before root-T0 review, numeric root
derivation, incomplete witness/evidence binding, mutable failed root,
outside write, scientific invocation, retry, or weakened T4ai gate is a
blocker.

## No Writes Or Dispatch

Do not edit files, artifacts, environment, status, handoffs, or candidate.
Do not run helper execute mode, preflight, witness, official audit, solver,
matrix, or science. Do not dispatch T4/T7ch/T8 or perform GitHub actions.

## Exact Decision

Return to root T0 with exactly one:

```text
REVIEW GREEN / T0 PRE-EDIT PRESERVATION ROOT-BINDING REPAIR APPROVED
REVIEW YELLOW / T0 PRE-EDIT PRESERVATION ROOT-BINDING CHANGES REQUIRED
REVIEW RED / T0 PRE-EDIT PRESERVATION ROOT-BINDING REPAIR INVALID
```

Then report the exact candidate identity, independent evidence, and bounded
blockers if any.
