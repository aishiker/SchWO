# T4ba — Full-Image Cache-Accounting Projection Repair

Work only in existing T4 task
`019f5fa6-1288-7c01-8a87-4c4370cf5517` using the model/effort supplied by
root T0. Never use `max` or `ultra`. Do not create a task, subagent, proxy or
descendant.

## Start Gate

Fully read project/status/current T0/T4/T7/T8 handoffs, the complete T4ba
exact-four package and T7da decision. Require the exact candidate supplied by
root T0 and exact:

```text
REVIEW GREEN / T0 FULL-IMAGE CACHE ACCOUNTING PROJECTION REPAIR APPROVED
```

Freshly verify all identities and guards before edits.

## Preserve The Consumed Full Unit

Reconstruct and freeze the exact T4ae full unit:

- durable request/prelaunch/running/receipt/final/stream hashes;
- child 34989, exit1/no signal/exact wait/reap/PG-empty;
- 3234 files/363 directories/627046494 bytes/tab-ledger
  `330dfdcf...520b`;
- exact post-compute cache-contract stderr;
- matching audit0/checkpoint0/full pair absent;
- consumed, immutable, non-reusable authorization.

Never mutate, populate, repair, rerun, audit, copy, promote or reuse it.
Never treat its lost in-memory result as scientific PASS.

## Independently Prove The Formula

Read unchanged runner/test/config/legacy sidecar directly. Reproduce exact:

```text
masked / valid              197 / 57884
convergence / total calls    48 / 57932
final-grid keys                    358
convergence keys       214/262/310/358
cache hits                   20735842
```

Verify runner/test old literal `10367742` and legacy exact
`20735842/57932/358`. If any value or identity differs, stop fail-closed.

## Phase A Only

1. Create one fresh unique no-overwrite Phase-A root.
2. Freeze one immutable wrapper above exact T4az SHA
   `9e424fab...0d21`.
3. Preserve T4az/T4ax/T4ar behavior and intercept only the exact T4ar
   adapter load.
4. Wrap only `adapter._load_runner`; call original exactly once before any
   assignment.
5. Verify exact runner/config/legacy paths, hashes, stat/link/alias,
   implementation, old literal and pure formula.
6. Set only the loaded runner module's in-memory
   `FULL_IMAGE_OPTIMIZED_CACHE_HIT_COUNT = 20735842`.
7. Accept only exact full-image run/audit logical routes and reject all
   frequency/preflight/cross-case/arbitrary routes.
8. Delegate unchanged shim/adapter/runner main exactly once and propagate
   return/SystemExit.
9. Update only the artifact-local driver's physical launcher path/SHA.
10. Add and run every static/dataflow/formula/positive/negative test in the
    design under exact Python3.14.
11. Build and validate fresh prospective producer/audit requests without
    launching them.
12. Run only the authorized zero-science synthetic and split-root
    preflights, requiring `solver_started=false`, empty stderr, unchanged
    canonical 5/6 and zero residual state.
13. Freeze wrapper/driver hashes and exact diff, all tests/requests,
    identities/guards and science counts zero.

Diagnosed pre-execution artifact-local helper/fixture/manifest/path/hash/
bookkeeping defects must be preserved in immutable failed roots and minimally
corrected in fresh roots until checkpoint. Blind retry is forbidden.

Any need to change runner/test/scientific source/runtime/input/config/
legacy/golden/fixed identity/canonical/scientific or performance criteria/
matrix order returns fail-closed to root T0.

Only complete PASS returns exact:

```text
CHECKPOINT / FULL-IMAGE CACHE ACCOUNTING PROJECTION FROZEN
```

Then stop for root-T0 audit.

## Later Boundary

This prompt does not authorize a full producer or official audit. Only a
separate later root-T0 authorization may launch one completely fresh full
producer and, after producer PASS, one matching official audit and atomic
checkpoint. Any launched failure consumes the unit and forbids retry.

No frequency run, old-pair reuse, canonical write, new frequency,
production/plot/fixture/Kirchhoff/paper/GitHub/T7ch/downstream work is
authorized.
