# T4af — Control-Provenance Witness Repair

Work only in existing T4 task
`019f5fa6-1288-7c01-8a87-4c4370cf5517`. Do not create or replace a task.

## Start Gate

Before any write or computation, fully read and verify:

```text
docs/superpowers/specs/
  2026-07-25-t4af-control-provenance-witness-repair-design.md
docs/superpowers/plans/
  2026-07-25-t4af-control-provenance-witness-repair.md
```

Also read the frozen T4ae package, current project/status/handoffs, T4ae
note/artifacts, and the exact T4ae YELLOW evidence.

Require the exact four-file T4af candidate identity supplied by T0 and exact
matching:

```text
REVIEW GREEN / T0 REPAIR PACKAGE APPROVED
```

Verify implementation `45face32...`, runner/benchmark identities, exact
existing pair hashes, absent remaining matrix, and no live/transient state.
Stop on any mismatch.

## Phase A — No Scientific Compute

Using `gpt-5.6-sol/ultra`, implement only the artifact-local durable control
driver and its synthetic preflight required by the reviewed design. Do not
invoke a scientific solver, create a witness, or start a missing benchmark
case.

Return exactly:

```text
CHECKPOINT / DURABLE CONTROL DRIVER FROZEN
```

Include driver SHA/source path, raw synthetic record hashes, environment,
path confinement, process state, canonical pre-hashes, tests, and scope.
Then pause.

## Phase B — Exact T0 Follow-Up Required

Proceed only after T0 sends exactly:

```text
AUTHORIZED / CONTROL-PROVENANCE WITNESS COMPUTE
```

Run exactly one isolated `kM=1.58125` witness under the frozen driver. Do not
overwrite or reinterpret the canonical pair. Apply every control,
path-isolation, direct-load, bitwise scientific-equivalence, metadata,
warning, cache, fingerprint, atomicity, and canonical-hash check in the
reviewed design. Do not retry on failure.

After witness PASS, pause and report:

```text
CHECKPOINT / CONTROL-PROVENANCE WITNESS PASSED
```

T0 must freshly verify the witness before authorizing the remaining original
matrix.

## Conditional Matrix Continuation

Only after T0 sends exactly:

```text
AUTHORIZED / ORIGINAL T4AE MATRIX CONTINUATION
```

run only the still-absent original T4ae cases in the frozen order, each under
the durable driver and each fully validated before the next. Preserve the
two complete canonical pairs without any write.

Finish every equivalence/performance/resource/cache/determinism/fault/
interface/test/scope/provenance requirement from the frozen T4ae package.

Code or driver changes, identity mismatch, missing final record, unexpected
warning/output, scientific anomaly, test failure, or scope drift stop
immediately and return to T0. Driver design/modification and anomaly handling
use `gpt-5.6-sol/ultra`; only frozen fixed-command mechanics may use
`gpt-5.6-terra/medium`.

## Exact Final Decision

Return exactly one:

```text
GREEN / EQUIVALENCE-PRESERVING METHODS GATE READY
YELLOW / EQUIVALENCE-PRESERVING METHODS EVIDENCE INCOMPLETE
RED / EQUIVALENCE-PRESERVING METHODS GATE INVALID
```

Report the original irrecoverable-exit fact, prospective witness evidence,
all raw durable control records, exact implementation and artifact
identities, full frozen T4ae results, tests, scope, and non-claims.

Do not start T7ch, T8, any new frequency, production, plot, fixture,
Kirchhoff, paper-style output, or GitHub action.
