# Formal T7 prompt — V3.1-U repair-cycle-2 package review

Perform an archive-only, zero-solver review in the existing formal T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed`.

Read and rehash the liveness protocol/template, V3.0 authorities, V3.1-U
replacement and repair-1 authorities, both immutable failed roots, formal
terminal review SHA `7552b2...25c1`, the stable BHPT snapshot authority, the
new package/design, this prompt, the T4 prompt, and the future implementation
and sentinel-review prompts.

Review only package readiness.  Do not edit implementation, launch Python
science or WolframKernel, acquire a real lock, create a dispatch/root, mutate
evidence, or start V3.2.

## Required checks

Independently verify:

1. the sole class-A item is
   `v31u_route_c_external_totality_and_provenance` and one final bounded
   scientific repair remains;
2. implementation scope is exactly the six frozen paths and their baselines;
3. CLI, Route-U oracle, protected radial files, V3.0 authorities, exact graph,
   16 thresholds, five certificates, 23-anchor order and MST/90/45/45 are
   unchanged;
4. the restored external snapshot has exactly the same 25 source bytes as the
   immutable historical ledger and audit ZIP, with honest `.git`/commit
   nonclaims and no network/substitute source;
5. every success and failure terminal branch preserves exact key/error/raw
   streams/process receipt/start-end source identities and rejects incomplete,
   reordered or mixed records;
6. the one-child, one-call-per-anchor sentinel is O_EXCL, single-use,
   non-resumable and non-retryable after dispatch;
7. the sentinel and official authority chains are unique and non-circular,
   with no live-handoff, environment/argv/boolean/fallback authority escape;
8. sentinel records can never populate the official root and official
   execution must recompute A/U/B/C;
9. a failed sentinel consumes repair cycle 2 and requires T0 escalation;
10. the zero-science T4 verification and adversarial inventory are complete.

Any ambiguity in the stable snapshot chain, no-CLI sentinel invocation, or
future-review authority must be classified now; T4 may not invent another
channel.  Use the mandatory dual-axis and complete finding schema.

If ready, return exactly:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: NOT_ASSESSED
GATE_LABEL: ACCEPT GREEN / V3.1-U REPAIR CYCLE 2 PACKAGE READY FOR T4
```

Persist the durable review at
`docs/handoffs/archive/T7_2026-08-13_v3_1_u_repair_cycle2_package_review.md`
and report its SHA, exact package/member/source/protected identities,
incremental passed/failed/not-assessed inventories and nonclaims.  Package
review does not consume repair cycle 2.

No T4 action occurs until Root T0 separately dispatches the unchanged approved
package.  No science, sentinel, official run, V3.2 or global GREEN.
