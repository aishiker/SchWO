# T7cz — Historical-JSON Reference-Surface Package Review

Work only in existing T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed` using
`gpt-5.6-sol/high`. Never use `max` or `ultra`. Do not create a task,
subagent, proxy or descendant.

This is an independent read-only review. Do not modify files, refs,
artifacts, status, handoffs, permissions, environment or processes. Do not
execute a launcher, producer, audit, solver or matrix command.

## Identity Gate

Bind only the candidate supplied by root T0. Require exact parent:

```text
552ba41bf1b7f9e64777c6dbd529761e7b500cea
```

and primary HEAD:

```text
45face32f52537ac4b8ab79cb1746d8ac78e9b82
```

Require exactly four added paths relative to parent:

```text
docs/superpowers/specs/2026-07-28-t4aw-historical-json-reference-surface-repair-design.md
docs/superpowers/plans/2026-07-28-t4aw-historical-json-reference-surface-repair.md
docs/prompts/phase5_t7cz_historical_json_reference_surface_review.md
docs/prompts/phase5_t4aw_historical_json_reference_surface_repair.md
```

Verify candidate/ref/parent, commit/worktree bytes, blob/SHA/size and both
diff-checks. Reject every superseded or unreferenced identity.

## Independent Consumed-HOLD Review

Reconstruct both T4av Phase-B roots from raw files.

For the consumed v2 root verify:

- final 823 files, 117 directories inclusive, 531851545 bytes, all files
  regular non-symlink `nlink=1` mode0400, all directories mode0500;
- final canonical relative content index
  `1c1f88da368b0107d441e87b35602d75794584b036147dd96b957bf3a2fe74d1`;
- pre-terminal 821-record index file
  `6a8a956c...930a` and exact terminal checkpoint
  `c8794b03...51e9`;
- setup/request/source/input/reference/prelaunch/running/receipt/streams/
  supervisor-failure/checkpoint identities;
- exact clean legacy source 48 records, complete-input 290 unique
  roles/paths/inodes and contract `77927103...fd24`, reference 3 records and
  index `418fa14a...091a`;
- PID/SID/PGID 2090, exit0/no signal/exact wait/reap/PG-empty, canonical
  765-byte stdout and empty stderr;
- producer pair hashes `a0cf2ac4...ecaa` and `10675ce9...c46`, both
  mode0400, present but permanently non-reusable;
- matching reconstruction audit0, producer1, solver1, optimized
  runner/official audit/matrix0 and consumed authorization;
- supervisor failure `78e44bb3...ba71` with exact reason
  `noncanonical_json_record`;
- canonical four unchanged, all eight later outputs absent, external guard
  `73034fc9...a878` exact and no process/transient/link drift.

Verify the separate `audit-failure` attempt returned code2 with exact
`reconstruction_output_collision` stderr. Do not represent it as a
successful failure audit and do not let it obscure the raw supervisor
failure.

The isolated pair must not be scientifically loaded, accepted, reused,
audited or promoted by this review.

## Independent Causal Review

Read the live driver and exact source/target legacy sidecar directly.
Independently prove:

1. `_post_scientific_audit_snapshot()` reaches
   `_reconstruction_pair_snapshot()` after the child exits zero;
2. the pair snapshot calls the generic canonical-control `_read_json()` on
   the legacy sidecar;
3. `_read_json()` requires compact sorted explicit-false JSON plus newline;
4. source and target sidecar bytes are exact SHA
   `d5c839cd...9788`, size57925, read-only and inode-disjoint;
5. strict finite duplicate-free JSON parsing yields one 40-key object;
6. the historical pretty-printed bytes legitimately differ from the
   driver's compact canonical bytes;
7. failure therefore is a reader-surface mismatch, not reference drift or
   a scientific result;
8. no unaccepted array, warning value or pair is claimed as PASS.

## Repair Contract Review

Require all of the following:

1. Old driver, launcher, roots, locks and pair remain immutable.
2. Existing canonical control-record `_read_json()` remains byte-identical.
3. Existing canonical produced-witness reader remains byte-identical.
4. One dedicated historical reader is used at exactly one literal
   legacy-sidecar production call site.
5. Historical reading requires exact validated path/role/hash/size/stat/
   mode/link/inode/reference-index binding before parsing.
6. It rejects invalid UTF-8/JSON, duplicate keys, non-finite constants and
   non-object top levels.
7. It does not require formatting/key-order/whitespace reserialization
   equality and never rewrites or normalizes bytes.
8. It has no arbitrary path, fallback, basename/prefix/glob/regex,
   case/Unicode normalization or argument/environment-controlled surface.
9. Canonical/historical/produced-witness cross-use is statically rejected.
10. All source/input/reference/runtime/fixed-identity/warning/canonical/
    process guards and one-shot semantics remain unchanged.
11. Exact Python3.14 Phase A is zero-science and stops for T0 audit.
12. Any later launched failure remains one-shot/no-retry.

Freshly verify T4av candidate/checkpoint/driver/launcher/preflight,
runtime, complete290, reference index, legacy/golden/warning, fixed
identity, relocated witness, canonical four, eight absences and
external/process/transient/link guards.

## Decision

If and only if every gate passes, return exact:

```text
REVIEW GREEN / T0 HISTORICAL JSON REFERENCE SURFACE REPAIR APPROVED
```

Bind the exact candidate/ref/parent and report independently derived
evidence. State clearly that GREEN authorizes only root-T0 fresh
verification and possible later T4aw zero-science Phase A, not a producer,
matching audit or science.

Otherwise return YELLOW/RED with the smallest concrete bounded blocker and
required correction. Do not dispatch T4.
