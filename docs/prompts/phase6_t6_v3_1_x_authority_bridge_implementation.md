# Formal T6 prompt — V3.1-X authority-bridge implementation

Use only after formal T7 accepts the exact canonical package
`configs/phase6_v3_1_x_authority_bridge_package.json`, with the package and
review SHA-256 values supplied by Root T0.

Read the package, design, T0 adjudication, formal T7 package review and all
bound source implementation reviews completely. Implement only the exact
three-path delta declared by the package:

1. change production `IMPLEMENTATION_REVIEW_PATH` to the exact predeclared
   future T7 bridge archive;
2. add/adjust zero-science tests proving the positive non-circular authority
   path and every frozen negative;
3. preserve all passed implementation/science invariants and identities.

Run only focused/adjacent zero-science tests, clean-environment probes, Ruff,
format, compile and scoped diff checks. Produce fresh temporary evidence with
the six post-bridge implementation hashes and explicit zero counters.

Do not create the future T7 archive, dispatch, sentinel, official/staging
root; do not invoke Wolfram/solver; do not edit the package/design/prompts,
WLS/core/CLI, protected files, status/current handoffs; do not start V3.2.
Return a checkpoint to Root T0 for formal T7 delta-only review.
