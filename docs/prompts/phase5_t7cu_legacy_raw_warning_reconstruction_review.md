# T7cu — Legacy Raw-Warning Reconstruction Repair Review

Work only in existing T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed` using
`gpt-5.6-sol/high`. Never use `max` or `ultra`. Do not create a task,
subagent, proxy, or descendant. Review is strictly read-only.

## Start Gate

Fully read project/status/current T0/T4/T7/T8 handoffs, the exact T4ar
design/plan/T4 prompt, T4aq package/checkpoint, both consumed `2.91875`
attempts, frozen driver/launcher/runner, legacy and golden frequency
artifacts, complete-290 input evidence, runtime overlay, witness,
canonical output, and process/transient state.

Bind the review only to the candidate/ref/parent/primary identity supplied
by root T0. Verify exact-four added scope, commit/worktree bytes, hashes,
sizes, and diff-check. Do not review an earlier or unreferenced candidate.

## Required Independent Checks

1. Reconstruct the latest consumed producer request/prelaunch/running/
   receipt/final/control-audit/checkpoint chain and verify child `76752`
   exit1/no-signal/exact-wait/reaped/PG-empty, official audit0, later
   units0, canonical unchanged, and no reusable output.
2. Re-read stderr and derive the exact three rejected warning tuples and
   counts. Independently rehash/inspect the frozen overlay
   `scipy/integrate/_ivp/rk.py`.
3. Reproduce exact normalized diagnostic record
   `624 / e9ce10c6...15d97` without treating it as legacy proof.
4. Directly verify the immutable legacy `2.91875` sidecar has no
   `raw_runtime_warning_record` key, uses v1 driver
   `f5ac6a83...d08e7`, and has exact structured warnings `296/98`.
5. Verify immutable later v3 records only establish schema precedent; they
   do not determine the missing `2.91875` count.
6. Inspect exact commit `8fb8608c...6ada`, its five implementation paths,
   selected scientific code, `_compute_frequency`, inputs, amplitudes,
   boundary values, tolerances, points, and lmax `[192,216,240,264]`.
7. Review detached reconstruction materialization and require no
   scientific-source replacement, alias, writeability, symlink, hardlink,
   missing input, cache, or environment ambiguity.
8. Verify the reconstruction launcher cannot modify/wrap/filter any
   scientific computation and calls exact legacy `_compute_frequency`
   once.
9. Verify all 22 scientific arrays must be bitwise dtype/shape/bytes equal
   to legacy and golden and that structured warnings and fingerprints must
   match.
10. Verify exact overlay source normalization has no generic prefix,
    basename, glob, regex, Unicode/case alias, or argument-controlled path.
11. Verify matrix supplementation always runs the unchanged original
    legacy audit first, applies only to missing-field `2p91875`, validates
    the real witness fully, and replaces only the returned raw record.
12. Verify T4aq lazy-module closure, physical/logical argv, process and
    fixed-identity contracts remain intact.
13. Verify Phase A is zero-science; Phase B is one-shot reconstruction plus
    matching audit; Phase C is zero-science binding/preflight; every root-T0
    gate is separate.
14. Verify no warning suppression, count adjustment, missing-as-zero
    interpretation, old-root mutation, sidecar rewrite, legacy/golden/
    canonical mutation, or scientific standard relaxation.
15. Freshly verify candidate/ref/HEAD, driver/launcher/runner/runtime/
    complete-input/witness/canonical/absence/external/process/transient
    guards.

## Decision

Return exact:

```text
REVIEW GREEN / T0 LEGACY RAW-WARNING RECONSTRUCTION REPAIR APPROVED
```

only if every gate passes and no ambiguity remains.

Otherwise return:

```text
REVIEW YELLOW / T0 LEGACY RAW-WARNING RECONSTRUCTION CHANGES REQUIRED
```

or:

```text
REVIEW RED / T0 LEGACY RAW-WARNING RECONSTRUCTION REJECTED
```

with the smallest exact blocker and bounded correction. A GREEN authorizes
only root-T0 fresh verification and possible later T4ar Phase A. It does
not authorize reconstruction science, launcher execution, matrix retry,
canonical write, T7ch, or downstream work.

Do not modify any file, ref, artifact, status, handoff, environment, or
process. Do not execute a launcher, reconstruction, producer, official
audit, solver, or matrix unit.
