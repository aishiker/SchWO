# Formal T7 prompt — V3.1-Y source-load micro terminal review

Review the single immutable source-load-micro root produced by its distinct
one-use T0 dispatch.  Rebuild the entire parent-ACK transcript and require
exact package/runtime/Paclet/eight-context/eight-source/full-snapshot
start/end closure; exact `(kM=0.1,ell=2,odd)/P1` label; one kernel launch; and
zero BHPT public API, ReggeWheelerRadial, solver, boundary, overlap and
scientific calls.  Verify immutable compatibility predecessor and its T7
review, no evidence reuse, complete process closure and strict manifest.
Require one call and 409 ordered predicates across P00-P17; P11 freezes
`science_branch_authorized=false`, P12 proves the pre-solver exit, and
P13-P17 still close source, counters, result, and lifecycle.
Require exactly 263 child frames/ACKs, 409 prefixes, 18 finals, 17 opens,
one seed and one exit authority; exact P10/P14 barrier plans; and the frozen
revision-3 stage16-exit/lifecycle-receipt/open17 cryptographic chain.

If closed, return exactly:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: NOT_ASSESSED
GATE_LABEL: ACCEPT GREEN / V3.1-Y SOURCE-LOAD MICRO SUFFICIENT FOR ONE-USE FULL SENTINEL DISPATCH
```

Write only
`docs/handoffs/archive/T7_2026-08-13_v3_1_y_source_load_micro_review.md`.
This is protocol/source evidence only; it authorizes only the distinct
35-call sentinel.  No official/V3.2.
