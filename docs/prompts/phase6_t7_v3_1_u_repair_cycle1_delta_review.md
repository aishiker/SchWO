# Formal T7 prompt — V3.1-U repair-cycle-1 implementation delta review

This prompt becomes active only after formal T7 has approved the exact repair
package and formal T4 has returned zero-science implementation/preflight
evidence for exactly four changed paths.

Perform an incremental, archive-only review in formal T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed`.  Do not run a numerical science
solver, acquire a real-root lock, create a dispatch, mutate evidence, or start
V3.2.

Freeze the package, package-review archive, T4 evidence inventory and exact
four repaired hashes at review start.  Recheck only:

1. failed item `v31u_route_u_auxiliary_geometry_totality` and blocker
   `v31u_auxiliary_radius_closed_boundary_roundoff`;
2. class-C item `v31u_live_handoff_start_gate_recursion`;
3. whether the four-path delta damaged a previously passed invariant;
4. source, CLI, domain, threshold, convention and seven protected identities.

## Mandatory independent checks

- Re-derive the exact closed-boundary and just-below/just-above decisions from
  nonnegative algebra; reject epsilon/tolerance/floor/forced-exponent logic.
- Rebuild the exact 318 routed keys and three scheduled precisions and confirm
  954 deterministic successful geometry nodes with zero solver calls.
- Verify the original failure key passes at 120/160/220 dps without changing
  its radius, exponent set/order or precision.
- Run/review the frozen focused tests and the CLI preflight; independently
  inspect solver-call guards rather than trusting a reported counter alone.
- Verify exact four-path scope and frozen CLI SHA.
- Attack every immutable authority, required verdict token, repaired source
  hash, failure identity, protected identity, target-root binding and dispatch
  single-use rule; all must fail closed.
- Confirm live `T7_current.md`, status or T0 handoff drift cannot be selected
  as an alternate authority and cannot make a valid immutable chain fail.
- Confirm no failed-root science byte is reusable and official execution is
  impossible before a one-use T0 dispatch bound to this review.

Preserve all passed items from terminal review archive
`7aa90e012e7d079d7c16256647d97db663293cf9ff6f5a4346130aa1d82482f1`
unless the four-path delta provides exact new contrary evidence.

If both items close and all invariants hold, return exactly:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: NOT_ASSESSED
GATE_LABEL: ACCEPT GREEN / V3.1-U REPAIR CYCLE 1 IMPLEMENTATION READY FOR ONE-USE T0 DISPATCH
```

This consumes bounded scientific repair cycle 1 and authorizes only Root T0 to
create one exact fresh-root dispatch.  It is not V3.1-U scientific acceptance.

If a complete class-A blocker remains, return `REPAIR` and state whether it is
the same substantive blocker; the next bounded action, if any, is repair cycle
2 under the liveness limit.  A control-plane-only defect must be classified C
and handled by delta verification without reopening science.

Write the durable result to
`docs/handoffs/archive/T7_2026-08-12_v3_1_u_repair_cycle1_implementation_delta_review.md`
and report its SHA-256, the exact package/review/evidence/four-file identities,
test results, 954-node inventory identity, source/protected start/end hashes,
incremental item state and nonclaims.

No official execution, V3.2, Li-figure recomputation, finite-radius observer
claim, threshold relaxation, full-domain V3 or global GREEN is authorized by
this review alone.
