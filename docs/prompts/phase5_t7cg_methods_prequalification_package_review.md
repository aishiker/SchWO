# T7cg — Methods Prequalification Package Review

Work only in existing T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed` as the same independent read-only
repair-package reviewer. Do not create or replace a task.

## Required Reading And Identity

Fully read:

- `project.md`, `status.md`, current T0/T4/T7/T8 handoffs;
- T7cf failure evidence and immutable T4ad/T8as contracts/artifacts;
- the exact five-file candidate package supplied by T0;
- applicable `docs/physics_spec.md`, `docs/architecture.md`,
  `docs/numerics.md`, `docs/validation_plan.md`, and
  `docs/extension_interface.md`;
- the legacy source/tests needed to check feasibility and scope.

Verify the supplied candidate commit, parent, exact five added paths, every
commit/worktree blob, and every SHA-256. Review only that identity.

## Independent Review

Check independently that:

1. The T7cf artifact gate is valid and the reported `4/2,352` phase,
   `719/1,760` hierarchy, `80` summary, `1,085` extrema, and exact
   87-interval diagnostic inventory are correctly bound.
2. No diagnostic midpoint is authorized as a run input and no current frozen
   design/plan/prompt/artifact/threshold is changed.
3. The methods-only gate is a bounded prerequisite rather than an implicit
   new scientific stage.
4. The five frequencies and full image artifact are immutable existing
   evidence and cover low/transition/mid-high/phase-neighborhood/high regimes.
5. The error budget is fixed before results, separate from and no looser than
   the unchanged scientific acceptance criteria.
6. Performance cannot be bought by dropping modes/frequencies/points,
   reducing image resolution, loosening solver/science thresholds, changing
   physics, or removing independent checks.
7. Cache keys bind complete physics/solver/config/source/gate/implementation
   identity; radius-specific oracle states remain distinct; checkpoint reuse
   fails closed.
8. The typed interface separates every required subsystem, preserves the
   current legacy adapter, avoids hard-coded Schwarzschild separability, and
   makes no new spin-2 claim.
9. Exact implementation paths, baseline-before-edit ordering, pre-compute
   commit, tests, fault injection, profiling, resource bounds, stop rules,
   model/reasoning rules, and downstream dependencies are auditable.
10. T7ch independent equivalence/performance review is mandatory before T0
    may freeze any later scientific package.

## No Writes

Do not edit `status.md`, handoffs, archives, implementation, artifacts, or
candidate files. Do not dispatch T4ae/T7ch/T8, perform GitHub actions, or
repair the package.

## Exact Decision

Return exactly one project-defined decision, followed by candidate identity,
evidence, and bounded change requests if any:

```text
REVIEW GREEN / T0 REPAIR PACKAGE APPROVED
REVIEW YELLOW / T0 REPAIR PACKAGE CHANGES REQUIRED
REVIEW RED / T0 REPAIR PACKAGE INVALID
```

Return only to T0.
