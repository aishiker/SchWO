# T4ao — Terminal Filename-Set Closure Repair

Work only in existing T4 task
`019f5fa6-1288-7c01-8a87-4c4370cf5517` using
`gpt-5.6-sol/high`. Do not use `max` or `ultra`. Do not create a task,
subagent, proxy, or descendant.

## Start Gate

Fully read the exact T4ao design/plan/T7cr prompt, project/status/current
T0/T4/T7/T8 handoffs, T4an package/review and immutable failed roots,
frozen driver/v10/runtime/complete290/runner, consumed witness,
canonical/absence/external/process/transient state.

Require the exact candidate supplied by root T0 and exact:

```text
REVIEW GREEN / T0 TERMINAL FILENAME-SET CLOSURE REPAIR APPROVED
```

Freshly verify every identity. T4an/T7cq alone is not authority.

## Preserve T4an HOLD

Independently verify and then leave untouched:

- helper `a0b6e77b...def4`;
- launcher `517f8608...a3a`;
- 15-file audit inventory;
- child exit0/no signal/empty stderr/canonical stdout;
- exact wait/reap/PG-empty and pre/post equality;
- reserved execution absence and science0/0/0;
- manifest expected14/actual15 with only launcher extra;
- invalid embedded success, absent failure branch, open audit root;
- consumed authorization and no replay/repair/reuse.

## Phase A0 Only

1. Choose one fresh pairwise-distinct helper/audit/reserved-execution
   direct-child triple. Create helper and audit roots only.
2. Freeze one helper and one file-based launcher with exclusive/no-follow/
   atomic/fsync/reload/stat/inode/nlink guards.
3. Freeze project/control/helper/audit/execution, exact helper/launcher
   paths/hashes, and Python3.14 as direct full exact constants.
4. Preserve the dedicated v10 explicit-`ensure_ascii=False` serializer and
   separate default-true replay serializer. Preserve every T4an v10 and
   process-classifier fixture.
5. Freeze disjoint baseline/common/success-index/success-manifest/
   failure-index/failure-manifest names.
6. Implement pure `expected_before_manifest(branch)` and
   `expected_final_names(branch)` functions using:
   baseline launcher + common records + selected branch index + selected
   terminal manifest.
7. Make the live closure path call those exact functions. Raw substring
   counting, set-only duplicate-blind checks, and a separate production
   formula are forbidden.
8. Before child launch, run all frozen success/failure terminal-set
   positive/negative fixtures, including missing/duplicate/wrong launcher,
   missing/extra/cross-branch names, self-reference, and pre/final mismatch.
9. Require initial audit root contains exactly the frozen launcher and bind
   its exact identity separately.
10. Close helper/launcher and invoke the frozen launcher file at most once
    with `--first-attempt`, exact Python3.14/argv/cwd/bounded environment.
11. Capture complete pre inventories, raw stdout/stderr, return/signal/
    wait/reap/PG-empty, and complete post inventories before publication.
12. Select success/failure from exact child semantics. Publish common
    records, selected branch index, require exact pre-manifest names,
    publish selected terminal manifest, then require exact final names.
13. Require every final child is regular non-symlink nlink1 mode0400,
    close audit root mode0500, fsync audit/control, and report post-close
    identity without self-hash/stat circularity.
14. Reverify all driver/v10/runtime/complete290/witness/canonical/absence/
    external/process/transient/link/bytecode/quarantine and science0 guards.

Only complete PASS returns exactly:

```text
CHECKPOINT / T4AO TERMINAL FILENAME-SET AUDIT HELPER FROZEN
```

Report roots, helper/launcher identities, source/AST/dataflow review,
v10/process/terminal fixtures, argv/cwd/env, raw streams, child terminal,
all records/index/manifest identities, exact pre/final name sets,
post-close root, execution absence, frozen guards, and zero-science counts.
Then pause for root-T0 audit and one separately authorized replay.

Any failure freezes evidence, consumes the attempt when applicable, returns
HOLD, and stops. Do not create alternate roots/helper/launcher or retry.

## Not Authorized

No old-root mutation; replay-only; execute; execution-root creation;
driver snapshot/edit; T4ai preflight; scientific runner/official audit/
solver/witness/matrix; old pair reuse; canonical/external mutation;
cleanup/destructive/network/install/global action; task/subagent/proxy/
descendant; max/ultra; new frequency; production/plot/fixture/Kirchhoff/
paper/GitHub/downstream work.
