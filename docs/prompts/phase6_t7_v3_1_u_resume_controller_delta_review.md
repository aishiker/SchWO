# Formal T7 prompt — V3.1-U resume-controller implementation delta review

You are the existing formal SchWO T7 task.  Run this prompt only after Root T0
supplies the exact reviewed resume-controller package approval, the four final
T4 implementation hashes and the temporary-test evidence identities for gate
`phase6_v3_1_u_resume_controller_repair_1`.

Review independently.  Do not edit implementation, science, package, prompt or
current-handoff bytes; do not acquire/rename the real lock, append to the real
root, run a radial/AP/Wolfram solve, create a terminal artifact, dispatch T4 or
start V3.2.

## Archive-only governance

The original science start ledger binds `status.md`, `T0_current.md` and
`T7_current.md`.  They must remain byte-identical until terminalization.  Write
the durable delta-review decision only to a new archive under
`docs/handoffs/archive/`; do not update `docs/handoffs/T7_current.md`.  Confirm
the three frozen coordination hashes at both review start and end.  This is a
provenance-preserving publication delay, not a waiver of independent review.

## Required checks

Read the liveness protocol/template, original V3.1-U replacement authorities,
resume package/design/T4 prompt and package-approval archive, T4's reported
evidence, all four new files, the original five V3.1-U files and the interrupted
root read-only.

Independently verify:

1. the four-file diff is the complete allowed diff and changes no science
   formula, solver, selector, threshold, domain, convention or existing root;
2. read-only discovery reconstructs exactly 435 Route-A modes, 8,700 ladder
   nodes, ordinals 0..434, next key `(8,28,even)`, no U/B/C/terminal and no live
   writer, with every frozen hash/size/checkpoint identity exact;
3. the original `.writer.lock` is the never-renamed stable exclusion pathname,
   is exclusive and symlink/hardlink/live-owner safe, remains flocked through
   terminal 0555 validation, and is retained as manifest-bound evidence;
4. no science call is possible before package, package approval, implementation
   approval and one-use T0 dispatch identities all pass;
5. authority/prepared/checkpoint/commit files use complete staged bytes plus
   atomic no-replace publication, so partial final-name metadata is impossible;
   intent/prepared/append/checkpoint/commit ordering is crash-consistent for
   every injected interruption, never truncates and never re-solves an already
   prepared or committed result;
6. only the exact suffix can be appended; an authenticated partial JSONL block
   is completed solely by appending its missing byte tail, while any mismatch
   fails closed; prefixes remain byte-identical, and Route A must finish before
   route-map/U/B/C work;
7. subsequent system interruptions form a monotone authority chain, while a
   science/provenance/threshold/nonfinite/unknown-suffix failure is non-resumable;
8. original `source_start` remains unchanged; additive source/authority ledgers
   are complete; both original live-source validation and additive validation
   are mandatory before terminal PASS;
9. tests use real filesystem primitives and include positive/negative/temp-copy
   coverage sufficient to support one bounded real-root dispatch;
10. the real interrupted root and all protected/current identities remained
    unchanged throughout implementation and review.

The current claim is controller readiness only.  V3.1-U science remains
`NOT_ASSESSED`; no V3.2 or global GREEN is permitted.

If and only if the implementation is sufficient for exactly one T0-authorized
real-root resume, return exactly:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: NOT_ASSESSED
GATE_LABEL: ACCEPT GREEN / V3.1-U RESUME CONTROLLER READY FOR ONE-USE T0 DISPATCH
```

Otherwise return a governance-compliant `REPAIR` finding with all nine class-A
fields, or `ESCALATE` if the bounded controller cannot be made coherent without
changing frozen science.  Persist the exact decision and evidence in one new
archive-only file and return its path/SHA to T0; do not modify `T7_current.md`.
