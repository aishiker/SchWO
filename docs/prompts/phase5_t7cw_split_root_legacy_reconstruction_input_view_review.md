# T7cw — Split-Root Legacy Reconstruction Input-View Package Review

Perform an independent read-only review in existing T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed` using
`gpt-5.6-sol/high`. Never use `max` or `ultra`. Do not create a task,
subagent, proxy, or descendant.

## Review Identity

Root T0 supplies one immutable exact-four candidate, its parent/ref, and
primary HEAD. Verify:

- candidate/ref/parent/primary identities;
- exactly four added documentation paths;
- commit/worktree byte equality, blob/SHA/size, and diff-check;
- no superseded candidate is reviewed.

## Independently Reconstruct The Consumed RED

Read raw files, not summaries, from:

```text
t4as_phase_b_legacy_warning_witness_883375d9_20260727T212819p0800
```

Verify request/setup/prelaunch/running/receipt/streams/final failure/control
audit/failure index/checkpoint/ledger. Verify PID/SID/PGID `37999`, exit64,
no signal, exact wait/reap/PG-empty, empty stdout, exact stderr, one producer
launch, zero matching audit/optimized runner/official audit/matrix, no pair,
and consumed authorization.

Independently prove:

1. source manifest and input manifest bind the same directory;
2. legacy HEAD is exact;
3. source status has exactly the frozen ten records;
4. all ten are complete-290 target paths;
5. the launcher requires an empty status before `_compute_frequency()`;
6. the driver requires `input_root == detached_root == cwd`;
7. therefore materialization necessarily violates the unchanged launcher
   clean-source gate.

Do not classify the child as a scientific result.

## Review The Repair Contract

Require the corrected package to preserve the launcher byte-for-byte and
split:

```text
clean exact-legacy source root
separate complete-290 input root and process cwd
isolated output root
isolated HOME
separate producer/audit run roots
```

Review all of the following:

- source Git HEAD/status and tracked-source manifest remain exact and clean;
- no input target or manifest is written into the source root;
- input root contains exact 290 read-only, regular, non-link, unique-inode
  targets and canonical contract `77927103...fd24`;
- request cwd/allowed cwd equals input root;
- launcher `--detached-root`, source validation, module origins, runtime
  project root and `PYTHONPATH` use source root;
- post-child validation does not substitute cwd for source root;
- producer and matching audit bind both roots exactly;
- root distinctness, direct-child, overlap, alias, hardlink, symlink,
  writeability and post-drift gates are fail closed;
- split logic is limited to the two reconstruction phases;
- unchanged launcher still has one exact `_compute_frequency()` call and all
  warning/array/scientific semantics unchanged;
- Phase A is zero-science, includes import-only and full positive/negative
  coverage, and stops at the frozen checkpoint;
- a new one-shot plus matching audit, then separate Phase C, remain
  root-gated.

Verify exact fixed identities: driver, T4as checkpoint, launcher, legacy
commit/artifacts, raw/structured warning records, runtime, complete290,
relocated witness, fixed benchmark identity, canonical four, eight later
absences, locks/external/process/transient guards.

## Decision

Return exact:

```text
REVIEW GREEN / T0 SPLIT-ROOT LEGACY RECONSTRUCTION INPUT VIEW APPROVED
```

only if every gate passes. Otherwise return YELLOW/RED with the smallest
concrete blocker and bounded correction.

This review is read-only. It authorizes no T4 work, launcher execution,
producer, audit, solver, matrix, canonical write, Phase C, downstream work,
GitHub action, or new task/agent.
