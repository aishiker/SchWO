# Formal T7 prompt — V3.1-X repair-cycle-2 package review

Perform an archive-only, zero-solver review in formal T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed` with high reasoning effort.

Read and rehash the complete V3.1-X authority chain, both immutable failed
sentinel roots, the attempt-0002 terminal review, the repair-cycle-2 design and
T4 analysis, the package, this prompt, the T6 implementation prompt and the
future implementation-delta prompt.  Review readiness only.  Do not edit
implementation, launch Wolfram, create dispatch/root, or start V3.2.

Independently verify:

1. the only live Class-A blocker is the order-sensitive WLS Association
   comparison, with exact eight semantic source identities and zero prior
   solver/science calls;
2. this is final bounded science repair `2/2`; attempt-0001/0002 and their roots
   remain immutable/non-reusable and cycle 3 is forbidden;
3. the exact implementation scope is five paths or fewer and excludes the
   direct physics core and all protected science authorities;
4. the six-field projection, exact-key/type checks, frozen list order and all
   record/path/stat/inode adversaries are complete without semantic weakening;
5. the one-real-Wolfram source-load micro-sentinel is structurally unable to
   reach `ReggeWheelerRadial` and must close with exactly zero science counters;
6. micro, later full sentinel attempt-0003 and unchanged official attempt-0001
   form distinct one-use, non-circular authority namespaces;
7. fake-child evidence cannot satisfy the real RawJSON-to-Wolfram boundary;
8. every implementation-delta, micro or full-sentinel failure forces
   `ESCALATE / T0 ADJUDICATION REQUIRED`, with no retry or cycle 3;
9. package/domain/threshold/convention/source/method/precision/graph and seven
   protected hashes remain exact.

If ready, output exactly:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: NOT_ASSESSED
GATE_LABEL: ACCEPT GREEN / V3.1-X SOURCE-LEDGER REPAIR CYCLE 2 PACKAGE READY FOR T6
```

Write only
`docs/handoffs/archive/T7_2026-08-13_v3_1_x_sentinel_repair_cycle2_source_ledger_package_review.md`
and report its SHA-256, inventories, findings and nonclaims.  Package review
does not itself consume repair cycle 2 and authorizes no implementation,
science, dispatch, V3.2 or global GREEN.

