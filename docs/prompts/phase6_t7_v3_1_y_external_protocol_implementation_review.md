# Formal T7 prompt — V3.1-Y implementation review

Perform an incremental archive-only review after formal T6 completes the
exact-six zero-science implementation.  Bind the accepted package review,
exact implementation hashes, machine contract, source/protected identities
and tests.  Recheck only implementation items, protected passed invariants and
future authority absence.

Independently bind the final delta-2 package authority, then expand the corrected registry and require exact operation
counts `151/409/14315/65849`, global `80724`, exact call ordinals, ordered-ID
hashes, and 118 compatibility cases. Rebuild every wire schema/FSM edge and
mutate every field in isolation. Require exact revision-3 field counts
`27/29/18/24/21/29/25/19`, per-field type/domain/nullability/value rules,
the frozen encodings/digest input order, cross-stage event/prefix chaining,
operation-specific barrier totals, last-event prefix+final+open ACK,
stage-0 seed, stage16 exit plus lifecycle receipt cryptographically entering
open17/P17/final/terminal, and parent-only stage 17. Rebuild the
496-mode inventory and synthetic fresh selectors with `N_U=0/1/496`; reject
fixed-318 acceptance and all denylisted failed-U provenance. No science.

Run static/fake-process CPython 3.14 tests and adversaries for every predicate
template and compatibility case.  Prove compatibility WLS has no science call
site; prove the micro branch precedes the sole science-capable WLS public API
call; verify parent durability/ACK, explicit Boolean behavior, per-event
prefix/final-stage checkpoint progression without deadlock,
parent-barrier/stage-edge behavior, exact observer-role projection, fake-child
rejection, process closure, strict manifest/path grammar, graph totality and
noncircular authorities.  Do not run
the real WolframKernel in this review: the next formal compatibility dispatch
is the only real-kernel authority.

If closed, return exactly:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: NOT_ASSESSED
GATE_LABEL: ACCEPT GREEN / V3.1-Y IMPLEMENTATION READY FOR ONE-USE REAL-KERNEL COMPATIBILITY DISPATCH
```

Write only
`docs/handoffs/archive/T7_2026-08-13_v3_1_y_external_protocol_implementation_review.md`.
Any blocker is a bounded Y-gate `REPAIR`; no execution is authorized without
`ADVANCE`.  Do not create dispatch/root or start science/V3.2.
