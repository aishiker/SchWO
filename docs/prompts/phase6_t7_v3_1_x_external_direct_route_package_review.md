# Formal T7 prompt — V3.1-X external direct-route package review

Perform an archive-only, zero-science initial review in formal T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed`.

Gate: `phase6_v3_1_x_external_direct_route_v1`

This is a distinct redesigned gate after T0 adjudication. It is not V3.1-U
repair cycle 3. Use `docs/review_gate_liveness_protocol.md` and the mandatory
verdict template.

## Read and bind

Read completely and independently re-hash the package, design, T4 analysis,
T4 archive, this prompt, the future T4 implementation prompt, the future T7
science-review prompt, all V3.0 authorities, the T7 V3.1-U sentinel ESCALATE
archive, failed sentinel root, external base snapshot/authority, existing
bounded direct-integration implementation/evidence, review protocol/template,
V3 master prompt and all seven protected radial sources.

## Required initial-review items

Verify independently:

1. liveness lineage is exact: V3.1-U exhausted two repairs, failed sentinel is
   immutable, and V3.1-X is a distinct gate with zero completed repairs;
2. exact 23-key order derives from V3.0 and is not the observed 14-key success
   subset;
3. the same predeclared direct NumericalIntegration method applies to all 23
   keys with zero MST/fallback/prior-value reuse;
4. equation, boundary normalizations, Wronskian signs, V3-F02 conversion and
   direct `Gamma_flux` versus `Gamma_S` separation are correct;
5. exact rational frequency construction closes the demonstrated decimal
   input defect without claiming the `(2,18)` cause is proven; the frozen
   `90/45/45 -> 120/60/60` direct ladder does not lower the failed MST
   contract's working precision or goals;
6. only the first odd-sector `rin/rout` literals differ, all six transformed
   hashes reproduce, and the route is honestly labelled BHPT NumericalIntegration
   plus reviewed boundary-geometry overlay rather than pristine upstream;
7. official graph is exact `23*7=161` calls, `322` In/Up solves and `483`
   overlap records; sentinel graph is exact `35/70/105` with fixed extrema;
8. unchanged 16 thresholds, five certificate IDs, separate numerical and
   convention budgets, exact production graph and protected identities;
9. sentinel and official publication/dispatch/failure/resource protocols are
   non-circular, one-use, no-retry and source-closed;
10. implementation scope is exactly six new paths and the future prompts are
    executable without T4 inventing authority.

Freeze `passed_items`, `failed_items`, `partial_allowed_items` and
`not_assessed_items`. Every blocker must be a complete class-A finding.

## Prohibitions

Do not edit package/design/implementation, launch Python science or Wolfram,
create dispatch/root, mutate evidence, review implementation not yet written,
or start V3.2.

If ready, return exactly:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: NOT_ASSESSED
GATE_LABEL: ACCEPT GREEN / V3.1-X EXTERNAL DIRECT-ROUTE PACKAGE READY FOR T4
```

Persist the durable review at
`docs/handoffs/archive/T7_2026-08-13_v3_1_x_external_direct_route_package_review.md`,
report its SHA-256, and update `docs/handoffs/T7_current.md`. Package GREEN is
readiness only. No implementation science, sentinel, official V3.1, V3.2 or
global GREEN follows.
