# T7ck — Recursive Input-Package Closure Review

Work only in existing T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed` as an independent read-only
reviewer. Do not create a task, subagent, proxy, or descendant.

## Required Reading

Fully read:

- `project.md`, `status.md`, current T0/T4/T7/T8 handoffs;
- frozen T4ae, T4af, and T4ag design/plan/prompts;
- the exact four-file T4ah candidate supplied by root T0;
- the complete failed T4ag witness request, setup, independent prelaunch
  audit, running/final records, streams, control audit, detached source and
  48-item input manifest;
- the frozen driver, T4ag Phase-A preflight, hermetic overlay, runner, and
  `tablei_another_bounded_local_refinement._input_records()`;
- all six accepted upstream package roots and their active inventories.

Verify candidate commit, parent, ref, exact four added paths,
commit/worktree blobs, file sizes, SHA-256 values, and `git diff --check`.
Review only that exact identity.

## Independent Review

Check that:

1. T4ag RED is exact, immutable, and not reinterpreted as runtime or science
   success.
2. Exit1, 2769-byte stderr, reaping/PG-empty, runtime pre/post, solver0,
   audit0, no pair, canonical stability, and absent matrix units are
   independently confirmed.
3. The traceback is explained exactly by recursive package cardinality
   checks preceding the returned individual hash records.
4. The six accepted active counts are exactly
   `23,31,53,87,17,59`.
5. The inventories contain 270 distinct active members, of which 28 overlap
   the old 48-item semantic contract, requiring 242 additions and 290 final
   entries.
6. The design's exact UTF-8 canonical-JSON schemas and locale-independent
   Unicode code-point path comparator are complete and independently
   reproducible. Per-package content indexes, combined-content index
   `720216bc...c8c6`, membership index `9f0a743d...b0d1`, and final
   contract index `77927103...fd24` independently match. No implicit locale,
   filesystem order, shell sort, or `localeCompare` is admissible.
7. Quarantine is excluded exactly as the frozen runner excludes it; no
   `.tmp`, partial, lock, symlink, hardlink/shared inode, special file,
   alias, source drift, extra file, or path escape is admitted.
8. Existing semantic roles remain exact and deterministic new roles cannot
   collide.
9. The detached copy and manifest rules bind every source and target byte,
   stat identity, mutability, role, and repository-relative path.
10. The bounded driver change cannot alter runner, implementation, runtime
    overlay, source manifest, supervision, scientific audit, matrix,
    canonical guards, or scientific acceptance.
11. Phase A is exact Python-3.14 and zero-science, preserves all prior tests,
    and adds adequate positive/negative recursive-package coverage.
12. A new witness requires a fresh root-T0 checkpoint audit and a new
    one-shot authorization; failure cannot imply retry.
13. Matrix order and all downstream/non-claim guards are unchanged.
14. T4 code work uses sol/high by default, never ultra, with max only after
    explicit root-T0 complexity determination.

Freshly inspect current refs, primary/exact-fourteen, driver/preflight/
overlay/RED evidence, canonical hash/stat/inodes, remaining absent units,
package source identities, and process/transient state.

Any incomplete inventory, mutable/unbound package member, count-only
acceptance, hidden alias, implicit retry, weakened criterion, or identity
drift is a blocker.

## No Writes Or Dispatch

Do not edit status, handoffs, candidate files, driver, implementation,
artifacts, packages, or environment. Do not provision inputs, run preflight,
witness, audit, matrix, or science. Do not dispatch T4/T7ch/T8 or perform
GitHub actions.

## Exact Decision

Return to root T0 with exactly one:

```text
REVIEW GREEN / T0 RECURSIVE INPUT PACKAGE REPAIR APPROVED
REVIEW YELLOW / T0 RECURSIVE INPUT PACKAGE CHANGES REQUIRED
REVIEW RED / T0 RECURSIVE INPUT PACKAGE INVALID
```

Then report exact candidate identity, independent evidence, and bounded
blockers if any.
