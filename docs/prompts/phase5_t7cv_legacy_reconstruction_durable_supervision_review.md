# T7cv — Legacy Reconstruction Durable-Supervision Repair Review

Work only in existing T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed` using
`gpt-5.6-sol/high`. Never use `max` or `ultra`. Do not create a task,
subagent, proxy, or descendant. Review is strictly read-only.

## Start Gate

Fully read project/status/current T0/T4/T7/T8 handoffs, the exact
T4ar/T4as designs/plans/prompts, T7cu decision, T4ar Phase-A checkpoint,
prelaunch HOLD root, both consumed `2.91875` matrix attempts, current
driver/launchers/runner, exact legacy source and artifacts, runtime overlay,
complete inputs, witness, canonical output, and process/transient state.

Bind review only to the candidate/ref/parent/primary identity supplied by root
T0. Verify exact-four added scope, commit/worktree bytes, hashes, sizes, and
diff-check. Do not review an earlier or unreferenced candidate.

## Required Independent Checks

1. Rehash and reload the HOLD helper and manifest. Verify canonical bytes,
   exact two-file root, modes `0500/0400/0400`, zero child/request/detached/
   pair/process/transient, and `authorization_consumed=false`.
2. Independently inspect driver `_ALLOWED_PHASES`, phase sets,
   `_expected_scientific_commands`, launcher binding, `_validate_bindings`,
   runtime/control/failure audit paths, and post-success terminal parsing.
3. Prove witness/matrix phases require the unchanged optimized runner and
   cannot legally carry the reconstruction launcher's `run/audit` argv.
4. Verify exact new literal producer/audit phases are distinct and do not
   change any old phase value or behavior.
5. Verify reconstruction phases do not reuse optimized launcher/command/
   output/terminal schemas and cannot be accepted through synthetic paths.
6. Audit exact producer markers `false/true/true/run` and audit markers
   `false/false/true/audit`.
7. Audit the full reconstruction binding: launcher, argv, roots, commit,
   runtime, complete inputs, legacy/golden, Phase-A checkpoint, warning record,
   isolated output, canonical guards, and no-promotion semantics.
8. Audit the exact one-line producer and audit stdout schemas and direct pair
   reload/hash/stat/inode/22-array/warning validation.
9. Audit request/prelaunch/running/receipt/streams/final/control/failure
   durability for both phases, including exit/signal/wait/reap/PG semantics.
10. Audit producer PASS before audit construction and exact producer/audit
    root/launcher/runtime/source/input/pair/process provenance equality.
11. Audit token/ancestry process classification, duplicate prevention, and
    harmless observer-prose behavior.
12. Verify zero-science tests cover all positives/negatives from the design,
    including cross-surface parser/dataflow rejection and old-phase
    compatibility.
13. Verify the real prospective request gate launches no reconstruction
    launcher, optimized runner, audit, official audit, or solver and that its
    roots are non-reusable.
14. Verify local self-remediation is confined to diagnosed pre-execution
    artifact-local control defects and cannot change scientific meaning.
15. Freshly verify candidate/ref/HEAD, T4ar checkpoint, driver/launchers/
    runner/runtime/complete-input/witness/legacy/golden/canonical/absence/
    external/process/transient guards.

## Decision

Return exact:

```text
REVIEW GREEN / T0 LEGACY RECONSTRUCTION DURABLE SUPERVISION REPAIR APPROVED
```

only if every gate passes and no ambiguity remains.

Otherwise return:

```text
REVIEW YELLOW / T0 LEGACY RECONSTRUCTION DURABLE SUPERVISION CHANGES REQUIRED
```

or:

```text
REVIEW RED / T0 LEGACY RECONSTRUCTION DURABLE SUPERVISION REJECTED
```

with the smallest exact blocker and bounded correction.

A GREEN authorizes only root-T0 fresh verification and possible later T4as
Phase A. It does not authorize driver edit by T7, reconstruction launcher
execution, producer, audit, solver, matrix, canonical write, T7ch, or
downstream work.

Do not modify any file, ref, artifact, status, handoff, environment, or
process. Do not run a helper, launcher, producer, audit, solver, matrix, or
preflight that can invoke science.
