# Formal T7 prompt — V3.1-U repair-cycle-2 implementation delta review

This prompt activates only after formal package approval and formal T4 returns
zero-science implementation/preflight evidence for exactly six changed paths.

Perform an incremental, archive-only review in formal T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed`.  Freeze the package, package-review
archive, T4 evidence and six implementation hashes at review start.  Do not
launch WolframKernel/science, acquire a real-root lock, create a dispatch/root,
mutate evidence, or start V3.2.

Recheck only the failed Route-C totality/provenance item, any control-plane
repairs, preservation of already passed Route A/U/B and protected invariants,
and the exact six-file delta.

Independently attack:

- 23-anchor cardinality/order/uniqueness and MST/90/45/45 constants;
- first/middle/last `MST_FAILED` and `AMPLITUDE_INVALID` diagnostics;
- exit0/70, signal, timeout, missing/torn/noncanonical output and raw streams;
- one wait/reap receipt and complete success/failure artifact grammar;
- WLS/kernel/snapshot/audit/25-file/loaded-path start-end drift;
- symlink/hardlink/missing/extra/collision/permission adversaries;
- missing/reused/wrong-root/wrong-review/wrong-six-hash sentinel dispatch;
- sentinel failure followed by a forbidden second launch;
- official dispatch before formal sentinel review;
- official attempts to copy/reuse sentinel records;
- immutable authority substitution and live-handoff/env/argv/fallback bypass;
- zero real solver/Wolfram/science calls in implementation evidence.

Re-run or review the exact focused tests and unchanged CLI preflight.  Rehash
the six files and all frozen identities at review end.

If complete, return exactly:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: NOT_ASSESSED
GATE_LABEL: ACCEPT GREEN / V3.1-U REPAIR CYCLE 2 IMPLEMENTATION READY FOR ONE-USE ROUTE-C SENTINEL
```

Persist at
`docs/handoffs/archive/T7_2026-08-13_v3_1_u_repair_cycle2_implementation_delta_review.md`
and report exact identities, tests, adversaries, zero-call proof, incremental
item state and nonclaims.  This authorizes only Root T0 to create one exact
sentinel dispatch.  It does not accept V3.1-U science.

If a class-A blocker remains, return governance-compliant `REPAIR` only if a
bounded correction remains inside the same final repair before sentinel
dispatch; after the one-shot sentinel is consumed, any failure must return
`ESCALATE / T0 ADJUDICATION REQUIRED` rather than repair cycle 3.

No sentinel, official run, V3.2 or global GREEN in this review.
