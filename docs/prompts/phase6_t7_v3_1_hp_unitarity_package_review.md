# Formal T7 prompt — V3.1-U replacement-package review

You are the existing formal SchWO T7 task.  Review this package read-only and
independently.  Do not edit science/config/prompt/package bytes, run a radial,
AP or Wolfram solve, create an evidence root, dispatch T4, or start V3.2.

## Frozen candidate

```text
configs/phase6_v3_1_hp_unitarity_replacement_package.json
  decde34bcdc90db0bc69be446357e306cfe942c84a8d575902eddaa7d1ff6877
docs/phase6_v3_1_hp_unitarity_replacement_design.md
  243f312182528b10a895cef679d195dcedfd97d7a2e02e388449ae3af313dd28
docs/prompts/phase6_t4_v3_1_hp_unitarity_replacement.md
  a1bffc42a381489dadd483edf2e6952d97ce236fbad3fe9ca82eb8b0ca862945
docs/prompts/phase6_t7_v3_1_hp_unitarity_review.md
  8822263a09bde6d5b0bd3b2c00117eb5ac1648ae9f804a4e71380b27fa90cb82
```

Gate ID:
`phase6_v3_1_hp_unitarity_deficit_replacement_v1`.

This candidate follows formal T7's terminal predecessor verdict
`ESCALATE/FAIL / ESCALATE / T0 ADJUDICATION REQUIRED`.  Root T0 selected
governance option 2.  It must be a distinct replacement gate, not repair cycle
3, an r3 retry, threshold relaxation, protected-backend edit or V3.2.

Read fully: `project.md`, current status/T0/T4/T7 handoffs, liveness protocol
and verdict template, the final predecessor T7 archive, all four candidate
files, every package-bound V3.0 authority and source, the immutable r3 failure
root, and the cycle-2 AP diagnostic if still readable.  Rehash every package
binding and all seven protected files.

## Required independent checks

1. Recompute the representability diagnosis from original decimal strings and
   determine whether it causally supports a high-precision S-deficit route
   without converting the predecessor failure into PASS evidence.
2. Verify the exact 496-mode domain, all 16 threshold IDs/operators/values and
   five certificate IDs are unchanged.  Decide whether substituting a fresh
   independent high-precision S-route operand only in the already-frozen
   `Gamma_flux<1e-8` branch preserves the scientific meaning of `Gamma_S`.
3. Prove that the selector reads only direct Route-A horizon flux, is identical
   to the frozen threshold branch, cannot favor a residual, and freezes its
   complete 496-entry route map before Route U.
4. Check the signed-exponent precision formula, 30-digit margin, three-node
   ordering and two stricter admission limits for low, boundary and extremely
   small Gamma.  Identify any under-resolution, circularity or post-hoc path.
5. Verify Route-U's numerical-call graph is genuinely independent from Route A
   and all protected modules.  Shared analytic equations/conventions and shared
   implementation bytes with Route B must be disclosed; Route-B record/cache
   reuse must be impossible.
6. Verify the graph `496/9920 + N_U/3N_U + 102/458 + 23`, all threshold branches,
   numerical/convention budgets, provenance and five-certificate semantics are
   complete and independently rebuildable.
7. Check the fresh-root, failure/resume, source-start/source-end, permission,
   preflight, negative-test and resource-stop protocols.  Confirm r3 cannot be
   reused or promoted.
8. Check allowed files are minimally sufficient and every frozen/protected file
   is excluded.
9. Confirm the future science-review prompt starts a new gate review and cannot
   itself authorize V3.2 or global GREEN.

Use the mandatory dual-axis verdict and complete finding schema.  New desirable
work outside this package is follow-up debt unless it directly invalidates the
package claim.

If and only if the unchanged candidate is sufficient for bounded T4
implementation, preflight and conditional official execution, return exactly:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: NOT_ASSESSED
GATE_LABEL: ACCEPT GREEN / V3.1-U REPLACEMENT PACKAGE READY FOR T4
```

This is package readiness only; V3.1-U science remains `NOT_ASSESSED`.  If a
class-A defect exists, return `REPAIR` with all nine blocker fields.  If the
T0 redesign choice itself cannot be made coherent under frozen science, return
`ESCALATE` with exact evidence.  Do not dispatch downstream work.

Persist a durable T7 package-review archive and update only
`docs/handoffs/T7_current.md`; return their SHA-256 identities to T0.
