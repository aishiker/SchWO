# T4ae — Equivalence-Preserving Methods Gate

Work only in existing T4 task
`019f5fa6-1288-7c01-8a87-4c4370cf5517`. Do not create or replace a task.
All code design, implementation, modification, and performance-validation
logic must use `gpt-5.6-sol/ultra`.

## Start Gate

Before any write or computation, fully read and verify every source and
identity required by:

```text
docs/superpowers/specs/
  2026-07-24-t4ae-equivalence-preserving-methods-prequalification-design.md
docs/superpowers/plans/
  2026-07-24-t4ae-equivalence-preserving-methods-gate.md
```

Require the exact five-file candidate identity supplied by T0 and exact
matching `REVIEW GREEN / T0 REPAIR PACKAGE APPROVED`. Stop on any identity,
scope, artifact, source, test, or provenance mismatch.

## Required Execution

1. Generate the clean legacy baseline before editing code.
2. Profile stages and write focused TDD RED tests.
3. Implement the exact typed interface, legacy adapter, immutable cache,
   certified dense reuse, deterministic bounded executor, atomic resume, and
   provenance contracts in the exact allowed paths.
4. Preserve all current scientific conventions, solver tolerances,
   modes/frequencies/points, image resolution, independent checks, and
   fail-closed Q018 behavior.
5. Run focused tests and Ruff; create the exact fourteen-path implementation
   commit before optimized benchmark output.
6. Run the exact five existing frequencies and full `241x241` image
   regression; record wall/user/system CPU, peak RSS, solve/cache counts,
   stage profiles, environment, and all hashes.
7. Enforce every exact, floating, residual, image, performance, deterministic
   ordering, cache-rejection, and checkpoint-fault gate.
8. Run fresh focused/Ruff/full tests, `git diff --check`, exact
   commit/worktree/source checks, and forbidden-output searches.
9. Update only allowed T4 coordination records and return only to T0.

Only after code, tests, exact implementation commit, source/config identity,
and resume contract are frozen, if the remaining work is merely a long
benchmark run, orchestration may use `gpt-5.6-terra/medium`. Any subsequent
code change returns to `gpt-5.6-sol/ultra`, creates a new identity, and
invalidates incompatible checkpoints. Agent reasoning never changes solver
tolerances.

## Exact Decision

Return exactly one:

```text
GREEN / EQUIVALENCE-PRESERVING METHODS GATE READY
YELLOW / EQUIVALENCE-PRESERVING METHODS EVIDENCE INCOMPLETE
RED / EQUIVALENCE-PRESERVING METHODS GATE INVALID
```

Report exact implementation identity/blobs, benchmark identities, all
equivalence/performance maxima, resource records, cache/solve counts,
determinism/fault results, tests, scope, and non-claims.

Do not start T7ch, T8, any new frequency, production, plot, fixture,
Kirchhoff, paper-style output, or GitHub action.
