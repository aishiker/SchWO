# T4av — Scientific-Array Projection Repair

Work only in existing T4 task
`019f5fa6-1288-7c01-8a87-4c4370cf5517` using
`gpt-5.6-sol/high`. Never use `max` or `ultra`. Do not create a task,
subagent, proxy or descendant.

## Start Gate

Fully read project/status/current T0/T4/T7/T8 handoffs and the complete
T4ar–T4av design/plan/prompts and T7cu–T7cy decisions. Read the live driver,
consumed T4au launcher, T4au Phase-A checkpoint/preflight and complete
consumed PID-60188 Phase-B root.

Require the exact candidate supplied by root T0 and exact:

```text
REVIEW GREEN / T0 SCIENTIFIC ARRAY PROJECTION REPAIR APPROVED
```

Freshly verify all frozen identities and guards before edits.

## Preserve The Consumed RED

Reconstruct and freeze:

- T4au Phase-B root, 820 files/117 directories/531568940 bytes and root-T0
  canonical index `2afbf8e6...c4c9`;
- setup/request/source/input/reference/prelaunch/running/receipt/streams/
  supervisor failure/failure audit/checkpoint;
- child `60188`, exit64/no signal/exact wait/reaped/PG-empty;
- exact array-order stderr and empty stdout;
- no pair, matching audit0 and consumed authorization;
- every preceding failed/prelaunch-only/Phase-A root and lock;
- canonical four, eight later absences and all external/process/transient
  guards.

Never mutate, chmod, unlock, rename, delete, populate, repair, reuse or
promote an old root or artifact.

## Independently Prove The Defect

Directly load the immutable legacy/golden NPZ name order and inspect the
consumed launcher plus exact-legacy `_compute_frequency()`. Prove the exact
23-key producer-full versus 22-key scientific-only comparison described in
the frozen design. Do not claim the unpersisted computed array or warning
values.

## Phase A Only

1. Create one fresh unique no-overwrite evidence root and preserve exact old
   driver/launcher/checkpoint/preflight/RED-root identities.
2. Publish one new launcher derived from the consumed T4au launcher; leave
   the old launcher byte-identical.
3. Add only two exact literal mapping surfaces:
   `producer_full_mapping` and `audit_scientific_mapping`.
4. Producer requires the exact frozen 22 scientific names followed by one
   final scalar Unicode JSON `metadata_json`.
5. Audit requires exactly the same 22 scientific names and no metadata.
6. Compare exactly those 22 arrays against legacy and golden with unchanged
   order/dtype/shape/C-byte/finite-mask requirements.
7. Do not sort or use a set/prefix/glob/regex/fallback to normalize names.
8. Keep producer metadata in the output NPZ but do not treat its
   attempt-specific bytes as a scientific array.
9. Bind the new launcher and literal surface dataflow throughout the
   artifact-local durable driver.
10. Preserve the exact single `_compute_frequency()` call, warning capture,
    structured/raw warning gates, mode0400 pair lifecycle and every
    source/input/reference/runtime/fixed-identity/canonical/process guard.
11. Add and run every positive/negative/static/dataflow fixture from the
    design.
12. Build and validate fresh prospective producer and matching-audit
    requests without launching either child.
13. Freeze launcher/driver/diffs/functions/call sites/tests/requests and all
    old/new guards.

Phase A must execute zero launcher main, `_compute_frequency()`,
reconstruction producer/audit, optimized runner, official audit, solver or
matrix.

Diagnosed pre-execution zero-science artifact-local helper/fixture/manifest/
path/serialization/bookkeeping/ordinary-test defects must be preserved in
immutable failed roots, minimally corrected in fresh roots and continued
until checkpoint. Blind retry is forbidden.

Only complete PASS returns exact:

```text
CHECKPOINT / SCIENTIFIC ARRAY PROJECTION CONTRACT FROZEN
```

Report old/final driver and launcher hashes/diffs, exact projection
functions and literal call sites, all tests/rejections, prospective
producer/audit requests, consumed RED evidence, runtime/source/input/
reference/legacy/golden/warning/witness/canonical/absence/process/
transient/external guards, and science counts all zero. Then stop for
root-T0 audit.

## Later Boundary

Only a separate later root-T0 authorization permits one new producer and,
after full producer PASS, one matching zero-solver audit. Any launched
failure consumes that authorization and forbids retry. No old result,
request, root, reference view or pair may be reused.

No scientific source/runtime/dependency/complete-input/reference/
fixed-identity/criterion/order change, canonical write, Phase C, optimized
matrix, new frequency, production/plot/fixture/Kirchhoff/paper/GitHub/
T7ch/downstream work is authorized.
