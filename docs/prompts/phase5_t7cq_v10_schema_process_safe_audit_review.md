# T7cq — V10-Schema And Process-Safe Audit Repair Review

Work only in existing T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed` using
`gpt-5.6-sol/high`. This is strictly read-only. Do not create a task,
subagent, proxy, or descendant.

## Required Reading

Fully read:

- project/status/current T0/T4/T7/T8 handoffs;
- exact T4an design, plan, T7cq prompt, and T4an prompt supplied by T0;
- T4ai through T4am packages and decisions;
- all immutable failed helper/audit roots and T4al helper source;
- frozen v10 manifest and driver code that creates it;
- v10/runtime/complete-290/runner, consumed witness, canonical/absence/
  external/process/transient state.

Verify exact candidate/ref/parent/four added paths, commit/worktree
blob/SHA/size identities, and `git diff --check`. Review no superseded
candidate.

## Independent Gates

1. Reconstruct the exact T4al helper/audit/execution boundary and confirm
   one child exit1, pre/post equality, empty audit root, absent execution,
   zero execute/driver/preflight/science, and lost stderr.
2. Preserve the epistemic limit: exact historical exception/first thrown
   cause remains unrecoverable.
3. Parse T4al helper AST and prove `collect_before()` calls
   `v10_snapshot()` before `process_snapshot()`.
4. Prove T4al source compares `value["raw_records"]` directly with integer
   `1041`.
5. Independently load immutable v10 bytes and prove `raw_records` is a
   1041-entry list, not an integer.
6. Prove all records have exactly path/sha256/size, unique code-point-sorted
   paths, lowercase 64-hex hashes, and nonnegative integer sizes.
7. Recompute generator-exact canonical raw-list index with the live
   driver's explicit `ensure_ascii=False` exactly
   `f0d3a4a0...ad27`; do not infer serializer equality from the current
   all-ASCII paths.
8. Independently run the frozen non-ASCII in-memory discriminator and
   require false-mode digest `b4e06758...a3eee`, true-mode digest
   `fd74c550...c46343`, unequal bytes/hashes, and rejection of true mode
   as the v10-index serializer.
9. Verify capture count/index 29/`8aacda7e...a635`, tests 23, complete-290
   `77927103...fd24`, runtime occurrences 9, science 0/0/0.
10. Confirm the new helper requires list type and `len(...) == 1041`; it
   never compares a container directly with an integer.
11. Confirm the exact v10 validator rejects scalar/mapping, length,
    duplicate/order/path/alias/schema/hash/size/index/capture/test/runtime/
    complete-290/science drift.
12. Confirm all validator fixtures are in-memory and zero-science.
13. Confirm static dataflow gives the v10 raw-index function only
    explicit `ensure_ascii=False`, helper stdout/replay only
    omitted/default `ensure_ascii=True`, and no ambiguous shared or
    argument-controlled serializer.
14. Confirm direct exact root constants, frozen file-based launcher, and
    no heredoc/stdin/`-c` or implicit root construction.
15. Confirm variable PID is validation-only and excluded from canonical
    payload/digest.
16. Confirm structured unique self/direct-launcher ancestry and
    token-aware checks for all other processes.
17. Confirm malformed/duplicate/missing/wrong-parent/cycle/alias/
    unrelated-runner cases fail closed while harmless marker prose does
    not trigger broad-substring rejection.
18. Confirm process fixtures are complete and zero-science.
19. Confirm stable audit-root projection does not weaken full frozen
    scientific/runtime/input/canonical guards.
20. Confirm pre/post equality and execution absence precede publication.
21. Confirm raw stdout/stderr/return/signal/wait/reap and all common records
    persist after equality on both success and failure.
22. Confirm mutually exclusive success/failure indexes and
    non-self-referential terminal manifests.
23. Confirm failure consumes authorization, forbids execute, returns HOLD,
    and has no local retry.
24. Confirm success requires root-T0 full audit and identical file-based
    no-write replay before separate execute authority.
25. Freshly recompute driver/runner/v10/runtime/complete-290/witness/
    canonical/absence/external/process/transient/link guards.
26. Confirm T4am was never dispatched and T4 remains idle.
27. Confirm no execute, driver edit/preflight, science, witness, matrix,
    canonical write, standard relaxation, agent, max, ultra, or downstream
    action is authorized.

Any incorrect v10 container/schema assumption, conflated serializer
surface, index ambiguity, missing negative fixture, historical-cause
overclaim, broad process substring logic, variable PID in replay payload,
missing durable failure branch, implicit retry, or scope expansion is a
blocker.

## No Writes Or Execution

Do not edit files, artifacts, status, handoffs, refs, or environment. Do
not execute any helper/launcher/audit/replay/preflight/science/witness/
matrix command or dispatch T4/downstream work.

## Exact Decision

Return exactly one:

```text
REVIEW GREEN / T0 V10-SCHEMA PROCESS-SAFE AUDIT REPAIR APPROVED
REVIEW YELLOW / T0 V10-SCHEMA PROCESS-SAFE AUDIT CHANGES REQUIRED
REVIEW RED / T0 V10-SCHEMA PROCESS-SAFE AUDIT REPAIR INVALID
```

Then report exact identity, independent evidence, and bounded blockers.
GREEN authorizes only T0 fresh verification and a later T4an Phase-A0
dispatch.
