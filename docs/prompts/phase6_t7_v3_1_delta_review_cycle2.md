# Phase 6 V3.1 — formal T7 bounded delta review after repair cycle 2

Date frozen by T0: 2026-08-11

Scientific stage: `Phase 6 / V3.1`

Expected artifact revision: `r3` / `artifact_rev=3`

You are the existing formal T7 independent review task. Root T0 may dispatch
this prompt only after formal T4 returns a terminal cycle-2 result and T0
supplies its exact root/evidence identities.

Read the liveness protocol/template, initial review, cycle-1 package/delta
archives, approved cycle-2 package and package-review archive, T4 terminal
handoff/archive, terminal candidate and every bound source/runtime/input
identity.

This is delta review 2. Recheck only:

1. failed item `v31_route_a_first_required_node`;
2. failed item `v31_complete_producer_route_coverage`;
3. whether the repair damaged any frozen passed invariant;
4. protected/source/package identities;
5. compact JSONL and complete source-ledger class-C repairs.

Do not reopen a passed item without exact causal evidence that cycle 2 changed
its bytes, dependency or invariant.

For a successful `r3`, independently rebuild from raw records:

- exact 496 Route-A keys and 9,920 logical nodes, one protected call each;
- deterministic `n_aux/r_aux`, original requested matching radii and all
  continued-match raw/log operands;
- exact 102 AP keys and 458 AP nodes with separate implementation provenance;
- exact 23 fresh external odd BHPT records;
- all amplitudes, signed currents, positive fluxes, direct/S Gamma and log
  routes;
- all 16 frozen threshold outcomes/extrema and 248 parity pairs;
- exactly five ordered certificates and separate uncertainty budgets;
- JSONL, source start/end identities, manifest, permissions, checkpoint
  integrity and deterministic in-place/distinct-copy reload;
- full-suite result and protected hashes at start/end.

Run only read-only reconstruction and independent focused tests. Do not rerun
the producer or create science.

Accept only with:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: PASS
GATE_LABEL: ACCEPT GREEN / V3.1 MODE GREYBODY EVIDENCE READY FOR V3.2
```

Common absolute phase remains an allowed `PARTIAL` nonclaim. V3.2,
full-domain V3, Li, finite-radius observers and global GREEN remain outside
this review.

If the same substantive blocker remains after this second repair, return:

```text
ADVANCE_DECISION: ESCALATE
CLAIM_STATUS: FAIL
GATE_LABEL: ESCALATE / T0 ADJUDICATION REQUIRED
```

No third repair is permitted. A genuinely new defect may block only with all
class-A fields and a direct causal chain to the current frozen claim. The
review is read-only except for T7 current and one new dated archive.
