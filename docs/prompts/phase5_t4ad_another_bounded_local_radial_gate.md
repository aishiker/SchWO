# T4ad — Another Bounded Local Radial/Q018 Gate

Work only in existing T4 task `019f5fa6-1288-7c01-8a87-4c4370cf5517`.
Do not create or replace a task. Normal execution uses `gpt-5.6-sol/high`.

## Start Gate

Before any edit or computation, fully read `project.md`, `status.md`, current
T0/T4/T7/T8 handoffs, the reviewed candidate design, both plans, all four
candidate prompts, T7cd evidence, immutable T8ar/T4ac sources, and earlier
bound evidence. Verify the exact candidate commit/blob/SHA identity supplied
by T0 and matching independent package-review GREEN. Stop on any mismatch.

The candidate authorizes exactly 55 literal failed-child frequencies, 239,120
classification records, unchanged lmax seed rule/max 360, both sectors, every
`ell=2..ell_max`, and the exact eight points. It is not a uniform/full grid or
automatic/recursive selection.

## Allowed Implementation Paths

Create one pre-compute commit changing exactly:

```text
scripts/phase5_another_bounded_local_radial_gate.py
src/schwgw/numerics/q018_tablei_another_bounded_local_envelope.py
src/schwgw/numerics/radial_solver.py
tests/physics/test_q018_production_integration_design.py
tests/physics/test_radial_solver.py
```

Other allowed writes are only:

```text
runs/phase5/fig5_fig6_another_bounded_local_radial_gate/**
docs/phase5_another_bounded_local_radial_gate.md
status.md
docs/handoffs/T4_current.md
docs/handoffs/archive/T4_2026-07-20_pre_t4ad_another_bounded_local_radial_gate.md
```

Preserve unrelated worktree changes.

## Required Work

1. Independently re-derive the exact 55 failed-child midpoints, tokens, lmax
   windows, and 239,120 keys; require all five phase failures and no extra.
2. Add focused RED tests before implementation.
3. Classify all records and freeze a pre-adapter classification snapshot.
4. Measure sector-aware structured transitions; never assume their count.
5. Direct-oracle every measured transition with unchanged residual,
   normalization, log-derivative, condition, finiteness, and sensitivity gates.
6. Generate an exact sector-aware literal envelope and opt-in adapter
   `q018_tablei_another_bounded_local_transition`; default-covered modes make
   zero oracle calls and every mismatch fails closed.
7. Run focused tests and Ruff, then create the exact five-path implementation
   commit before final-adapter snapshot/output.
8. Produce exactly 55 atomic complete/PASS checkpoints plus four roots. Reuse
   only exact complete matching checkpoints; never recompute one.
9. Run direct-oracle preflight, focused tests, Ruff, fresh full pytest, exact
   scope/source checks, `git diff --check`, warning classification, and
   forbidden-output searches.
10. Archive/update only allowed coordination records and return to T0.

## Exact Decision

Return exactly one:

```text
GREEN / ANOTHER BOUNDED LOCAL RADIAL GATE READY
YELLOW / ANOTHER BOUNDED LOCAL RADIAL EVIDENCE INCOMPLETE
RED / ANOTHER BOUNDED LOCAL RADIAL GATE INVALID
```

Include commit/parent/five blobs, 55 checkpoint hashes, classification and
measured transition counts, both snapshots, oracle/preflight maxima, roots,
tests, scope, and non-claims. Do not start T7ce or T8as.

## Stop Rules

No threshold relaxation, lmax extension, automatic/recursive midpoint,
uniform/full grid, amplification, production, plot, fixture, Kirchhoff,
paper-style output, new/replacement task, or GitHub action. Genuine failure is
not a capacity interruption.
