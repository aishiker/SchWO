# T4ap — Split-Root Fixed-Primary Matrix Input-View Repair

Work only in existing T4 task
`019f5fa6-1288-7c01-8a87-4c4370cf5517` using
`gpt-5.6-sol/high`. Do not use `max` or `ultra`. Do not create a task,
subagent, proxy, or descendant.

## Start Gate

Fully read project/status/current T0/T4/T7/T8 handoffs, the exact T4ap
design/plan/T7cs prompt, frozen T4ai/T4ao packages, driver, runner,
relocated witness, complete-290 manifests, runtime overlay, current/archive
T7 records, canonical output, and absence/process/transient evidence.

Require the exact candidate supplied by root T0 and exact:

```text
REVIEW GREEN / T0 SPLIT-ROOT FIXED-PRIMARY MATRIX INPUT VIEW APPROVED
```

Freshly verify every identity. Earlier package review is not authority.

## Preserve The Boundary

Verify and preserve:

- primary HEAD `45face32...`;
- old driver `ffe68716...e8d3`;
- runner `18e79435...a768`;
- complete-290 `77927103...fd24`;
- fixed primary identity `46403a00...6c48`;
- witness checkpoint `2c448a70...cde9`;
- current primary T7 handoff `ef05071f...f1c0`;
- archived T7ce record `4b801e46...90d1c`;
- canonical four exact identities and eight absent later files;
- no related process, lock, temporary, partial, or quarantine state.

The earlier matrix dispatches failed before sampling. Treat them as
unmaterialized and superseded; do not launch science under them.

## Phase A Only

1. In fresh no-overwrite durable evidence, snapshot the exact old driver and
   all before guards.
2. Modify only the artifact-local durable driver and solver-free tests.
3. Split the matrix path contract into explicit:
   canonical project/runner/source/gates/config/output,
   detached input-working root, runtime source/import surface, and durable
   control root.
4. Require matrix runner, imported `schwgw` modules, gates, config, output,
   and benchmark identity remain exact primary.
5. Allow cwd/cwd-root only as one fresh detached clean exact-`45face32`
   complete-290 input view below the authorized control root.
6. Interpret clean in two stages: require empty Git status on the base
   checkout before materialization, then require exactly the reviewed
   seven-path modified/untracked delta from the complete input view. Reject
   every unexpected status/path/content; do not require an impossible empty
   status after materialization.
7. Treat historical source/input manifest hashes
   `dfb23566...32a3` / `e2cbf431...f9d` as evidence only. Derive fresh
   manifest bytes with the new detached absolute path/dev/inode bindings,
   while requiring the exact three-field complete-290 contract
   `77927103...fd24`. Preserve the exact one-entry
   `t4af_matrix_source_manifest_v1` canonical-primary runner contract and
   introduce exact exhaustive/read-only
   `t4ap_matrix_input_manifest_v2` for all 290 detached inputs; do not retain
   the old non-exhaustive matrix-input schema.
   Route benchmark/runtime/source validation through the canonical project
   root and cwd/allowed-cwd/input-target validation through the detached
   input root. Add static/dataflow negatives for any surviving
   `project_root == cwd` collapse.
8. Require environment `PYTHONPATH` to contain the frozen overlay and exact
   primary `src`; detached `src` is forbidden.
9. Bind the archived T7ce source to detached target
   `docs/handoffs/T7_current.md`, while recording and preserving the
   different primary current handoff unchanged.
10. Require producer and matching official audit to bind the same split
    roots, source origins, manifests, runtime, command, and fixed identity.
11. Keep the unchanged runner's direct canonical atomic output transaction.
   Do not add output copy, promotion, or reinterpretation.
12. Add all required positive, negative, static, fault, lock, audit, and
    post-mutation tests from the frozen design.
13. Run exact Python-3.14 AST/Ruff/diff/test guards and one real
    zero-science split-root runner `--mode preflight --case matrix`.
14. Require preflight natural exit0, empty stderr, exact fixed primary
    identity, existing complete-pair audits PASS, `solver_started=false`,
    no canonical write, and all pre/post guards unchanged.
15. Recompute every manifest/index/capture/test count and freeze the new
    driver SHA.

Only complete PASS returns exactly:

```text
CHECKPOINT / SPLIT-ROOT FIXED-PRIMARY MATRIX INPUT VIEW FROZEN
```

Report old/new driver hashes and diff, all test families, real preflight
argv/cwd/env/streams/result, both root identities, module origins,
complete-290/source-target binding, current/archive T7 guards, witness/
runtime/canonical/absence/process/transient guards, and exact zero-science
counts. Then pause for root-T0 audit.

## Local Self-Remediation

Do not return HOLD merely for a diagnosed pre-execution, zero-science,
artifact-local helper, fixture, manifest, path, serialization, bookkeeping,
or ordinary test-maintenance defect.

Preserve each failed evidence root immutable, make the smallest diagnosed
correction in a fresh no-overwrite root, and continue the current authorized
Phase A until the exact checkpoint. There is no fixed correction-count
limit. Blind retry is forbidden.

Return HOLD before science only if a correction would require runner,
scientific source, runtime dependency, complete-input meaning, fixed
identity, canonical data, scientific criteria/order, destructive/global
state, or other authority beyond this package.

## Not Authorized

No matrix run/audit child, solver, canonical write, primary handoff
mutation/swap, detached runner/import/output, output promotion, source/
runner/runtime/complete-input/criteria change, retry/duplicate, new
frequency, task/subagent/proxy/descendant, max/ultra, cleanup/destructive/
global/network/install action, production/plot/fixture/Kirchhoff/paper/
GitHub/T7ch/downstream work.
