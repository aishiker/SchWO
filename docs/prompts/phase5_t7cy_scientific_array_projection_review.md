# T7cy — Scientific-Array Projection Package Review

Work only in existing T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed` using
`gpt-5.6-sol/high`. Never use `max` or `ultra`. Do not create a task,
subagent, proxy or descendant.

This is an independent read-only review. Do not modify files, refs,
artifacts, status, handoffs, permissions, environment or processes. Do not
execute any launcher, producer, audit, solver or matrix command.

## Identity Gate

Bind only the candidate supplied by root T0. Require exact parent:

```text
3eb0d28b400e2fe069ec77fcd1d55164bb806b96
```

and primary HEAD:

```text
45face32f52537ac4b8ab79cb1746d8ac78e9b82
```

Require exactly four added paths relative to parent:

```text
docs/superpowers/specs/2026-07-28-t4av-scientific-array-projection-repair-design.md
docs/superpowers/plans/2026-07-28-t4av-scientific-array-projection-repair.md
docs/prompts/phase5_t7cy_scientific_array_projection_review.md
docs/prompts/phase5_t4av_scientific_array_projection_repair.md
```

Verify candidate/ref/parent, commit/worktree bytes, blob/SHA/size and both
diff-checks. Reject every superseded or unreferenced identity.

## Independent RED Review

Reconstruct the consumed T4au Phase-B root from raw files. Verify:

- setup/request/source/input/reference/prelaunch/running/receipt/streams/
  supervisor-failure/failure-audit/checkpoint identities;
- PID/SID/PGID `60188`, exit64/no signal, exact wait/reap/PG-empty;
- exact empty stdout and 170-byte stderr
  `c4aaf029...acf0`;
- child detail is `scientific array name/order mismatch`;
- failure audit `valid=true` and exact post-child provenance guards;
- producer1, matching audit0, pair absent and authorization consumed;
- immutable tree 820 files, 117 directories inclusive, 531568940 bytes,
  zero link/special/non-one-nlink and root-T0 index
  `2afbf8e66149c857260dce7497137bc66ffc6bbb79db6590d3e5fde56a7bc4c9`;
- no live child, canonical drift or later output.

Do not substitute the supervisor's downstream generic `missing_path` for
the exact launcher cause.

## Independent Causal Review

Read the consumed launcher, exact-legacy `_compute_frequency()` and both
legacy/golden NPZs directly. Prove:

1. legacy/golden full order is the same exact 22 scientific names followed
   by `metadata_json`;
2. `scientific_arrays()` intentionally returns only the first 22;
3. every successful `_compute_frequency()` return appends
   `metadata_json` after the same 22 scientific entries;
4. producer passes that 23-key mapping to `exact_array_comparison()`;
5. the consumed comparison requires `list(computed) == legacy_order`,
   where `legacy_order` has 22;
6. the mismatch is deterministic and occurs before pair publication;
7. no unpersisted array/warning value is claimed as PASS.

## Repair Contract Review

Require all of the following:

1. Old launcher, driver, attempt root and every lock/root remain immutable.
2. New launcher distinguishes literal producer-full and audit-scientific
   surfaces; no boolean/env/argv/heuristic selection.
3. Producer requires exact 23-key full order with one final
   `metadata_json`; audit requires exact 22-key scientific order without it.
4. Metadata is scalar Unicode parseable JSON, excluded only as the exact
   final metadata entry, and never substitutes for a scientific array.
5. All 22 scientific names/order/dtype/shape/C-bytes and finite-mask gates
   remain exact against both legacy and golden.
6. No sorting/set/prefix/glob/regex/case/Unicode normalization or fallback
   can hide a missing, extra, duplicate or reordered name.
7. Producer and audit call the exact required literal surfaces.
8. New launcher/driver provenance is bound through request/prelaunch/
   running/final/control/pair/audit/replay.
9. Scientific call, warning capture/counts, output mode0400 lifecycle,
   source/input/reference/runtime/fixed-identity/canonical/process guards
   remain unchanged.
10. Exact Python3.14 Phase A is zero-science and stops for T0 audit.
11. Any later launched failure remains one-shot/no-retry.

Freshly verify T4au checkpoint/driver/launcher/preflight, runtime,
complete290, reference index, legacy/golden/warning, fixed identity,
relocated witness, canonical four, eight absences, external/process/
transient/link guards.

## Decision

If and only if every gate passes, return exact:

```text
REVIEW GREEN / T0 SCIENTIFIC ARRAY PROJECTION REPAIR APPROVED
```

Bind the exact candidate/ref/parent and report independently derived
evidence. State clearly that GREEN authorizes only root-T0 fresh
verification and possible later T4av zero-science Phase A, not launcher
execution or science.

Otherwise return YELLOW/RED with the smallest concrete bounded blocker and
required correction. Do not dispatch T4.
