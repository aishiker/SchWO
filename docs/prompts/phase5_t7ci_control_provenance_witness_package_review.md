# T7ci — Control-Provenance Witness Repair Package Review

Work only in existing T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed` as the same independent read-only
repair-package reviewer. Do not create or replace a task.

## Required Reading And Identity

Fully read:

- `project.md`, `status.md`, current T0/T4/T7/T8 handoffs;
- frozen T4ae design/plan/prompts and T4ae note/artifacts;
- the exact T4ae YELLOW evidence and original Codex event-log evidence;
- the exact four-file T4af candidate package supplied by T0;
- the committed runner and source/tests needed to assess isolation,
  checkpoint, output-path, and process-control feasibility.

Verify the supplied candidate commit, parent, exact four added paths, every
commit/worktree blob, and every SHA-256. Review only that identity.

## Independent Review

Check independently that:

1. T4ae YELLOW is correctly limited to the irrecoverable original
   `kM=1.58125` exit/stderr record and that no scientific/artifact defect is
   hidden.
2. The package never claims to reconstruct the original exit and does not
   substitute a later audit for the missing record.
3. The one witness is prospective, separately identified, isolated,
   write-disjoint from canonical artifacts, and excluded from scientific
   inputs and performance aggregation.
4. The package adds evidence rather than lowering any scientific,
   numerical, performance, provenance, test, or independent-review gate.
5. The detached exact-implementation snapshot and read-only input manifest
   are technically feasible with the committed runner's repository-relative
   paths.
6. The durable driver contract survives a Codex stream disconnect and
   records exact argv/cwd/PID/exit/signal/stdout/stderr/source/environment
   identity atomically and fail-closed.
7. Phase A cannot run a scientific solver and requires a fresh T0
   driver/source/preflight review before the witness.
8. Canonical pair hashes are guarded before and after every action; the
   witness cannot overwrite, replace, quarantine, or become a cache for a
   complete canonical pair.
9. Only the four absent original T4ae work units may run after witness PASS,
   one at a time under the durable driver.
10. Model-tier, anomaly, stop, test, scope, downstream, and non-claim rules
    are complete and auditable.

Independently inspect current process and filesystem state. Any ambiguity,
path alias to canonical output, implicit retry permission, weakened
criterion, or unbounded write scope is a blocker.

## No Writes

Do not edit status, handoffs, archives, implementation, artifacts, or
candidate files. Do not dispatch T4af/T7ch/T8, perform GitHub actions, or
repair the package.

## Exact Decision

Return exactly one project-defined package-review decision, followed by the
candidate identity, independent evidence, and bounded change requests if
any:

```text
REVIEW GREEN / T0 REPAIR PACKAGE APPROVED
REVIEW YELLOW / T0 REPAIR PACKAGE CHANGES REQUIRED
REVIEW RED / T0 REPAIR PACKAGE INVALID
```

Return only to T0.
