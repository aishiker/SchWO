# T7cp — Process-Safe Audit And Failure-Capture Repair Review

Work only in existing T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed` as an independent read-only
reviewer. Use `gpt-5.6-sol/high`. Do not create a task, subagent, proxy, or
descendant.

## Required Reading

Fully read:

- `project.md`, `status.md`, and current T0/T4/T7/T8 handoffs;
- the exact four-file T4am candidate supplied by root T0;
- frozen T4ai through T4al designs, plans, reviewer prompts, executor
  prompts, and exact decisions;
- all prior immutable failed-helper roots and every file in them;
- T4al helper source and empty audit root;
- live driver, v10/runtime/complete-290/runner evidence, complete consumed
  witness, unusable isolated pair, canonical/absence/external/process/
  transient state.

Verify candidate commit, parent, ref, exact four added paths,
commit/worktree blobs, SHA-256 values, sizes, and `git diff --check`.
Review only that exact identity.

## Independent Review Gates

Check that:

1. T4al helper/audit/execution identities and exact counts independently
   match the frozen failure boundary.
2. The only audit-only child exited `1`; helper/audit/control pre/post
   inventories were equal; audit root remained empty; execution remained
   absent; no execute/driver/preflight/science occurred.
3. Exact stderr and exact dynamic cause are unrecoverable because the
   success-only launcher discarded them before durable publication.
4. The package never promotes the parent-marker hypothesis to fact.
5. Independent source review confirms a reachable broad-substring process
   scanner with no ancestry semantics.
6. All prior helper and audit roots remain immutable, non-executable,
   non-reusable, and must not be populated or chmodded.
7. Driver `057bb24b...59a9`, runner `18e79435...a768`, v10/runtime/
   complete-290, consumed witness, canonical four, absent later units,
   external object, and process/transient/link guards remain exact.
8. T4am uses fresh helper/audit/reserved-execution direct-child roots and
   freezes helper plus launcher before the first attempt.
9. Helper and launcher use direct full exact AST constants; implicit root
   construction and aliasing reject.
10. Launcher is invoked from a frozen file, never stdin/heredoc/`-c`.
11. Helper root and helper/launcher files are not edited after first
    launch; the audit-record root receives only reviewed attempt records
    after no-write equality.
12. Audit-record stable projection remains exactly
    path/dev/inode/uid/gid/is-directory/non-symlink/direct-child and
    excludes only mutable launcher-publication fields.
13. Scientific/runtime/input/canonical/preservation/execution guards are
    not weakened by that stable projection.
14. Canonical JSON is runner-compatible default `ensure_ascii=True`,
    sorted compact UTF-8, `allow_nan=False`, no newline.
15. Launcher PID is validation-only and never enters canonical payload or
    replay digest.
16. One structured `pid/ppid/pgid/sid/state/command` snapshot is parsed
    fail-closed; self and exact direct launcher are uniquely verified.
17. Only exact helper/direct-launcher rows receive ancestry exemption.
18. Every other process is checked through normalized executable/argument
    tokens rather than whole-command substring search.
19. Missing/duplicate/malformed IDs, wrong PPID, cycles, aliases, unrelated
    exact runner/control paths, and ambiguous tokens reject.
20. Harmless marker prose does not become a broad-substring false positive.
21. Zero-science positive and negative classifier fixtures cover all those
    branches without launching the real runner/audit/solver.
22. Pre/post no-write equality and execution absence are required before
    any attempt record is published.
23. After equality, raw stdout/stderr, return code/signal, wait/reap,
    argv/cwd/env, inventories, and guards are persisted on both success and
    failure.
24. Empty streams remain exact zero-byte binary records.
25. Success and failure branches are mutually exclusive and use separate
    index/terminal filenames.
26. A nonzero/signal/stderr/malformed-output failure produces durable
    failure evidence, consumes authorization, forbids execute, returns
    HOLD, and cannot locally retry.
27. Both terminal manifests are non-self-referential and bind their
    records index plus exact filename set without claiming their own
    hash/stat/post-close timestamp.
28. A success checkpoint requires root-T0 full read/AST/dataflow audit and
    one frozen-launcher `--replay-only` run with byte-identical
    stdout/digest, empty stderr, exit zero, zero writes, and absent
    execution root before any execute authorization.
29. T4am Phase A0 authorizes no execute, driver edit/preflight, science,
    witness, matrix, pair reuse, canonical write, destructive action,
    downstream work, or GitHub action.
30. T4 uses sol/high only; max and ultra are forbidden.
31. No task, subagent, proxy, or descendant is created.

Any epistemic overclaim, missing failure capture, variable PID in canonical
payload, broad substring scanner, unjustified ancestor exemption, weakened
unrelated-process rejection, mutable launcher, non-durable failure branch,
self-referential terminal record, hidden retry, or scientific-scope
expansion is a blocker.

## No Writes Or Dispatch

Do not edit files, artifacts, environment, status, handoffs, or candidate.
Do not execute any helper, launcher, audit-only, replay, execute, preflight,
witness, official audit, solver, matrix, or science. Do not dispatch
T4/T7ch/T8 or perform GitHub actions.

## Exact Decision

Return to root T0 with exactly one:

```text
REVIEW GREEN / T0 PROCESS-SAFE AUDIT AND FAILURE-CAPTURE REPAIR APPROVED
REVIEW YELLOW / T0 PROCESS-SAFE AUDIT AND FAILURE-CAPTURE CHANGES REQUIRED
REVIEW RED / T0 PROCESS-SAFE AUDIT AND FAILURE-CAPTURE REPAIR INVALID
```

Then report exact candidate identity, independent evidence, and bounded
blockers if any.
