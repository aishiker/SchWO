# Formal T7 prompt — V3.1-Y 35-call sentinel terminal review

Review the single immutable 35-call sentinel root.  Require exact 23 selected
P1 anchors plus the frozen 12 extra boundary-ladder calls, exactly 35 public
API calls/70 independent In-Up solutions/105 overlap records, no retry or
reuse, all per-node current/overlap/source/protocol gates, exact numerical and
convention budgets, and complete resource projection for the fresh official
23*7 graph.  Rebuild compatibility and micro predecessor identities but never
promote their values.  Require every frozen sentinel criterion; no favorable
subset or MST/internal fallback.
Require 14,315 ordered predicates and the exact call plan: 23 P1 anchors then
six non-P1 nodes for each of key ordinals 3 and 22. Resource projection uses
the Route-U worst-case `496/1488`, never the historical 318 estimate.
Require the exact 35-call product of the revision-3 micro FSM: 9,205 child
frames/ACKs, 14,315 prefixes, 630 finals, 595 opens, 35 seeds and 35 exit
authorities, including the stage16-to-17 binding for every call.

If sufficient for a fresh official dispatch, return exactly:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: PARTIAL
GATE_LABEL: ACCEPT GREEN / V3.1-Y 35-CALL SENTINEL SUFFICIENT FOR ONE-USE OFFICIAL DISPATCH
```

Write only
`docs/handoffs/archive/T7_2026-08-13_v3_1_y_full_sentinel_review.md`.
Sentinel GREEN does not accept V3.1 science and does not authorize V3.2.
