# T4aq — Lazy-Oracle Loaded-Module Closure Repair

Work only in existing T4 task
`019f5fa6-1288-7c01-8a87-4c4370cf5517` using
`gpt-5.6-sol/high`. Never use `max` or `ultra`. Do not create a task,
subagent, proxy, or descendant.

## Start Gate

Fully read project/status/current T0/T4/T7/T8 handoffs, the exact T4aq
design/plan/T7ct prompt, T4ap/T4ai packages, the consumed matrix unit,
driver, runner, legacy `2.91875` sidecar, runtime overlay, complete-290
manifests, witness, canonical output, and absence/process/transient state.

Require the exact candidate supplied by root T0 and exact:

```text
REVIEW GREEN / T0 LAZY-ORACLE LOADED-MODULE CLOSURE REPAIR APPROVED
```

Earlier review is not execution authority.

## Preserve The Failure

Freshly verify and preserve:

- failed unit checkpoint `bbd8ae06...ecaf`;
- producer request/prelaunch/running/final and all streams;
- child `59291` exit1/no-signal/exact-wait/reaped/PG-empty;
- old driver `61c23880...d0bb`;
- runner `18e79435...a768`;
- primary HEAD `45face32...e9b82`;
- complete-290 `77927103...fd24`;
- fixed identity `46403a00...6c48`;
- witness checkpoint `2c448a70...cde9`;
- canonical four exact identities and eight absent later files;
- no related process, lock, tmp, partial, quarantine, link, or alias.

Do not mutate, chmod, rename, delete, populate, reuse, repair, or reinterpret
the failed sequence. The old matrix authorization is consumed.

## Phase A Only

1. Create one fresh unique no-overwrite durable evidence root.
2. Preserve the exact old driver and all before guards.
3. Freeze one regular, non-symlink, nlink-1, read-only file launcher.
4. Keep the primary scientific runner and every `src/schwgw` byte
   unchanged.
5. In the launcher, verify exact Python/runner/source/runtime/roots, load
   the runner as a named module without invoking `main`, retain the original
   `_loaded_module_identity`, and wrap only that function.
6. The wrapper must always call the original verifier first. Only afterward
   may it remove exactly:

   ```text
   schwgw.numerics.experimental
   schwgw.numerics.experimental.q018_rescaled_oracle
   ```

   from the module-set digest.
7. Require absent-pair raw 44 and exact-pair-present raw 46 states to
   normalize to exact count 44 and manifest
   `190176ee7707a7579f8f504168d310f1ea5a34cf1c98c493f63e121a16db81f5`.
   Reject a partial pair and every other module addition/removal.
8. Set logical `sys.argv` to the unchanged primary runner command and call
   runner `main()` once. Propagate its return/SystemExit exactly.
9. Extend only artifact-local driver/helper/launcher/tests so requests bind
   physical launcher argv separately from logical runner argv, launcher
   content/stat identity, exact two-name projection, and same producer/
   official-audit launcher.
10. Keep embedded runner `source_command`, runner SHA, benchmark identity,
    output transaction and scientific metadata unchanged.
11. Process guards must track the exact launcher child and reject unrelated
    runner/launcher/duplicate/alias/descendant states.
12. Add every positive/negative test in the frozen design, including exact
    original-verifier source checks, raw 44/46 manifests, normalized 44,
    extra `schwgw.cli`, partial pair, wrong origin/hash, launcher drift,
    physical/logical argv mismatch, producer/audit mismatch, and final/
    replay equality.
13. Run exact Python-3.14 AST/dataflow/Ruff/diff/targeted tests and one full
    zero-science synthetic preflight.
14. Run exactly one fresh real split-root `--mode preflight --case matrix`
    through the frozen launcher.
15. Require natural exit0, empty stderr, exact wait/reap, PG-empty, existing
    two complete-pair audits PASS, fixed identity `46403a...`, normalized
    loaded manifest `190176...`, `solver_started=false`, canonical unchanged,
    all eight later files absent, and zero residual state.
16. Recompute and freeze driver/launcher/test/preflight identities.

Only complete PASS returns exactly:

```text
CHECKPOINT / STABLE LAZY-ORACLE MODULE CLOSURE FROZEN
```

Report old/new driver hashes and exact diff, launcher identity and source,
raw/normalized module manifests and records, every test family, real
preflight argv/cwd/env/streams/result, complete inputs/runtime/witness/
canonical/absence/process/transient guards, and exact zero-science counts.
Then pause for root-T0 audit.

## Local Self-Remediation

Do not return HOLD merely for a diagnosed pre-execution zero-science
artifact-local launcher/helper/fixture/manifest/path/serialization/
bookkeeping/ordinary-test defect. Preserve each failed evidence root,
correct the exact diagnosis in a fresh root, and continue until checkpoint.
Do not blind-retry.

Return HOLD before science only if a correction would require changing the
primary runner, scientific source, runtime dependencies, complete-input
meaning, fixed identity, canonical data, scientific criteria/order, or
destructive/global state.

## Not Authorized

No producer, official audit, solver, witness, matrix run, canonical write,
old-attempt retry/reuse, sidecar rewrite, output copy/promotion, runner/
source/runtime/input/identity/criteria change, new frequency, task/
subagent/proxy/descendant, max/ultra, cleanup/destructive/global/network/
install action, production/plot/fixture/Kirchhoff/paper/GitHub/T7ch/
downstream work.
