# Formal T7 prompt — V3.1-Y official scientific review

Review the fresh official attempt-0001 root and rebuild it from immutable
Route A/U/B/C records.  Require exact `496/9920`, fresh dynamic
`N_U/3*N_U` with `0 <= N_U <= 496`, `102/458` and
`23/161/322/483` totality; all 16 frozen thresholds; all five certificates;
separate numerical/convention budgets; exact source/protocol/runtime
identities; independent odd/even AP routes; odd-only external role; and no
predecessor, compatibility, micro or sentinel value reuse.  Check all
scientific formulae, flux/S separation, parity probability/phase and external
translation.  Do not use Li figures as acceptance.

Rebuild ordered inventory SHA-256
`e4d09740c032c3c48ae43be56c1dd11512527b6266f5974f444302bb4afc45d8`
and apply only `direct log_Gamma_flux < log(1e-8)` after fresh Route-A
closure. Reject fixed 318/954 acceptance and every byte/hash/count/timing from
the failed-U denylist. Require 65,849 ordered official protocol predicates.
Require the exact 161-call product of the revision-3 FSM: 42,343 child
frames/ACKs, 65,849 prefixes, 2,898 finals, 2,737 opens, 161 seeds and 161
exit authorities, with every P17 and terminal manifest bound to its stage16
exit and durable lifecycle receipt.

If the bounded V3.1 gate is scientifically sufficient for V3.2, return:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: PASS | PARTIAL
GATE_LABEL: ACCEPT GREEN / V3.1-Y MODE GREYBODY SUFFICIENT FOR BOUNDED V3.2 ENTRY
```

`PARTIAL` is permitted only for explicitly classified nonblocking limitations;
every blocking frozen item must pass.  Write only
`docs/handoffs/archive/T7_2026-08-13_v3_1_y_scientific_review.md`.
This verdict is bounded V3.1 advance, not full-domain V3 or global GREEN.
