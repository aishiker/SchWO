# Formal T7 prompt — V3.1-Y external protocol package review

Gate: `phase6_v3_1_y_external_protocol_v1`  
Review: initial archive-only package review  
Task: formal T7 `019f5ed1-b421-7ec2-9bac-8d134855a1ed`

Use the review-gate liveness protocol and mandatory dual-axis verdict.  Read
and independently rehash the package, machine contract, frozen design, T4
analysis/archive, T0 final X adjudication, T7 final X review, all package
members, all V3.0 authorities, source/runtime identities, denylisted roots,
the exact 23-anchor derivation and seven protected radial sources.

Independently verify:

1. Y is a genuinely distinct protocol/authority graph, not X repair cycle 3;
2. no X/U science, successful subset, request, root, cache or dispatch is
   promotable;
3. exact 23 anchors, 496/9920, 318/954, 102/458, 23/161/322/483 and
   35/70/105 graphs are frozen without post-hoc selection;
4. method, precision, source snapshot, transformations, equations,
   conventions, 16 thresholds, five certificates and seven protected sources
   are unchanged;
5. Protocol A's frame/ACK state machine has explicit observer roles,
   parent-owned durability, per-event prefix ACKs without multi-event
   deadlock, parent-barrier/stage-edge semantics, leaf-specific diagnostics
   and no compound or symbolic acceptance;
6. the compatibility WLS is structurally zero-science and distinct from the
   science-capable WLS;
7. the machine predicate registry and compatibility matrix cover every known
   and latent cross-runtime/lifecycle surface, including the static
   `StringNormalize` diagnosis without rewriting historical runtime evidence;
8. exact-six implementation scope and all future prompts are executable and
   mutually consistent;
9. future authority paths are predeclared but future digests are supplied only
   by later one-use T0 dispatches;
10. compatibility/micro/sentinel/official roots are fresh, mutually exclusive,
    nonreusable, UTC-qualified and terminal on failure; resource/stop rules
    are complete.

Freeze passed/failed/partial/not-assessed inventories.  Every blocker must be
a complete class-A finding.  Do not modify the package or implementation, run
Wolfram/science, create dispatch/root, or start V3.2.

If ready, return exactly:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: NOT_ASSESSED
GATE_LABEL: ACCEPT GREEN / V3.1-Y EXTERNAL PROTOCOL PACKAGE READY FOR T6
```

Write only
`docs/handoffs/archive/T7_2026-08-13_v3_1_y_external_protocol_package_review.md`
and report its SHA-256.  Package GREEN authorizes only the exact-six
zero-science implementation.
