# Formal T4 prompt — implement V3.1-X external direct route

Gate: `phase6_v3_1_x_external_direct_route_v1`

Stage: zero-science implementation and preflight only

Work only in formal T4 task
`019f5fa6-1288-7c01-8a87-4c4370cf5517` after Root T0 sends this exact
identity-bound prompt and the formal T7 package review has returned
`ADVANCE_DECISION: ADVANCE`.

## Start gate

Read and re-hash completely:

1. `project.md`, the `status.md` current header and current T0/T4/T7
   handoffs;
2. `docs/review_gate_liveness_protocol.md`;
3. `docs/prompts/phase6_v3_master_prompt.md`;
4. all V3.0 domain/threshold/formula/phase/convention/external-anchor
   authorities;
5. `docs/phase6_v3_1_x_external_route_redesign_analysis.md`;
6. `docs/phase6_v3_1_x_external_direct_route_design.md`;
7. `configs/phase6_v3_1_x_external_direct_route_package.json`;
8. this prompt, both frozen T7 prompts and the formal T7 package-review
   archive named by the package;
9. the immutable 25-file BHPT snapshot and its authority;
10. all failed predecessor roots as denylisted immutable evidence only;
11. the seven protected radial sources.

Every identity must equal the package. If any member, source, protected file,
review authority or expected path differs, stop before writing implementation.

## Exact allowed implementation paths

Create or modify only:

```text
scripts/phase6_v3_1_x_bhpt_direct.wls
src/schwgw/validation/phase6_v3_external_direct.py
src/schwgw/validation/phase6_v3_mode_greybody_external_direct_replacement.py
scripts/phase6_v3_1_x_external_direct.py
tests/unit/test_phase6_v3_external_direct.py
tests/regression/test_phase6_v3_external_direct_publication.py
```

Do not edit the package/design/prompts, V3.0 authorities, predecessor
implementation, external base snapshot, seven protected radial sources,
status/current handoffs, or immutable roots.

## Required implementation

Implement the design literally:

- exact rational frequency construction and precision assertion, with the
  frozen `90/45/45 -> 120/60/60` schedule and selected 120-digit node;
- one public BHPT `NumericalIntegration` call returning independent `In/Up`
  per node; zero MST and zero SchWO radial calls;
- exact first-occurrence odd-only two-literal overlay, complete copied-snapshot
  inventory and the six frozen transformed hashes;
- exact ordered 23 keys, seven nodes/key, three overlaps/node, with official
  cardinalities `161/322/483`;
- sentinel graph exact `35/70/105` using all 23 selected nodes plus six extra
  nodes for fixed ordinals 3 and 22;
- minimal WLS amplitude/current serialization and Python-only derivation of
  redundant fields;
- exact Wronskian signs and V3-F02 translation;
- separate numerical/convention budgets and unchanged threshold IDs/values;
- no selection, fallback, retry, cache, predecessor/sentinel science reuse or
  missing-node acceptance;
- exact source/protected/runtime start/end ledgers, one-child supervision,
  wait/reap/process-group closure, O_EXCL/no-follow atomic publication,
  immutable success/failure terminals and single-use dispatch consumption;
- official whole-gate orchestration that recomputes fresh Route A/U/B/C, but
  does not run it during this turn.

Implementation must default fail-closed and expose pure validation/build
functions sufficient for independent T7 temp-root reconstruction.

## Tests and adversaries

Using TemporaryDirectory/fake child fixtures only, cover at least:

- exact domain/node derivation and totality;
- duplicate/gap/reorder/noncanonical/torn records;
- machine-precision frequency rejection;
- wrong/extra overlay byte, wrong occurrence, even-sector drift and loaded
  source drift;
- zero/MST/internal solver call proofs;
- amplitude signs, direct flux versus S separation and serialization-digit
  consistency;
- determinant/conditioning, boundary/match/nonfinite failures;
- numerical/convention budget separation and threshold identity drift;
- predecessor/sentinel reuse, fallback and favorable-subset attacks;
- dispatch replay, wrong root/hash/argv/environment, second writer, child
  failure/timeout/signal/orphan, source-end drift and unknown control files;
- immutable PASS and FAIL publication plus full manifest rebuild;
- exact sentinel and official resource projections.

Run exact CPython 3.14 focused and adjacent tests, Ruff check/format,
in-memory compile and `git diff --check`. Do not invoke Wolfram or any science
backend in tests or preflight.

## Hard stop

This turn must have:

```text
WolframKernel launches = 0
wolframscript launches = 0
solver/science calls = 0
dispatch files created = 0
real sentinel/official/staging roots created = 0
```

Do not dispatch T7. Return exact six implementation hashes, test reports,
temporary evidence identities, source/protected start/end hashes and the
checkpoint:

```text
CHECKPOINT / V3.1-X EXTERNAL DIRECT ROUTE IMPLEMENTATION READY FOR T7 DELTA REVIEW
```

No sentinel, official science, V3.2 or global GREEN is authorized.
