# T7cn — Split-Root Preservation Helper Repair Review

Work only in existing T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed` as an independent read-only
reviewer. Use `gpt-5.6-sol/high`. Do not create a task, subagent, proxy, or
descendant.

## Required Reading

Fully read:

- `project.md`, `status.md`, current T0/T4/T7/T8 handoffs;
- the exact four-file T4ak candidate supplied by root T0;
- frozen T4ai and T4aj design/plan/reviewer/executor prompts and their exact
  GREEN decisions;
- all three immutable failed-helper roots and every file in them;
- the live driver, T4ah v10 evidence, complete-290 contract, hermetic
  runtime, complete consumed T4ah witness, successful-but-unusable isolated
  pair, canonical/absence/external/process/transient state.

Verify candidate commit, parent, ref, exact four added paths,
commit/worktree blobs, sizes, SHA-256 values, and `git diff --check`.
Review only that exact identity.

## Independent Review

Check that:

1. The T4aj root contains exactly one helper SHA
   `7db5372f...ba9f`, mode `0400`, and no captures, manifest, snapshot,
   preflight, bytecode, or science.
2. Independent AST reconstruction gives direct project/control constants
   but a `JoinedStr` evidence root, and audit-only/execute counts are zero.
3. Both earlier T4ai failed roots remain exact and immutable.
4. The live driver remains `057bb24b...59a9`; no T4ai edit or preflight
   occurred.
5. T4aj authorization is exhausted and neither its root nor helper may be
   reused, executed, chmodded, repaired, deleted, renamed, or populated.
6. The candidate closes the prospective mode geometry bug: the immutable
   helper root is never reopened or used as the execution output root.
7. T4 must choose one fresh helper root and one distinct reserved absent
   execution root before helper publication.
8. Project/control/helper/execution assignments are each full exact
   `ast.Constant` strings; `JoinedStr`, `BinOp`, calls, implicit joining,
   formatting, and environment substitution are rejected.
9. The static gate is AST-semantic and explicitly rejects raw source
   substring counting as an acceptance test.
10. Exact argv/cwd/git-toplevel, helper `__file__`, direct-child geometry,
    containment, alias/link/writeability, candidate/ref/HEAD, and all
    evidence guards are complete.
11. Audit-only and execute call the same pure `collect_before()`, whose
    payload includes the reserved execution path and its absence.
12. Audit-only emits exact runner-compatible canonical bytes with default
    `ensure_ascii=True` and no trailing newline, empty stderr, exit zero,
    and writes nothing.
13. Phase-A0 captures are atomic/no-overwrite/fsynced and pre/post
    inventories are collected before capture publication.
14. T4 stops at
    `CHECKPOINT / T4AK SPLIT-ROOT PRESERVATION HELPER FROZEN`; T7 GREEN
    does not authorize execute mode.
15. Root T0 must independently read/AST-check and rerun audit-only before
    execute authorization.
16. Execute first recomputes the same payload and digest while the reserved
    root remains absent, then creates that root once and writes only there.
17. Snapshot and before-manifest semantics close exact bytes/hash/size/
    mode/inode/nlink/non-alias, atomic no-replace, reload, fsync, and final
    permission requirements.
18. Helper root remains untouched during execution; any partial execution
    root is immutable and no retry is implied.
19. The complete 2,566-file/582,625,016-byte witness ledger
    `c2182870...bb97`, eight mappings, v10/runtime/complete-290/runner,
    canonical four, absent units, external object, and process/transient/
    link/bytecode guards remain exact.
20. Preservation PASS may resume only the already reviewed T4ai
    artifact-local driver/test repair and one zero-science preflight.
21. The package authorizes no witness, scientific audit, solver, matrix,
    pair reuse, canonical write, scientific/runtime/input/threshold change,
    destructive action, agent, downstream work, or GitHub action.
22. T4 uses sol/high only; max and ultra are forbidden.

Any incomplete split-root geometry, implicit root construction, writable
checkpoint reuse, helper/audit execution before root-T0 approval, incomplete
durability/guard binding, scientific invocation, or weakened T4ai gate is a
blocker.

## No Writes Or Dispatch

Do not edit files, artifacts, environment, status, handoffs, or candidate.
Do not run any failed helper, audit-only helper, execute mode, preflight,
witness, official audit, solver, matrix, or science. Do not dispatch T4,
T7ch, T8, or perform GitHub actions.

## Exact Decision

Return to root T0 with exactly one:

```text
REVIEW GREEN / T0 SPLIT-ROOT PRESERVATION HELPER REPAIR APPROVED
REVIEW YELLOW / T0 SPLIT-ROOT PRESERVATION HELPER CHANGES REQUIRED
REVIEW RED / T0 SPLIT-ROOT PRESERVATION HELPER REPAIR INVALID
```

Then report the exact candidate identity, independent evidence, and bounded
blockers if any.
