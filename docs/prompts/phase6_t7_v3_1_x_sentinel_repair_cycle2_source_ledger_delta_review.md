# Formal T7 prompt — V3.1-X repair-cycle-2 implementation delta

This prompt activates after package approval and formal T6 exact-scope
zero-science implementation/preflight.  Perform an incremental archive-only
review in formal T7 task `019f5ed1-b421-7ec2-9bac-8d134855a1ed` with high
reasoning effort.  Recheck only the failed source-ledger boundary, protected
PASS invariants and exact implementation identities.

Mandatory checks:

- independently reconstruct the raw canonical JSON member order and Wolfram
  Association behavior that caused rc69;
- verify one exact six-field projection is applied to imported expected and
  independently rebuilt start/end records, with exact-key/type/cardinality and
  frozen list/context order checks;
- execute all key-order positives and missing/extra/duplicate/reorder/
  wrong-value/path/stat/inode/alias negatives;
- prove the micro operation has one fixed key/node and exits before every
  `ReggeWheelerRadial`/external API/solver/science branch;
- independently execute only the reviewed zero-science real-Wolfram
  preflight, if required by the package, and bind the exact transcript;
- verify fake-child self-confirmation and micro/full/official evidence reuse
  fail closed;
- verify exact implementation scope, unchanged direct core, source snapshot,
  graph/method/precision/domain/threshold/convention and seven protected
  sources;
- verify fixed, non-circular implementation-review, micro dispatch/root/review,
  full-sentinel attempt-0003 and official attempt-0001 authorities;
- verify the producer accepts only the fixed future micro-review path plus its
  later dispatch-supplied digest and exact `ADVANCE / NOT_ASSESSED / SOURCE-LOAD
  MICRO ... SUFFICIENT FOR ONE-USE FULL SENTINEL DISPATCH` envelope; stale,
  alternate, failure-envelope and 35-call sentinel labels must fail closed;
- verify no dispatch/root/science exists and both failed roots remain exact.

If closed, output exactly:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: NOT_ASSESSED
GATE_LABEL: ACCEPT GREEN / V3.1-X SOURCE-LEDGER REPAIR CYCLE 2 IMPLEMENTATION READY FOR ONE-USE MICRO DISPATCH
```

This consumes bounded repair cycle 2 and authorizes only a Root-T0 fresh
one-use source-load micro dispatch.  Any remaining implementation blocker is
`ESCALATE / T0 ADJUDICATION REQUIRED`; no repair cycle 3.

Write only
`docs/handoffs/archive/T7_2026-08-13_v3_1_x_sentinel_repair_cycle2_source_ledger_implementation_delta_review.md`
and report its SHA-256, tests, exact implementation hashes, frozen inventories
and nonclaims.  Do not create dispatch/root, run science, authorize full
sentinel/official, start V3.2 or claim global GREEN.
