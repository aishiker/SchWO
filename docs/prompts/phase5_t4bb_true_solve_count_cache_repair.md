# T4bb — True Solve-Count Cache Repair

Continue only in existing T4 task
`019f5fa6-1288-7c01-8a87-4c4370cf5517`. Use `gpt-5.6-sol/high`; never
max/ultra. Do not create tasks, subagents, proxies, or descendants.

Start only after root T0 supplies the exact reviewed candidate/ref/parent and
matching:

```text
REVIEW GREEN / T0 TRUE SOLVE-COUNT CACHE REPAIR APPROVED
```

The frozen design is authoritative:

`docs/superpowers/specs/2026-07-28-t4bb-true-solve-count-cache-repair-design.md`

## Phase A Authorization Only

Implement the real-execution cache dataflow and strict loaders using only the
bounded methods surfaces. Preserve the accepted matrix and every scientific
source/config/runtime/input/canonical identity.

Required zero-science evidence:

- direct `3668/3668`, limit `2567` reconstruction;
- direct T4ad `98/408/480=986` reconstruction;
- low-frequency `166 modes * 8 points = 1328 records`;
- projected actual online total `2516`;
- exact cache-hit versus solve dataflow and counters;
- strict cache admission/rejection fixtures;
- synthetic cache facades reproducing required scalar/diagnostic surfaces;
- producer/audit/replay physical/logical provenance equality;
- exact Python 3.14 focused/full tests, Ruff, diff/scope checks;
- one synthetic and one real zero-science preflight, each with
  `solver_started=false`, unchanged canonical outputs, and zero residual
  process/transient state.

Preserve every failed root immutable. For diagnosed pre-execution
artifact-local defects, repair minimally in a fresh no-overwrite root and
continue. Return to T0 before any scientific-source, runner, runtime, input,
threshold, tolerance, frequency, point, mode, `lmax`, or canonical change.

Do not run cache generation, producer, official audit, solver, or six-case
benchmark in Phase A. Stop exactly:

```text
CHECKPOINT / TRUE SOLVE-COUNT CACHE DATAFLOW FROZEN
```

Phase B and Phase C require separate root-T0 authorizations.
