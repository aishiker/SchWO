# Phase 6 V3.1 — formal T7 repair-cycle-2 package review

Date frozen by T0: 2026-08-11

Review class: read-only final bounded repair-package review

You are the existing formal T7 independent review task. This is not V3.1
scientific delta review 2 and authorizes no numerical execution by itself.

Read completely `project.md`, the current status/T0/T4/T7 handoffs, the review
liveness protocol/template, all V3.0/V3.1 frozen authorities, the initial and
cycle-1 T7 archives, the cycle-1 T4 blocker archive/sentinel, and every member
of the exact cycle-2 package supplied by Root T0.

Independently verify that the package:

1. addresses the same two frozen class-A blockers and no new acceptance item;
2. preserves all passed items, formulas, conventions, domain, selectors,
   16 thresholds, predecessor identities and seven protected radial bytes;
3. keeps every requested match radius and exact 20-node graph, with no
   favorable replacement by an observed passing farther node;
4. defines `n_aux/r_aux` solely from the predeclared geometric policy and
   performs the final match at the original requested radius;
5. has mathematically correct positive-rescaling/log-scale coefficient and
   transmission normalization, signed-current, direct-Gamma and independent
   S-route Gamma formulae;
6. keeps Route B code-independent from Route A and Route C external/fresh;
7. provides complete executable success/failure paths for exact
   `496/9920/102/458/23`, 16 thresholds and five ordered certificates;
8. fixes compact JSONL and complete start/end source ledgers;
9. permits iterative pre-official debugging only inside the reviewed V3-local
   algorithm and stops on any needed package/protected/threshold change;
10. has sufficient fixed sentinels, synthetic same-path publication,
    runtime/disk, checkpoint/resume, terminal immutability and full-suite gates;
11. correctly consumes final repair cycle `2 of 2`, forbids cycle 3, V3.2 and
    global GREEN, and requires escalation if the same blocker survives delta
    review 2.

Do not edit any package member, science implementation, config, artifact,
status or T0/T4 handoff. Do not run numerical science, implement the repair,
dispatch T4, or start V3.2. You may update only T7 current and one new dated
T7 archive.

Approve only with:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: NOT_ASSESSED
GATE_LABEL: ACCEPT GREEN / V3.1 REPAIR CYCLE 2 PACKAGE READY FOR T4
```

This accepts only the bounded final repair plan. Otherwise return
`REPAIR / NOT_ASSESSED` with complete class-A fields if the package is locally
repairable, or `ESCALATE / T0 ADJUDICATION REQUIRED` if its scope is
intrinsically invalid. Bind the exact package-manifest SHA and every member
hash in the verdict.
