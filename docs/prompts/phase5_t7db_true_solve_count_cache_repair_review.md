# T7db — True Solve-Count Cache Repair Package Review

Continue only in existing T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed`. Use `gpt-5.6-sol/high`; never
max/ultra. Read-only review only. Do not create tasks, modify files, or
dispatch T4.

Fully read project/status and current T0/T4/T7/T8 handoffs, T7ch final, the
accepted T4ae/T4ba sequence, immutable T4ad oracle evidence, and this exact
four-file package.

Independently verify:

1. candidate/ref/parent, exact-four scope, commit/worktree bytes, hashes,
   sizes, and both diff checks;
2. direct performance arithmetic: legacy/optimized `3668/3668`, limit
   `2567`, all non-solve performance gates and all scientific gates PASS;
3. exact T4ad counts `98/408/480=986`;
4. exact low-frequency geometry:
   two sectors, `ell=2..84`, 166 unique dense solves, eight points, 1328
   records;
5. projected online count
   `0+286+524+666+682+358=2516<=2567`;
6. that cache generation is separately reported scientific work, cache hits
   bypass the solver, and misses/fallbacks remain solves;
7. strict identity/content/schema/point/radius/tolerance/origin/consumer
   admission and fail-closed rejection;
8. Phase A zero-science, Phase B one-shot cache producer+audit, and Phase C
   fresh isolated six-case benchmark boundaries;
9. unchanged science, runner, runtime, inputs, criteria, canonical outputs,
   and no final-result or cross-identity cache.

Return exactly:

```text
REVIEW GREEN / T0 TRUE SOLVE-COUNT CACHE REPAIR APPROVED
```

only if all gates pass. Otherwise return YELLOW/RED with the smallest exact
blocker. GREEN authorizes only root-T0 fresh verification and possible later
T4bb Phase A, not cache generation or benchmark science.
