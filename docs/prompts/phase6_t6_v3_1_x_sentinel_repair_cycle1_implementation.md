# Formal T6 prompt — V3.1-X sentinel bounded repair cycle 1

This prompt activates only after formal T7 accepts the exact repair package.
Work in the existing formal T6 task
`019f5faf-bfdd-7aa3-80a9-1c9829daecac`.

Read completely and rehash: `project.md`; review-gate liveness protocol;
current status/T0/T6/T7 handoffs; V3.1-X package/design and all formal review
archives; failed sentinel dispatch/root; formal terminal review; the repair
package/design; and its formal package-review archive.  Stop without edits on
any identity mismatch.

Modify exactly four paths:

```text
scripts/phase6_v3_1_x_bhpt_direct.wls
src/schwgw/validation/phase6_v3_mode_greybody_external_direct_replacement.py
tests/unit/test_phase6_v3_external_direct.py
tests/regression/test_phase6_v3_external_direct_publication.py
```

Implement only the frozen exact `$CommandLine` parser and non-circular future
review authority described by the design.  Preserve formulas, serialization,
source loading, numerical method, graph, overlays, precision, admission
criteria, environment, CLI, core, package, V3.0 and protected files.

Add positive and adversarial tests.  Run one real-Wolfram zero-science
handshake with an absent request: exact launch must pass argv parsing, emit
only `request must exist and output must be absent`, return 73 and make zero
imports/source loads/solver calls.  Test missing/extra/reordered/alternate
argv forms fail before request import.  Use only temporary paths and remove
them after the probe.

Run exact CPython3.14 focused/adjacent tests under the frozen overlay, Ruff
check/format, in-memory compile and four-path diff-check.  Rehash authorities
at end and return exact before/after hashes, test counts and zero-science
evidence.  Do not contact T7, launch a scientific node, create a dispatch or
real root, edit status/handoffs, reuse the failed sentinel, or start V3.2.

End successful work with:

```text
CHECKPOINT / V3.1-X SENTINEL REPAIR CYCLE 1 IMPLEMENTATION READY FOR T7 DELTA REVIEW
```

