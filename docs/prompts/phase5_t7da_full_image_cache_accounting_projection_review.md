# T7da — Full-Image Cache-Accounting Projection Package Review

Work only in existing T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed` using
`gpt-5.6-sol/high`. Never use `max` or `ultra`. Do not create a task,
subagent, proxy or descendant.

This is an independent read-only review. Do not modify files, refs,
artifacts, status, handoffs, permissions, environment or processes. Do not
execute any wrapper, producer, audit, solver or matrix command.

## Identity Gate

Bind only the candidate supplied by root T0. Require parent:

```text
e27d07cc7bf99a26b9556daab4738cd1f581af2c
```

and primary HEAD:

```text
45face32f52537ac4b8ab79cb1746d8ac78e9b82
```

Require exactly four added paths relative to parent:

```text
docs/superpowers/specs/2026-07-28-t4ba-full-image-cache-accounting-projection-repair-design.md
docs/superpowers/plans/2026-07-28-t4ba-full-image-cache-accounting-projection-repair.md
docs/prompts/phase5_t7da_full_image_cache_accounting_projection_review.md
docs/prompts/phase5_t4ba_full_image_cache_accounting_projection_repair.md
```

Verify candidate/ref/parent, exact commit/worktree bytes, blob/SHA/size and
both diff-checks. Reject every superseded/unreferenced identity.

## Independent Consumed-Unit Review

Reconstruct the exact full unit from raw files. Verify:

- request/prelaunch/running/receipt/final/stream identities from the frozen
  design;
- supervisor 34980 and child PID/SID/PGID 34989;
- natural exit1/no signal/exact `Popen.wait`/reaped/PG-empty/wait-errors0;
- elapsed `5176352829541` ns;
- 216-byte start-only stdout and 1712-byte exact stderr ending in
  `full-image call/cache/true-solve contract mismatch`;
- 3234 regular files, 363 directories inclusive, 627046494 bytes, zero
  symlink/non-one-link file and exact tab-ledger `330dfdcf...520b`;
- matching official audit0, checkpoint0 and full pair absent;
- consumed authorization and immutable/non-reusable unit;
- canonical five accepted pairs exact and no output transient/process.

Do not treat the lost in-memory result as scientific PASS or reusable data.

## Independent Arithmetic and Legacy Review

Read the unchanged runner, regression test, exact config and immutable legacy
sidecar directly.

Independently prove:

1. runner/test literal is exactly `10367742`;
2. grid is 241x241, mask gives 197 invalid and 57884 valid points;
3. convergence is four lmax values times 12 probes, or 48 calls;
4. polarization calls are 57932;
5. final grid requests 358 keys; convergence requests
   `214/262/310/358`;
6. exact hit formula gives `20735842`;
7. legacy raw record independently contains exact hit/call/solve values
   `20735842/57932/358`;
8. no accepted specification freezes the erroneous reduction;
9. failure occurs after compute but before comparisons/publication.

Classify it only as a deterministic control-accounting defect.

## Projection Contract Review

Require every frozen T4ba gate:

1. Runner/test/config/legacy/golden/scientific source remain byte-identical.
2. Wrapper loads exact T4az, preserves T4ax/T4ar behavior and intercepts
   only exact T4ar adapter loading.
3. Wrapped `adapter._load_runner` calls original once and verifies the
   unchanged named runner before assignment.
4. Only exact loaded-module
   `FULL_IMAGE_OPTIMIZED_CACHE_HIT_COUNT` changes from exact old to exact
   derived/legacy value.
5. Formula, config, sidecar and call/solve counts are independently bound.
6. Only exact full-image run/audit routes accept; every
   frequency/preflight/cross-case/arbitrary route rejects.
7. Physical/logical argv and producer/audit/replay provenance are exact.
8. Wrong paths/hashes/stats/links/aliases/literals/formula fields and all
   argument/environment/fallback selection fail closed.
9. No comparison, budget, performance, array, metadata or publication rule
   changes.
10. Phase A is exact-Python3.14 zero-science and stops for T0 audit.
11. Any later launched failure remains one-shot/no-retry.

Freshly verify runner, driver, T4az shim, implementation, runtime,
complete290, fixed identity, five canonical pairs/checkpoints, full absence,
external/process/transient/link guards.

## Decision

If and only if every gate passes, return exact:

```text
REVIEW GREEN / T0 FULL-IMAGE CACHE ACCOUNTING PROJECTION REPAIR APPROVED
```

Bind the exact candidate/ref/parent and report independently derived
evidence. State GREEN authorizes only root-T0 fresh verification and possible
later T4ba zero-science Phase A, not a full producer or audit.

Otherwise return YELLOW/RED with the smallest bounded blocker and required
correction. Do not dispatch T4.
