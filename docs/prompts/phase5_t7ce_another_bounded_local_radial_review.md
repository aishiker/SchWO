# T7ce — Another Bounded Local Radial Gate Independent Review

Work only in existing T7 task `019f5ed1-b421-7ec2-9bac-8d134855a1ed` as a
read-only reviewer. Do not create a task, repair implementation/artifacts, or
start T8as. Normal review uses `gpt-5.6-sol/high`.

## Start Gate And Required Reading

Fully read `project.md`, `status.md`, current T0/T4/T7/T8 handoffs, the exact
reviewed candidate design/plans/prompts, T4ad gate note/artifacts, T7cd
evidence, immutable T8ar/T4ac sources, and earlier bound sources. Verify the
candidate identity and require exact
`GREEN / ANOTHER BOUNDED LOCAL RADIAL GATE READY`.

Before replacing T7 handoff, archive its exact bytes at:

```text
docs/handoffs/archive/T7_2026-07-20_pre_t7ce_another_bounded_local_radial_review.md
```

The pre-review bytes must have SHA-256
`99b669d8f7b1000dd9f002c27d1234a286d00690ad82c84f1ebc159ec33bb3fc`.

## Allowed Writes

Only `status.md`, `docs/handoffs/T7_current.md`, and the archive path above.

## Independent Checks

1. Re-derive exactly 55 unique failed-child midpoints from all 253 T7cd
   hierarchy failures; require all five phase failures and no passing interval.
2. Recompute tokens/lmax windows/max 360 and exact 239,120 classification.
3. Require 55 complete/PASS checkpoints, canonical hashes/contracts, exact
   manifest membership, and no temp/quarantine.
4. Independently reconstruct classification, all sector-aware transition
   groups including explicit empty groups, direct oracle, anchors, sensitivity,
   and preflight.
5. Verify both snapshots, immutable inputs, parent radial blob, exact five
   commit blobs, and snapshot bridge.
6. Expand the literal envelope without helper import; require exact keys,
   both sectors, all points/tokens/ells/boundaries, no IO or inferred
   membership, and strict fail-closed behavior.
7. Require default-covered zero calls and integrated adapter/direct saved-value
   identity.
8. Run focused tests, exact-five Ruff, fresh full pytest, `git diff --check`,
   exact scope/source checks, warning classification, and empty forbidden
   output searches.

## Exact Decision

Return exactly one:

```text
ACCEPT GREEN / ANOTHER BOUNDED LOCAL RADIAL GATE ACCEPTED
ACCEPT YELLOW / ANOTHER BOUNDED LOCAL RADIAL EVIDENCE INCOMPLETE
REJECT RED / ANOTHER BOUNDED LOCAL RADIAL GATE INVALID
```

GREEN requires every artifact/oracle/snapshot/adapter/test/scope/provenance
check. Report exact counts/hashes/maxima and changed review-only paths. Return
only to T0 and never start T8as.

## Forbidden

No repair, threshold relaxation, lmax extension, automatic/recursive midpoint,
uniform/full grid, amplification, production, plot, fixture, Kirchhoff,
paper-style output, task creation/replacement, or GitHub action.
