# T7cx — Read-Only Legacy Reference-View Package Review

Work only in existing T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed` using
`gpt-5.6-sol/high`. Never use `max` or `ultra`. Do not create a task,
subagent, proxy or descendant.

This is an independent read-only review. Do not modify files, refs,
artifacts, status, handoffs, permissions, environment or processes. Do not
execute any launcher, producer, audit, solver or matrix command.

## Identity Gate

Bind only the candidate supplied by root T0:

```text
parent
  3ac5c9daf53b03ae1dc3cb5da3051aad77fd6113
ref
  refs/heads/codex/t4au-read-only-legacy-reference-view-repair-candidate
primary HEAD
  45face32f52537ac4b8ab79cb1746d8ac78e9b82
```

Require exactly four added paths relative to parent:

```text
docs/superpowers/specs/2026-07-27-t4au-read-only-legacy-reference-view-repair-design.md
docs/superpowers/plans/2026-07-27-t4au-read-only-legacy-reference-view-repair.md
docs/prompts/phase5_t7cx_read_only_legacy_reference_view_review.md
docs/prompts/phase5_t4au_read_only_legacy_reference_view_repair.md
```

Verify commit/worktree bytes, blob/SHA/size and both diff-checks. Reject any
superseded or unreferenced identity.

## Independent Failure Review

Reconstruct the consumed T4at Phase-B root from raw files. Verify:

- request/setup/source/input/prelaunch/running/receipt/streams/failure/
  failure-audit/checkpoint identities;
- PID/SID/PGID `50124`, exit64/no signal, exact wait/reap/PG-empty;
- exact empty stdout and 294-byte stderr
  `82f5123f...b5497`;
- child detail is writable legacy sidecar;
- failure audit `valid=true`, exact runtime/source/input/canonical guards;
- producer1, matching audit0, runner0, official audit0, solver0, matrix0;
- no isolated pair and consumed authorization;
- immutable tree counts/bytes/content index and no link/live process.

Inspect the frozen launcher directly. Prove the read-only sidecar rejection
is reached before `_compute_frequency()`. Independently verify primary
legacy NPZ/sidecar/golden hashes, sizes, modes and full identities. Do not
reinterpret the supervisor's generic missing-pair reason as the child cause.

## Repair Contract Review

Require all of the following:

1. No primary artifact chmod, replacement, mutation or permission
   relaxation.
2. Exactly one fresh attempt-local reference root with exactly three fixed
   relative target paths.
3. Exclusive byte copies, exact hashes/sizes, mode0400, regular,
   non-symlink, nlink-1, target/target and source/target inode disjointness.
4. Exact source pre/post full identities and closed target inventory.
5. Deterministic manifest and dedicated driver serializer; launcher stdout
   serializer remains separate.
6. Old launcher immutable; one new Phase-A launcher with required
   `--legacy-reference-root` only.
7. No arbitrary individual paths, fallback, glob/prefix/regex/basename/
   case/Unicode alias, env hook or import hook.
8. Explicit dataflow from derived view paths through static validation,
   sidecar load, array comparison, run and audit.
9. Unchanged one `_compute_frequency()` call, arguments, warning capture,
   22-array criteria and pair/audit semantics.
10. Reachable old-launcher output-mode defect is closed without byte drift:
    producer sets both isolated NPZ/JSON to mode0400 before atomic final
    publication and PASS; audit requires those exact identities.
11. Driver request/prelaunch/running/final/audits bind primary sources,
    reference manifest/root/targets, launcher, roots and runtime.
12. Producer and matching audit require the exact same view and reject all
    drift/alias/writability/link/content/path/root cases.
13. T4at clean-source/complete-input split remains exact.
14. Phase A is exact-Python3.14 zero-science and stops for T0 audit.
15. Any later launched failure remains one-shot/no-retry.

Freshly verify driver `efe256d2...e01bd`, old launcher
`20bd7e78...79c6`, T4at checkpoint, runtime, complete290, legacy/golden/
warning, fixed identity, relocated witness, canonical four, eight absences,
external/process/transient/link guards.

## Decision

If and only if every gate passes, return exact:

```text
REVIEW GREEN / T0 READ-ONLY LEGACY REFERENCE VIEW REPAIR APPROVED
```

Bind the exact candidate/ref/parent and report all independently derived
evidence. State clearly that GREEN authorizes only root-T0 fresh verification
and possible later T4au Phase A, not launcher execution or science.

Otherwise return YELLOW/RED with the smallest concrete bounded blocker and
required correction. Do not dispatch T4.
