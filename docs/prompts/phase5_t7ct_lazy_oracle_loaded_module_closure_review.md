# T7ct — Lazy-Oracle Loaded-Module Closure Review

Work only in existing T7 task
`019f5ed1-b421-7ec2-9bac-8d134855a1ed` using
`gpt-5.6-sol/high`. Never use `max` or `ultra`. Do not create a task,
subagent, proxy, or descendant.

## Scope

Perform one strict read-only review of the exact T4aq candidate supplied by
root T0. Read project/status/current T0/T4/T7/T8 handoffs, the exact T4aq
design/plan/prompts, T4ap/T4ai packages, the consumed matrix unit, driver,
runner, legacy `2.91875` sidecar, runtime overlay, complete-290 manifests,
witness, canonical output, and process/transient state.

Do not modify files, refs, artifacts, status, handoffs, permissions,
environment, or processes. Do not launch run/audit mode, solver, witness,
or matrix science. Exact Python-3.14 import-only reconstruction with
`PYTHONDONTWRITEBYTECODE=1` is permitted.

## Required Independent Checks

1. Verify candidate/ref/parent/primary, exact-four added scope,
   commit/worktree bytes, exact blobs/SHA/sizes and `git diff --check`.
2. Reconstruct request/prelaunch/running/final/receipt/stream/checkpoint
   hashes and exact terminal state for the consumed `2.91875` unit.
3. Verify child `59291` exit1/no-signal/exact-wait/reaped/PG-empty,
   normal durable audit `valid=true`, official audit absent, later units
   unstarted, canonical four unchanged and all eight later files absent.
4. Verify the exact failure message is the generic post-compute frozen
   identity error; do not accept an unsupported retrospective diagnosis.
5. Independently load the frozen legacy `2.91875` sidecar and confirm
   adapter-use count `98`, exact adapter/warning identity, and the
   corresponding lazy import in unchanged `radial_solver.py`.
6. Under exact Python/runtime/detached input view, reproduce:
   initial 44/`190176...`; exact oracle import; added exact package/module;
   raw 46/`9dc258...`.
7. Recompute all other identity components after the import and require
   exact equality. This establishes the causal failing condition.
8. Review the stable projection: the original verifier must validate every
   module path/content/blob before exactly two names are removed only from
   the set digest. Reject prefix/glob/regex/argument-controlled filtering.
9. Require normalized 44/`190176...` for both absent and exact-pair-present
   states, reject a partial pair and every other module-set change.
10. Verify exact oracle package/module source hashes and primary origins.
11. Review the immutable file launcher: named-module runner load, exact
    one-function wrapper, unchanged logical `sys.argv`, one `main()` call,
    return-code propagation, no `-c`/stdin/sitecustomize/import-hook/overlay
    mutation.
12. Verify the durable driver binds launcher and logical runner separately,
    tracks exact process identity, and requires producer/audit equality.
13. Verify existing canonical sidecars remain valid at
    `190176...`; no sidecar or canonical file may be rewritten.
14. Verify positive/negative/static/dataflow tests cover exact import
    states, extra/missing/aliased modules, wrong source, launcher drift,
    argv/process mismatch, final/replay equality, and unadapted failure.
15. Verify Phase A is zero-science and ends at a checkpoint; a new matrix
    one-shot requires later root-T0 audit and authorization.
16. Freshly verify runner/source/runtime/complete-input/fixed identity,
    witness, canonical/absence/external/process/transient guards.

## Decision

Return exactly one:

```text
REVIEW GREEN / T0 LAZY-ORACLE LOADED-MODULE CLOSURE REPAIR APPROVED
REVIEW YELLOW / T0 LAZY-ORACLE LOADED-MODULE CLOSURE CHANGES REQUIRED
REVIEW RED / T0 LAZY-ORACLE LOADED-MODULE CLOSURE UNSAFE
```

Bind the decision to the exact candidate and report independent evidence,
blockers, and prohibited actions. GREEN authorizes only root-T0 fresh
verification and possible later T4aq Phase A. It does not authorize
launcher execution, producer, official audit, solver, canonical write,
matrix retry, T7ch, or downstream work.
