# T4ag — Hermetic Runtime-Provenance Repair

Work only in existing T4 task
`019f5fa6-1288-7c01-8a87-4c4370cf5517`. Reuse only the existing local proxy
`019f9be5-2deb-7c52-b740-a0db8fa387ca`. Do not create a task, subagent,
proxy, or descendant.

## Start Gate

Fully read and verify:

```text
docs/superpowers/specs/
  2026-07-26-t4ag-hermetic-runtime-provenance-repair-design.md
docs/superpowers/plans/
  2026-07-26-t4ag-hermetic-runtime-provenance-repair.md
```

Also read project/status/current handoffs, frozen T4ae/T4af packages, every
T4af driver/preflight/disposition record, and the complete failed witness.

Require the exact T4ag candidate identity supplied by T0 and exact:

```text
REVIEW GREEN / T0 RUNTIME REPAIR PACKAGE APPROVED
```

Verify candidate/ref/parent/exact-four, primary/exact-fourteen, runner,
benchmark/environment/Python, old driver/evidence, failed witness,
canonical hash/stat/inodes, remaining absent units, and zero unexpected
process/transient state. Stop on mismatch.

## Phase A — Runtime Overlay And Driver Freeze

Using `gpt-5.6-sol/ultra`:

1. preserve the T4af RED evidence and old driver/preflights immutably;
2. build the exact SciPy 1.17.1 plus PyYAML 6.0.3 content-addressed,
   read-only overlay under a fresh unique control-root directory;
3. modify only the artifact-local durable driver and solver-free tests to
   bind and validate the overlay and exact environment;
4. retain all existing process/source/input/audit/canonical/scientific
   semantics;
5. use the single proxy only for bounded read-only control-plane review;
6. run the complete exact-Python-3.14 import-only and full zero-science
   preflight required by the reviewed design.

No scientific runner CLI, official audit, solver, witness, matrix, or
canonical write is allowed in Phase A.

Return exactly:

```text
CHECKPOINT / HERMETIC RUNTIME ENVIRONMENT FROZEN
```

Include exact builder/driver/overlay/runtime/preflight/raw/capture/package
identities, tests and rejection counts, import origins, zero-science counts,
canonical and process guards, and:

```text
new_subagents_created_total_after_policy=1
proxy_descendants=0
existing_reviewers_reused=1
```

Then pause for root T0.

## Phase B — New Exact T0 Authorization Required

Proceed only after root T0 sends exactly:

```text
AUTHORIZED / HERMETIC-RUNTIME CONTROL-PROVENANCE WITNESS COMPUTE
```

Run exactly one new prospective isolated existing `kM=1.58125` witness under
the frozen overlay and driver. Apply every frozen T4af durable, detached
source, exhaustive input, path, scientific, official-audit, equivalence,
metadata, warning, cache, fingerprint, atomicity, and canonical-read-only
check. Do not retry on failure.

After complete PASS, pause and return:

```text
CHECKPOINT / CONTROL-PROVENANCE WITNESS PASSED
```

Root T0 must freshly verify it.

## Conditional Matrix

Only exact:

```text
AUTHORIZED / ORIGINAL T4AE MATRIX CONTINUATION
```

permits the still-absent fixed order:

```text
2.91875 -> 3.759375 -> 3.89375 -> full 241x241
```

Use the same frozen overlay and durable driver for each producer and matching
official audit. Validate each complete unit before starting the next.

## Stop Conditions

Any package, source, dependency, builder, overlay, import, driver, process,
stderr, scientific, identity, path, canonical, test, or evidence anomaly
stops immediately and returns to root T0. No retry, cleanup, global install,
network access, criterion relaxation, frozen-file edit, new frequency,
production/plot/fixture/Kirchhoff/paper/GitHub work, or new agent is
authorized.

Only final T4 exact GREEN plus root-T0 full T4ae PASS permits frozen T7ch.
