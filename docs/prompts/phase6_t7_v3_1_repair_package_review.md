# Phase 6 V3.1 — formal T7 repair-cycle-1 package review

Date frozen by T0: 2026-08-11

Review class: read-only, identity-bound repair-package review

You are the existing formal T7 independent review task. This is not the V3.1
scientific delta review.

Read `project.md`, the current status/T0/T4/T7 handoffs, the liveness protocol,
the complete initial V3.1 T7 archive, the original frozen V3.0/V3.1 materials,
and every file listed in the repair package manifest supplied by Root T0.

Independently verify that the unchanged package:

1. addresses both and only the frozen class-A blockers;
2. preserves all passed items and protected identities;
3. changes no convention, formula, domain, selector, threshold or acceptance
   operator;
4. never modifies or consumes either failed `r1` root;
5. provides a complete success path for 496 Route-A modes, 9,920 Route-A
   nodes, 102 AP keys, 458 AP nodes, 23 external records, all 16 thresholds and
   exactly five certificates;
6. uses the byte-frozen pole-safe public backend only for Route A while keeping
   AP and BHPT algorithms independent and provenance-qualified;
7. closes the JSONL and source-ledger class-C defects;
8. has sufficient synthetic, real-sentinel, runtime, checkpoint, immutable
   publication and full-suite gates before any accepted claim;
9. stops before official science if a protected change or infeasible route is
   required;
10. obeys repair cycle `1 of 2` and does not authorize V3.2/global GREEN.

Do not edit package files, science code, configs, artifacts, status or T0/T4
handoffs. Do not run numerical science, implement the repair, dispatch T4 or
start V3.2. You may update only T7 current and one new dated T7 archive.

Approve only with:

```text
ADVANCE_DECISION: ADVANCE
CLAIM_STATUS: NOT_ASSESSED
GATE_LABEL: ACCEPT GREEN / V3.1 REPAIR CYCLE 1 PACKAGE READY FOR T4
```

This means the bounded plan is safe to execute; it is not V3.1 scientific
acceptance. Otherwise return `REPAIR / NOT_ASSESSED` with complete class-A
finding fields, or `ESCALATE` if the bounded scope is intrinsically invalid.
Bind the exact package-manifest SHA and all member hashes in the verdict.

